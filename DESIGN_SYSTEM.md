---
status: normative
last_verified: 2026-09-24
---

# Mentalix — Design System

Смысловые SVG-иллюстрации и motion-поведение дополнительно зафиксированы в `MENTALIX_SEMANTIC_MOTION.md`. Для новых карточек оба документа являются единым source of truth.

Статус: описание фактического состояния `main`, а не редизайн, кроме §5.1–§5.3
и §6 (целевые спецификации; эталон — `docs/references/stoic-video-2026-09-23.md`).

Card System v2 в §5.1 — целевая нормативная спецификация карточек и нижней навигации.

## 1. Источник правды

До согласованной миграции фактический источник токенов:

1. `src/index.css`;
2. `tailwind.config.js`;
3. используемые компоненты.

До отдельного продуктового решения код и `DESIGN_SYSTEM.md` являются
источником правды для визуальных токенов. Документ должен следовать коду. Исключение: §5.1 Card System v2 — целевая
спецификация; код приводится к ней, а расхождения ведутся в §8.
Любое изменение токенов или шрифта — отдельная визуальная задача с явным
согласованием.

## 2. Фактические токены

### Тёмная тема

| Роль                     |                               CSS |                            Hex |
| ------------------------ | --------------------------------: | -----------------------------: |
| Основной фон             |                   `--c-bg: 5 4 3` |                      `#050403` |
| Карточка                 |                 `--c-card: 9 9 9` |                      `#090909` |
| Вторичная поверхность    |             `--c-card2: 17 17 17` |                      `#111111` |
| Активная статус-карточка |             `--c-card3: 46 46 46` |                      `#2E2E2E` |
| Нижняя навигация         |               `--c-nav: 28 28 28` |                      `#1C1C1C` |
| Граница навигации        |        `--c-nav-border: 51 51 51` |                      `#333333` |
| Активная навигация       |        `--c-nav-active: 58 58 58` |                      `#3A3A3A` |
| Основной текст           |           `--c-text: 243 243 243` |                      `#F3F3F3` |
| Вторичный текст          |          `--c-muted: 133 133 133` |                      `#858585` |
| Третий уровень текста    |          `--c-faint: 100 100 100` |                      `#646464` |
| Граница                  |            `--c-border: 41 41 41` |                      `#292929` |
| Основная линия           |           `--c-line: 230 230 230` |                      `#E6E6E6` |
| Вторичная линия          | `--c-line-secondary: 106 106 106` |                      `#6A6A6A` |
| Золотой акцент           |            `--c-gold: 237 189 96` |                      `#EDBD60` |
| Лазурный акцент          |          `--c-azure: 111 183 224` | `#6FB7E0` (временное значение) |
| CTA фон                  |                        `--btn-bg` |                      `#EDEDED` |
| CTA текст                |                      `--btn-text` |                      `#111111` |

Mentalix использует только тёмную тему. Автоматического дневного режима и
пользовательского переключателя темы нет.

## 3. Типографика

Фактически загружаются:

- Onest 400/500/600/700/800 — весь основной интерфейс;
- Manrope 500/600 — вторичный шрифт, только для лейблов/эйбраузов;
- JetBrains Mono 400/500 — моноширинные случаи.

`font-display` и `font-body` оба указывают на Onest. Заголовки отличаются весом и трекингом, а не гарнитурой.

До 30.07.2026 в коде стоял Manrope, хотя дизайн-система с самого начала указывала Onest. Расхождение устранено в пользу дизайн-системы.

### Manrope — вторичный шрифт лейблов

С 28.08.2026 `font-label` (Manrope 500/600) — отдельный токен для малых
caps-подписей: эйбраузов над заголовком карточки (например «Тема недели»
над заголовком карточки темы) и секционных caps-лейблов («Цифры»,
«Данные», категории практик). Решение принято владельцем по аналогии со
Stoic.

Область применения `font-label` — только эти подписи. Заголовки карточек,
основной текст и текст кнопок остаются на Onest (`font-display`/`font-body`)
без изменений. Подключение — самостоятельный хостинг через npm-пакет
`@fontsource/manrope` (веса `500.css`/`600.css`, кириллица включена по
unicode-range), без Google Fonts CDN — см. `src/index.css`.

Минимальный размер интерактивных полей ввода — 16 px. Это не эстетическое, а техническое требование: iOS увеличивает страницу при фокусе в поле мельче шестнадцати. Именно это раньше выглядело как «случайный зум».

### MXL-TYPE-CONSISTENCY-001 — единая шкала размеров текста

Кроме гарнитур (выше) и шкалы контраста (ниже), в `src/index.css` зафиксирована единая оптическая шкала размеров для пяти основных вкладок — проверяемый контракт, одинаковый для «Сегодня», «Практик», «Наставника», «Библиотеки» и «Прогресса». Значения фиксированы намеренно и не подбираются на глаз под конкретный экран.

Базовые роли (`--mx-type-*-size` как CSS-переменные в `:root`):

| Класс               |           Размер | Вес | Line-height |  Трекинг | Где используется                                           |
| ------------------- | ---------------: | --: | ----------: | -------: | ---------------------------------------------------------- |
| `.mx-type-page`     |  30px (1.875rem) | 700 |           1 | -0.035em | заголовок вкладки: «прогресс.», «библиотека.», «практики.» |
| `.mx-type-greeting` |  18px (1.125rem) | 700 |        1.05 | -0.025em | приветствие на «Сегодня»                                   |
| `.mx-type-hero`     |   28px (1.75rem) | 800 |        1.12 | -0.025em | hero-текст главной карточки                                |
| `.mx-type-section`  |  18px (1.125rem) | 600 |         1.2 |  -0.02em | заголовок секции                                           |
| `.mx-type-card`     |      16px (1rem) | 600 |        1.28 | -0.015em | заголовок карточки                                         |
| `.mx-type-body`     |  14px (0.875rem) | 400 |        1.45 |        — | основной текст                                             |
| `.mx-type-control`  |      16px (1rem) | 600 |         1.2 |  -0.01em | текст в элементах управления                               |
| `.mx-type-segment`  |  14px (0.875rem) | 600 |         1.2 |        — | сегмент-переключатели                                      |
| `.mx-type-meta`     | 11px (0.6875rem) | 600 |         1.2 |        — | мета-текст                                                 |

Дополнительные роли с фиксированными (не токенизированными) значениями:

| Класс                                               |           Размер | Вес | Line-height |  Трекинг | Где используется                                                              |
| --------------------------------------------------- | ---------------: | --: | ----------: | -------: | ----------------------------------------------------------------------------- |
| `.mx-type-weekday`                                  |  10px (0.625rem) | 600 |        1.15 |   0.01em | Today: подпись дня недели — тихий ориентир, не второй hero                    |
| `.mx-type-calendar-date`                            | 13px (0.8125rem) | 600 |         1.1 |  -0.01em | дата в календаре                                                              |
| `.mx-type-insight`                                  | 19px (1.1875rem) | 600 |         1.3 |  -0.02em | карточки наблюдений/инсайтов (Прогресс)                                       |
| `.mx-type-list-title`, `.mx-type-analytics-heading` | 15px (0.9375rem) | 600 |        1.25 |  -0.01em | компактные заголовки каталога/контента (v4)                                   |
| `.mx-type-persona-title`¹                           |  18px (1.125rem) | 600 |         1.2 | -0.015em | заголовок AI-персоны                                                          |
| `.mx-type-list-body`, `.mx-type-persona-body`       | 13px (0.8125rem) | 400 |         1.4 |        — | тело в списках/у персон                                                       |
| `.mx-type-article-title`                            |      16px (1rem) | 600 |        1.25 | -0.015em | заголовок статьи                                                              |
| `.mx-type-article-body`                             |   12px (0.75rem) | 400 |         1.4 |        — | тело статьи                                                                   |
| `.mx-type-article-action`                           |   12px (0.75rem) | 600 |         1.2 |        — | действие в карточке статьи                                                    |
| `.mx-type-article-meta`                             |  10px (0.625rem) | 500 |         1.2 |        — | мета статьи                                                                   |
| `.mx-type-flow-title`                               |  22px (1.375rem) | 600 |        1.15 |  -0.02em | заголовок флоу (v5, AI-экраны)                                                |
| `.mx-type-flow-body`                                | 13px (0.8125rem) | 400 |        1.45 |        — | тело флоу                                                                     |
| `.mx-type-flow-action`                              |  14px (0.875rem) | 600 |         1.2 |  -0.01em | действие флоу                                                                 |
| `.mx-type-checkin-title`                            |    24px (1.5rem) | 700 |         1.3 |        — | заголовок карточки дня (тип A, §5.1)                                          |
| `.mx-type-checkin-label`                            | 13px (0.8125rem) | 400 |        1.25 |        — | надпись над заголовком карточки дня (тип A)                                   |
| `.mx-type-tab`                                      |   12px (0.75rem) | 500 |         1.2 |        — | подпись пункта нижней навигации; добавить в `src/index.css` при реализации v2 |

¹ `.mx-type-persona-title` объявлен в `index.css` дважды: сначала в общей группе с `.mx-type-list-title`/`.mx-type-analytics-heading` (15px), затем отдельным правилом чуть ниже, которое переопределяет размер до 18px. В итоговом computed style побеждает 18px — именно это значение указано в таблице выше как фактическое.

Не вводить новые локальные `font-size` в компонентах верхнеуровневых экранов — переиспользовать один из существующих классов `.mx-type-*`. Если нужной роли нет — это отдельная задача на расширение шкалы, а не повод хардкодить размер на месте.

## 4. Семантика цвета

Цветовой акцент — динамическая пара Gold (`--c-gold`) ↔ Azure (`--c-azure`): прогресс,
завершение, значимая цифра или действие. Выбранный пользователем или темой цвет
не меняет смысл композиции; ни один из акцентных цветов нельзя использовать как
произвольный декор.

**Переключаемый акцент интерфейса** (`src/lib/accentColor.js`, атрибут `data-accent`
на `<html>`, `src/App.jsx`) — это фактически реализованный, выбираемый в Настройках
список, отдельный от токена `--c-azure` из §2 (тот используется только в
второстепенных местах — курсор `PracticeWritingCanvas.css`, UI Lab — и к этому
переключателю отношения не имеет):

| id                    | Название в UI | Hex       |
| --------------------- | ------------- | --------- |
| `gold` (по умолчанию) | «Золотой»     | `#EDBD60` |
| `ice`                 | «Лазурный»    | `#5EB2ED` |

Переключение — CSS-override `--c-gold` внутри `[data-accent='ice']` (`src/index.css`).

Legacy-имена Tailwind сейчас являются алиасами:

| Старое имя              | Фактическая роль      |
| ----------------------- | --------------------- |
| `emerald-deep`          | фон                   |
| `emerald`               | карточка              |
| `emerald-light`         | вторичная поверхность |
| `cream`, `sage`, `mint` | текст                 |
| `gold`, `cognac`        | золото                |

Новые компоненты не должны вводить новые legacy-имена. Переименование существующих — отдельная техническая миграция.

### Шкала текста

Иерархия текста задаётся тремя токенами, а не прозрачностью. Прозрачность
меняет контраст непредсказуемо: `text-cream/35` даёт 2.8:1 и не ����итается,
хотя выглядит в коде безобидно.

| Класс        | Токен       | Контраст на фоне | Где применяется                            |
| ------------ | ----------- | ---------------- | ------------------------------------------ |
| `text-cream` | `--c-text`  | 15.1:1           | основной текст, заголовки                  |
| `text-muted` | `--c-muted` | 5.69:1           | вторичный текст любого размера             |
| `text-faint` | `--c-faint` | 3.55:1           | только крупный: 18px+ либо 14px полужирный |

Ниже `text-faint` не опускаться. Если элемент должен быть тише, чем
`faint`, — это ошибка иерархии: тише делают размером, весом и отступом,
а не контрастом.

Прозрачность у текста допустима только как **анимация** — появление,
затухание, — но не как постоянное состояние.

Миграция идёт поэкранно: в коде остаются места на `text-cream/NN`,
и они постепенно переводятся на шкалу.

## 5. Форма и композиция

Рабочая шкала радиусов:

- 32 px — крупный hero;
- 28 px — крупная карточка;
- 24 px — обычная карточка;
- 16 px — вложенный блок;
- `999px/full` — круглые кнопки и pill.

Для карточек и навигации действуют значения §5.1: 24px и 999px. Значения
28px и 32px остаются только для экранов, ещё не описанных в v2.

Композиция mobile-first, обычно `max-w-md`, много воздуха, одна главная CTA в состоянии экрана.

Все иллюстрированные карточки используют единый визуальный язык: спокойная
символическая flat-иллюстрация, собранная из крупных форм, с тонкой белой и серой
контурной графикой на поверхности карточки и не более одной смысловой точки.
Акцентная точка использует выбранный цвет Gold или Azure и обозначает фокус,
цель или активное состояние, а не заполняет весь рисунок. Полностью акцентные
иллюстрации не используются. Светящиеся плашки под
рисунком — тоже: свечение делает подложку самостоятельным объектом и спорит
с самой иллюстрацией.

Тёмная подложка `bg-artbed` при этом разрешена и является рабочим приёмом:
область под рисунком остаётся тёмной в обеих темах — окно в ночь внутри
карточки. Условие одно: карточка под ней залита плоским цветом. Если
поверхность карточки имеет градиент, плоская подложка отделяется от неё и
читается как вставленный прямоугольник — именно так это выглядело на
«Сегодня» до 06.08.2026, и лечилось снятием градиента, а не подложки.

Градиенты поверхностей вне таблицы токенов не вводятся. Единственное
исключение — главная карточка дня в состоянии A-активная (§5.1).

### Отступы, радиусы и тени — точные значения

**Отступы `--mx-space-*` (CSS-переменные в `:root`, `src/index.css`):**

| Токен           | Значение |
| --------------- | -------: |
| `--mx-space-1`  |      4px |
| `--mx-space-2`  |      8px |
| `--mx-space-3`  |     12px |
| `--mx-space-4`  |     16px |
| `--mx-space-5`  |     20px |
| `--mx-space-6`  |     24px |
| `--mx-space-7`  |     28px |
| `--mx-space-8`  |     32px |
| `--mx-space-10` |     40px |

Шаг `--mx-space-9` (36px) в шкале отсутствует — переход идёт сразу с 32px на 40px.

**Радиусы — именованные токены:**

| Токен                 | Значение | Роль                   |
| --------------------- | -------: | ---------------------- |
| `--mx-radius-control` |     16px | вложенный блок/контрол |
| `--mx-radius-card`    |     24px | обычная карточка       |
| `--mx-radius-hero`    |     32px | крупный hero           |
| `--mx-radius-pill`    |    999px | pill/круглые кнопки    |

Промежуточный шаг 28px («крупная карточка», см. шкалу радиусов выше) в коде
реально используется (`Library.css`, `TodayFocusCard.jsx`, `Rituals.jsx` и др.),
но не вынесен в отдельный именованный токен — применяется как литеральное
значение (`border-radius: 28px` / `rounded-[28px]`). Не ��шибка, но стоит иметь
в виду при рефакторинге.

**Тени (тёмная тема):**

| Токен                    | Значение                         |
| ------------------------ | -------------------------------- |
| `--shadow-float`         | `0 8px 32px rgba(0, 0, 0, 0.35)` |
| `--shadow-float-compact` | `0 6px 26px rgba(0, 0, 0, 0.45)` |

**Анимации (`@keyframes` в `src/index.css`):**

| Keyframe   | Длительность/easing       | Где используется          |
| ---------- | ------------------------- | ------------------------- |
| `mx-float` | 4.5s ease-in-out infinite | плавающие иллюстрации     |
| `mx-sway`  | 3.2s ease-in-out infinite | покачивание иллюстраций   |
| `mx-glow`  | 3s ease-in-out infinite   | золотое свечение          |
| `mx-draw`  | 1.4s ease-out forwards    | «прорисовка» контура      |
| `fadeIn`   | 0.22s ease-out both       | появление экрана/карточки |

### 5.1. Mentalix Card System v2 — геометрия Stoic, смысл Mentalix

Решение владельца от 22.09.2026. Заменяет Card System v1 целиком.
Раздел нормативный: код приводится к нему, а не наоборот. Пока код не
догнал спецификацию, расхождения фиксируются в §8.

#### Главное правило

Эталон геометрии карточек и нижней навигации — приложение **Stoic** (iOS).
Задача агента: попасть в размеры, пропорции и плотность Stoic один в один,
оставив содержание и смысл Mentalix. Карточки статей остаются отдельным
утверждённым эталоном; v2 на них не распространяется без согласования владельца.

| Берём из Stoic один в один                   | Не берём из Stoic                                |
| -------------------------------------------- | ------------------------------------------------ |
| ширину, высоту и пропорции карточек          | тексты и формулировки (в том числе переводы)     |
| радиусы, толщину рамок, внутренние отступы   | иллюстрации (птица и любые их рисунки)           |
| зазоры между карточками и отступы экрана     | логотип, названия, узнаваемые фирменные элементы |
| размеры и расп��ложение иконок, кругов, pill | палитру — используем токены Mentalix из §2       |
| иерархию, размеры и вес текста               |                                                  |
| размеры и плотность нижней навигации         |                                                  |

Если значение в этом разделе и скриншот Stoic расходятся — прав скриншот
Stoic: агент снимает размер заново и обновляет таблицу в этом разделе.
Если значения для нужного экрана здесь нет — агент не придумывает его,
а просит у владельца скриншот Stoic этого экрана.

#### Как снимать размеры со скриншота Stoic

- Эталонное устройство: iPhone шириной 393 pt, скриншот @3x (1179 px).
- Размер в CSS px = пиксели скриншота ÷ 3, округление до целого.
- Результат проверяется наложением: скриншот Mentalix на той же ширине
  поверх скриншота Stoic с прозрачностью 50%. Рамки карточек и таб-бара
  должны совпасть с точностью ±2 px.

#### Общая грамматика карточки

- Шрифт — только Onest (`font-display`/`font-body`). Serif-гарнитуры
  (Cormorant Garamond и любые другие) в карточках запрещены.
- Размеры текста — только классы `.mx-type-*`. Нужной роли нет — сначала
  добавить её в `src/index.css` и в §3, потом использовать.
- Одна оболочка на карточку: «рамка в рамке» и внутренние светящиеся
  прямоугольники запрещены.
- Без градиентов (исключение — A-активная), теней внутри карточки, `inset`-подсветок.
- Акцент Gold/Azure — не больше одной точки на карточку.
- Нажатие: `transform: scale(0.97)` за 140–200 ms; `transition: all` запрещён.

#### Типы карточек (эталон — экран 393 px, отступ экрана 16 px)

#### Экран «Сегодня» — порядок и шапка

Эталон — видео Stoic 23.09.2026 (`docs/references/stoic-video-2026-09-23.md`,
кадры `01`–`03`). Порядок сверху вниз: шапка → неделя → две карточки дня
(тип A) → счётчик «Сегодня в пути вместе с тобой: N» → «Твои практики» →
остальные секции. Счётчик показывается только при наличии данных pulse.

| Элемент          | Значение                                                                                                                                                      |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Строка шапки     | высота 39 px, первая строка под зоной wordmark (§7); элементы по центру строки                                                                                |
| Чип серии        | слева, отступ 18 px от края; 57 × 38 px, радиус 999 px, фон `--c-card2`, рамка 1 px `--c-border`; огонёк-контур 16 px + число 17 px / 600, оба `--c-text` 70% |
| Приветствие      | по центру экрана, `.mx-type-greeting` (18 px / 700), строчными и с точкой: «доброе утро.», «добрый день.», «добрый вечер.»                                    |
| Кнопка справа    | профиль/настройки, отступ 18 px, круг 39 px, фон `--c-card2`, рамка 1 px `--c-border`, иконка 22 px `--c-text`                                                |
| Неделя           | 9 px под шапкой; 7 дней Пн–Вс текущей недели; центры ячеек через 52 px, крайние центры в 41 px от краёв экрана                                                |
| Ячейка дня       | 39 × 50 px, радиус 12 px; день недели — центр на 17 px от верха, `.mx-type-weekday`; число — центр на 30 px, `.mx-type-calendar-date`                         |
| Выбранный день   | рамка 1.5 px `--c-line-secondary`, без заливки; день недели и число `--c-text`, число 700                                                                     |
| Прошедшие дни    | `--c-text` 70%; будущие — `--c-faint`                                                                                                                         |
| Пройденный день  | число даты 13 px `--c-text` (день недели остаётся); галочки не используются; в тот же момент, когда число серии увеличивается                                 |
| Главная карточка | 16 px под неделей, тип A ниже                                                                                                                                 |
| Секции ниже      | 32 px под карточкой                                                                                                                                           |

Чип серии — вход в шторку «Серия и значки» (§5.3). Если пользователь выключил
показ серии, чип не рисуется, а приветствие остаётся по центру.

**A. Карточки дня** — две карточки в ряд: «Утренний чек-ин» и «Разбор дня».
Каждая имеет ширину по сетке (`grid-template-columns: repeat(2, minmax(0, 1fr))`),
высоту **260 px**, радиус **28 px** и зазор **8 px**.
Окно утра: 05:00 → время разбора; окно разбора: время разбора → 04:59
следующего локального дня. Время разбора берётся из настройки пользователя,
по умолчанию **19:00**; расчёт выполняется в часовом поясе пользователя.

Размеры для двух колонок (фактические значения из `Today.css`):

| Элемент         | Размер                           | Примечание                                                   |
| --------------- | -------------------------------- | ------------------------------------------------------------ |
| Карточка        | ширина по сетке, высота 260 px   | padding 20 px 10 px 16 px, радиус 28 px                      |
| Глиф            | 100% ширины, 64 px высота        | `CardSystemGlyph` в active                                   |
| Надпись (label) | 13 px / 500, lh 1.2              | `text-muted`, `white-space: nowrap`                          |
| Заголовок       | 18 px / 700, lh 1.25             | `text-cream`, `line-clamp: 2`, класс `mx-type-checkin-title` |
| CTA «Начать»    | 82 × 40 px, 17 px / 600          | радиус 999 px, `--btn-bg` / `--btn-text`                     |
| Текст done      | 17 px / 400, lh 1.3              | `text-muted`, по центру                                      |
| Текст locked    | 17 px / 400, lh 1.3              | `text-muted`, по центру                                      |
| Pill настроения | высота 26 px, 12 px / 500        | радиус 999 px, `--c-card2`, точка 8 px + текст               |
| Узкий (<360 px) | padding-inline 7 px, title 20 px | адаптация для узких экранов                                  |

У каждой карточки одно из четырёх независимых состояний:

- **active** — доступна, не пройдена: градиент `#242424` → `#323232`,
  `CardSystemGlyph`, надпись 13 px / 500, заголовок 18 px / 700 и CTA «Начать»;
  вся карточка — одна кнопка.
- **locked** — разбор до своего времени: без заливки, рамка 1 px `#333`,
  текст «Откроется в 19:00» (время из настройки), не нажимается.
- **done** — пройдена: без заливки, рамка 1 px `#333`, текст «Утро отмечено.»
  или «День закрыт.» и pill настроения, если оно выбрано.
- **missed** — утро пос��е открытия разбора, не пройдено: как locked, текст
  «Утро прошло», без кнопки.

Для active и done нажатие масштабируется до `scale(0.97)` за 140–200 ms.

Счётчик «Сегодня в пути вместе с тобой: N» располагается сразу под карточками,
с отступом 16 px, 12 px / 500, muted, по центру.

**B. Плитка практики** — «Твои практики» и любые сетки быстрых практик.

| Параметр     | Значение                                                                   |
| ------------ | -------------------------------------------------------------------------- |
| Сетка        | 3 в ряд, зазор 8 px                                                        |
| Размер       | ширина по сетке (~114 px), `aspect-ratio: 5 / 6` (~137 px)                 |
| Поверхность  | заливка `--c-card2`, без рамки                                             |
| Радиус       | 24 px                                                                      |
| Круг глифа   | 52 px, фон `--c-border`, по центру                                         |
| Глиф в круге | 26 px, цельная заливка `--c-line` (не контур)                              |
| Подпись      | 14 px / 600, `text-cream`, по центру, сразу под кругом (10 px), до 2 строк |
| Запрещено    | прижимать подпись к низу, рамка, фон под подписью                          |

Иконка и подпись выровнены по центру плитки по вертикали.

**C. Широкая карточка** — напоминания, инфо-блоки на всю ширину.

| Параметр    | Значение                                                              |
| ----------- | --------------------------------------------------------------------- |
| Размер      | 100% ширины, высота по контенту, минимум 120 px                       |
| Поверхность | заливка `--c-card2`, без рамки                                        |
| Радиус      | 24 px                                                                 |
| Отступы     | 20 px                                                                 |
| Закрытие    | крестик 18 px `text-muted`, в правом верхнем углу, зона нажатия 44 px |

**D. Pill статуса** — настроение, короткий статус внутри карточки A.

| Параметр    | Значение                                                                                         |
| ----------- | ------------------------------------------------------------------------------------------------ |
| Размер      | высота 26 px, padding 0 12 px, ширина по содержимому (~104 px)                                   |
| Поверхность | `--c-card2`, радиус 999 px                                                                       |
| Содержимое  | точка 8 px `--c-muted` + текст 12 px / 500, `--c-text` 70%, первое слово с заглавной; зазор 6 px |

#### Заголовки секций над карточками

- «Твои практики», «Напоминание» и т. п.: `.mx-type-section`
  (18 px / 600), с заглавной буквы, без `lowercase`.
- Отступ заголовка до карточек 16 px, между секциями 32 px.
- Действие секции (фильтр, настройки) — иконка 22 px справа, без круглого фона.

#### Нижняя навигация (`BottomNavigation`)

| Параметр             | Значение                                                                                         |
| -------------------- | ------------------------------------------------------------------------------------------------ |
| Бар                  | высота 53 px, отступ 20 px от краёв экрана                                                       |
| Положение            | снизу `--app-safe-bottom` + 8 px                                                                 |
| Поверхность          | `--c-nav` (#1C1C1C), рамка 1 px `--c-nav-border` (#333333), радиус 999 px                        |
| Иконки               | 22 px, `strokeWidth` 1.5                                                                         |
| Подписи              | 11 px / 500, lh 1.2, отступ до иконки 3 px; размер задаётся в px, не в rem                       |
| Узкий экран (<375px) | отступ бара 8 px от краёв, подписи 10.5 px                                                       |
| Активный пункт       | плашка ~75 × 47 px, радиус 999 px, фон `--c-nav-active` (#3A3A3A), иконка и подпись `--c-text`   |
| Неактивный           | иконка и подпись `--c-line` (#E6E6E6) — светлые, как у Stoic; активный отличается только плашкой |
| Проверка             | computed-значения на устройстве в Telegram: бар 53 px, плашка 75 × 47 px, подпись 11 px          |
| Нет                  | плавающей кнопки «+» — отменена владельцем                                                       |

Оболочка (`App.jsx`) добавляет контенту нижний отступ = высота бара +
safe-area + 24 px, чтобы ничего не уходило под навигацию.

#### Иллюстрации (`CardSystemGlyph` / `SemanticGlyph`)

Геометрия карточек — из Stoic, рисунки — только свои:

- переиспользовать `CardSystemGlyph`/`SemanticGlyph`; нужного смысла нет —
  расширить общий реестр, отдельную SVG-систему не создавать;
- плоская символическая иллюстрация из 3–7 крупных форм, цельные заливки
  `--c-line` плюс тонкая линия, одна точка-акцент Gold/Azure;
- запрещены градиенты, 3D, текстуры, звёзды, частицы, неон, человечки,
  текст внутри SVG и любые рисунки, повторяющие иллюстрации Stoic;
- рисунок не касается краёв своей области.

Смысловые типы `CardSystemGlyph`:

| Тип                   | Смысл                     | Движение                                          |
| --------------------- | ------------------------- | ------------------------------------------------- |
| `asceza-boundary`     | удержать границу выбора   | границы спокойно сходятся к порогу                |
| `neuro-synapse`       | соединить элементы        | сигнал проходит по собранной сети                 |
| `breath-flow`         | раскрыть и вернуть        | верхние и нижние дуги расходятся и сходятся       |
| `focus-convergence`   | сузить область внимания   | лучи собираются в одну точку                      |
| `meditation-contours` | успокоить внутреннее поле | контуры мягко выравниваются вокруг центра         |
| `path-corridor`       | увидеть следующий шаг     | коридор раскрывает маршрут, точка проходит вперёд |

Движение глифа — медленный цикл 8.8–13.5 s, при `prefers-reduced-motion`
остаётся читаемая статичная композиция. Анимация не меняет размер карточки.

#### Другие экраны

Детали ритуала/аскезы, дыхание/фокус/медитация, персоны, каталог практик
используют ближайший тип A–D. Точные размеры этих экранов добавляются в
этот раздел только после снятия со скриншота Stoic соответствующего экрана.
До этого действуют прежние каркасы из `docs/archive/CARD_SYSTEM_V1_2026-09-22.md` как
временные.

#### Проверка перед PR

1. Наложение на скриншот Stoic совпадает по рамкам ±2 px.
2. Использованы только типы A–D и значения из таблиц выше.
3. Нет текстов, иллюстраций и фирменных элементов Stoic.
4. Шрифт Onest, размеры через `.mx-type-*`, цвета через токены §2.
5. Проверено на 320 px и 393 px, в Telegram fullscreen и web.
6. Проверены `prefers-reduced-motion`, длинные подписи (2 строки), пустые
   и загрузочные состояния.
7. `npm run lint`, `npm run build`, `npm run test:unit`, `npm run ux:check` — зелёные.

#### Короткий запрос для Codex, Claude или Claude Code

> Используй «Mentalix Card System v2» в `DESIGN_SYSTEM.md` как единственный
> источник правды по карточкам и нижней навигации. Геометрию (размеры,
> пропорции, радиусы, отступы, размеры текста) бери из таблиц раздела —
> это эталон Stoic, попадай в него точно. Тексты, иллюстрации и фирменные
> элементы Stoic не переносить; рисунки — только `CardSystemGlyph`/`SemanticGlyph`.
> Если нужного размера в разделе нет — не придумывай, запроси скриншот Stoic.

### 5.2. Экраны прохождения чек-ина — геометрия Stoic, смысл Mentalix

Решение владельца от 23.09.2026. Эталон — скриншоты Stoic: шкала, редактор
ответа, завершение, серия. Правила §5.1 (что берём / что не берём, как снимать
размеры, шрифт Onest, токены §2) действуют и здесь. Тексты — только Mentalix.

Порядок шагов не меняется: шкалы → текст → завершение → серия
(`src/screens/CheckIn.jsx`). Меняется только вёрстка.

#### Общий каркас экрана чек-ина

| Параметр             | Значение                                                                                                                              |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Фон                  | `--c-bg`, без карточек-подложек под контентом                                                                                         |
| Боковой отступ       | `--mx-screen-x` (16 px)                                                                                                               |
| Верх экрана          | по §7: весь контент ниже контролов Telegram (`--app-safe-top` уже включает +56 px в fullscreen), через `src/lib/fullscreenSurface.js` |
| Кнопки сверху справа | первая строка контента, справа: круги 40 px, фон `--c-card2`, рамка 1 px `--c-border`, иконка 20 px; зазор 8 px                       |
| Состав кнопок        | «закрыть» не рисуем — это кнопка Telegram «Назад». Текст: только чип ИИ; завершение и серия: только «поделиться»; шкала: ничего       |
| Нижняя навигация     | скрыта на всех шагах                                                                                                                  |
| Прогресс шагов       | не показывается (у Stoic его нет)                                                                                                     |
| Нижняя полоса        | полосы действий на всю ширину с разделительной линией нет ни на одном шаге                                                            |

#### Шаг «Шкала» (`CheckInScaleQuestion`)

| Параметр        | Значение                                                                                                                                               |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Положение блока | вопрос и шкала — в нижней части экрана, центр вопроса ≈ 60% высоты экрана                                                                              |
| Вопрос          | по центру, 28 px / 600 / lh 1.15, `text-cream`, до 2 строк                                                                                             |
| Подсказка       | по желанию: 8 px под вопросом, 15 px / 400, `text-muted`, 1 строка                                                                                     |
| Шкала           | 5 вариантов в ряд, 30 px под вопросом (или подсказкой), ширина ряда ~337 px, по центру                                                                 |
| Вариант         | внешний круг 57 px, фон `--c-card2`; внутренний круг 35 px                                                                                             |
| Внутренний круг | уровни 1–5: 1 — только контур 1 px `--c-line`; 2–4 — заливка `--c-line` снизу на 25/50/75%; 5 — полная заливка                                         |
| Выбранный       | внешний круг — рамка 1.5 px `--c-line`; остальное без изменений                                                                                        |
| Подписи         | только крайние варианты, 20 px под кругами, 14 px / 500, `text-muted`                                                                                  |
| Низ экрана      | справа: «Пропустить» 17 px / 500 `text-muted` + круглая кнопка «далее» 45 px, фон #F2F2F2, шеврон 20 px `--c-bg`; зазор 24 px; низ — safe-area + 16 px |

Шкала с лицами (`mood`) сохраняет лица внутри внутреннего круга 35 px вместо заливки.
Кнопка «далее» неактивна (фон `--c-border`, шеврон `text-muted`), пока вариант не выбран.
Кнопки «Продолжить» на всю ширину на этом шаге н��т.

#### Шаг «Текст» (редактор, `JournalTextarea`)

| Параметр               | Значение                                                                                                                                               |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Чип ИИ                 | слева от «закрыть»: высота 40 px, padding 0 16 px, радиус 999 px, фон `--c-card2`, рамка 1 px `--c-border`, иконка-искра 18 px + «Лила» 16 px / 500    |
| Вопрос                 | слева, 16 px под кнопками, 22 px / 700 / lh 1.2, `text-cream`                                                                                          |
| Подсказка              | по желанию: 6 px под вопросом, 15 px / 400, `text-muted`                                                                                               |
| Поле ввода             | 12 px под вопросом/подсказкой, 17 px / 400 / lh 1.4, placeholder `text-muted` 60%                                                                      |
| Панель над клавиатурой | 12 px над клавиатурой, по краям `--mx-screen-x`                                                                                                        |
| Слева                  | круги 45 px («+», «Aa»), фон `--c-card2`, рамка 1 px `--c-border`, зазор 6 px                                                                          |
| Справа                 | pill «Пойти глубже» высота 45 px, padding 0 20 px, фон `--c-card2`, рамка 1 px `--c-border`, 17 px / 500 + круг «далее» 45 px, фон #F2F2F2; зазор 6 px |
| Переход дальше         | только круг «далее» в панели; кнопки «Продолжить» на всю ширину и отдельной стрелки нет                                                                |
| Кнопки панели          | заливка `--c-card2` + рамка 1 px `--c-border`; белые контурные кольца запрещены                                                                        |

#### Шаг «Завершение»

| Параметр       | Значение                                                                                                              |
| -------------- | --------------------------------------------------------------------------------------------------------------------- |
| Иллюстрация    | `CheckInCompletionArt`, область ~90 × 120 px, по центру, верх ≈ 28% высоты экрана                                     |
| Заголовок      | 2 строки по центру, 32 px под иллюстрацией, 28 px / lh 1.2, `text-cream`: 1-я строка 400, 2-я — 700                   |
| Тексты         | утро: «Утренний чек-ин» / «завершён.»; вечер: «Разбор дня» / «завершён.»                                              |
| Вопрос отзыва  | над кнопками отзыва, 16 px, 17 px / 400 / lh 1.3, `text-muted`, до 2 строк, ширина ≤ 240 px                           |
| Кнопки отзыва  | 3 в ряд по центру, 72 × 102 px, зазор 8 px, радиус 24 px, фон `--c-card2`, рамка 1 px `--c-border`                    |
| Содержимое     | иконка 24 px (контур 1.5) + подпись 14 px / 500, `text-cream`, зазор 12 px                                            |
| Выбранная      | фон `--c-line`, иконка и подпись `--c-bg`                                                                             |
| Следопыт       | только вечер: текстовая кнопка под карточками отзыва, 16 px / 600, `text-cream`; открывает handoff сегодняшней записи |
| Главная кнопка | «Сохранить» — см. «Главная кнопка экрана»                                                                             |
| Убрать         | eyebrow «ЧЕК-ИН ЗАВЕРШЁН», поясняющий абзац, чип «+ Добавить заметку»                                                 |
| Появление      | иллюстрация и заголовок сразу; вопрос отзыва, кнопки отзыва и «Сохранить» — ч��рез 500 ms (см. §6)                    |

#### Шаг «Серия»

| Параметр       | Значение                                                                                                                                                                                                     |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Иллюстрация    | `StreakFlower`, ~66 × 62 px, по центру, центр ≈ 33% высоты экрана                                                                                                                                            |
| Заголовок      | «N-дневная серия.», по центру, 28 px / 700 / lh 1.3, `text-cream`                                                                                                                                            |
| Текст          | сразу под заголовком, 28 px / 400 / lh 1.3, `text-cream`, по центру, до 3 строк, ширина ≤ 330 px                                                                                                             |
| Дни            | 24 px под текстом: дни текущей серии (максимум 6) + один следующий день; ряд по центру, не растягивать на ширину экрана. Дни вне текущей серии не показываются: серия 1 в среду → «Ср» (огонь), «Чт» (пусто) |
| Круг дня       | 34 px, зазор 7 px; прошедший — фон `--c-line` + галочка 14 px `--c-bg`; сегодня — фон `--c-line` + огонь 14 px `--c-bg`; следующий — фон `--c-border`, пусто                                                 |
| Подпись дня    | 8 px под кругом, 14 px / 500, `text-muted`, короткое название дня недели                                                                                                                                     |
| Главная кнопка | «Продолжить» — см. ниже                                                                                                                                                                                      |

#### Вечерний разбор (`CheckInCore`, mode `evening`)

Эталон — вечерний путь Stoic. Порядок шагов:

1. **Шкалы** — только если утренний чек-ин за день не пройден (текущая логика
   `skipScales`); вид — шаг «Шкала» выше.
2. **Эмоции** — один экран, вид ниже.
3. **Текст** — три вопроса Mentalix подряд, каждый отдельным экраном шага
   «Текст» выше: «Что получилось?», «Что было трудно?», «Какой вывод забираешь?».
4. **Завершение** — шаг «Завершение» выше, тексты «Разбор дня» / «завершён.»
5. Возврат на «Сегодня». Экрана «Серия» после вечера нет.

Шаг «Эмоции» — эталон: кадр `11-mood-emotions.jpg` (видео 23.09.2026).
Сетка 2 колонки отменена: у Stoic эмоции — вертикальный столбец под лицом
настроения, столбцы уровней листаются вбок.

| Параметр         | Значение                                                                                                                                                    |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Вопрос           | по центру, первая строка контента под кнопками сверху, 22 px / 600 / lh 1.3, `text-cream`, до 2 строк; текст — «Что ближе всего к тому, что ты чувствуешь?» |
| Лицо уровня      | 30 px, контур `--c-line`, по центру, верх ≈ 28% высоты экрана; то же лицо, что на шаге «Шкала»                                                              |
| Столбец          | 20 px под лицом; pill друг под другом по центру, шаг 42 px (pill 38 px + зазор 4 px); столбец прокручивается вертикально                                    |
| Pill эмоции      | высота 38 px, padding 0 18 px, ширина по слову, радиус 999 px, фон `--c-card2`, 15 px / 500, `text-cream`, первая буква заглавная                           |
| Выбранная        | фон `--c-line`, текст `--c-bg`; выбор один, повторное нажатие снимает                                                                                       |
| Уровни           | 5 столбцов — по одному на уровень `mood`, начальный — выбранный на шкале; горизонтальный свайп со snap, шаг столбцов 243 px                                 |
| Соседний столбец | выглядывает у края экрана, прозрачность 30%, не нажимается, пока не прокручен в центр                                                                       |
| Набор            | `EMOTIONS[level]`, все слова уровня; «Показать ещё» не нужно — остальные уровни открываются свайпом                                                         |
| Тяжёлые эмоции   | ссылка «Поговорить об этом с Собеседником →» остаётся под столбцом, 16 px под последним pill, 15 px / 600                                                   |
| Низ экрана       | только круглая кнопка «далее» 45 px справа; до выбора — неактивна (фон `--c-line-secondary`, шеврон `--c-bg`)                                               |

#### Практика «Настроение» (эталон — Mood Check-In Stoic)

Отдельная короткая практика вне утреннего/вечернего чек-ина: заметить эмоцию
и при желании разобрать её. В Mentalix её вход — плитка «Настроение» в
«Твоих практиках». `MoodCheckGate` (opt-in гейт при запуске) — отдельный
компонент, не часть этой практики. Шаги (кадры `09`–`13`):

**API-контракт** (бэкенд `mentalix-bot`, PR #81): практика сохраняется
отдельной записью, независимой от чек-ина и серии.

| Метод | Путь | Тело | Ответ |
| ----- | ---- | ---- | ----- |
| POST | `/api/mood-practices` | `{ mood, emotion, context, note, breathing_completed }` | 201 — полная запись; 422 — неверные данные; 401 — без авторизации |
| GET | `/api/mood-practices?from=<ISO>&to=<ISO>` | — | массив записей текущего пользователя, новые сверху |

`user_id` не передаётся в теле — берётся из подписанного Telegram initData /
web-сессии тем же способом, что в `/api/checkin` (заголовок `Authorization:
tma <initData>` или `X-Web-User-ID`). Поля записи: `id`, `user_id`,
`recorded_at` (UTC сервера при создании), `mood` (int 1..5), `emotion`
(непустая строка), `context` (`work`/`home`/`relationships`/`health`/
`study`/`other` или `null`), `note` (строка до 2000 символов или `null`),
`breathing_completed` (bool, по умолчанию `false`). `/checkin` и расчёт
серии не затрагиваются.

1. **Вступление** (только при первом запуске): заголовок «настроение.»
   `.mx-type-page` по центру, верх ≈ 17% высоты; под ним 40 px — 3 абзаца
   15 px / 400 / lh 1.45, `text-cream`, слева, отступы 28 px, между абзацами
   8 px; внизу по центру «Начать» 82 × 40 px как в карточке A, низ — safe-area + 40 px.
   Тексты абзацев: «Иногда достаточно остановиться и назвать то, что чувствуешь.»
   / «Это короткая практика — заметить настроение и эмоцию.» / «Можно
   добавить пару слов и подышать, если захочешь.»
2. **Лица** — геометрия шага «Шкала». Слева внизу: «Дата:» 14 px / 500
   `text-muted` + pill 84 × 34 px, фон `--c-card2`, «Сейчас» 16 px / 500.
   Дата не редактируется — иконки-карандаша нет, pill не нажимается.
   Автоперехода нет: тап по лицу подсвечивает его; после выбора появляется
   белая кнопка «Далее» со стрелкой (как `CheckInNextControls`), низ —
   safe-area + 16 px. До выбора кнопка «Далее» неактивна.
3. **Эмоции** — шаг «Эмоции» выше. Выбор эмоции обязателен: кнопка «Далее»
   неактивна, пока эмоция не выбрана.
4. **Что происходит** — вопрос слева 22 px / 700 + поле ввода как в шаге
   «Текст» (необязательное). Ниже плитки контекста 3 в ряд (как шаг «Фокус»
   чек-ина): «работа» → `work`, «дом» → `home`, «отношения» →
   `relationships`, «здоровье» → `health`, «учба» → `study`, «другое» →
   `other`. Контекст необязательный: можно не выбирать (значение `null`).
   Повторный тап по выбранной плитке снимает выбор. Справа внизу «далее».
5. **Предложение подышать**: текст по центру ≈ 45% высоты, 22 px / lh 1.3:
   первые строки 700 («Ты остановился и заметил, что чувствуешь.»), дальше 400
   («Попробуешь короткое дыхание?»). Внизу две кнопки на всю ширину минус
   2 × 28 px, высота 53 px, радиус 999 px, зазор 8 px: «Подышать минуту»
   (фон `--c-line`, текст `--c-bg`) и «Не сейчас» (фон `--c-card2`, текст
   `text-cream`), 17 px / 500; низ — safe-area + 12 px. Дыхание —
   необязательный шаг; `breathing_completed` = `true`, если пользователь
   прошёл дыхание, `false` — если выбрал «Не сейчас».
6. **Завершение** — шаг «Завершение» выше, тексты «Настроение» /
   «отмечено.». Практика завершена после успешного сохранения записи через
   `POST /api/mood-practices`. При ошибке сохранения — сообщение об ошибке
   и кнопка повтора; данные в полях не теряются.

Практика не увеличивает серию и не затрагивает чек-ин дня.

#### После прохождения на «Сегодня»

- Карточка дня — состояние A-завершённая (§5.1), 260 px.
- Число в чипе серии увеличивается сразу, без анимации счётчика; в календаре недели пройденные дни остаются числами, без галочек.
- В полосе недели день, в котором пройден хотя бы один чек-ин, показывает
  галочку вместо числа (название дня остаётся). Выбранный день — та же рамка.
- Если чек-ин открыл значок — через 400 ms шторка «Новый значок» (§5.3).
- Последовательность и тайминги — §6.

#### Redo сегодняшнего чек-ина

- Кнопка «…» справа в шапке детального экрана сегодняшней записи (и из
  «Сегодня», и из истории). У прошлых дней её нет.
- Меню: «Пройти утро заново»; если разбор завершён — «Пройти разбор заново».
- Перед запуском — подтверждение «Пройти заново? Текущие ответы заменятся.»
- В режиме redo поля пустые, экран серии не показывается. Сохранение —
  PUT /checkin/today (атомарная замена), разбор уходит с `review_completed:
true`.

#### Главная кнопка экрана

| Параметр  | Значение                                                            |
| --------- | ------------------------------------------------------------------- |
| Где       | только шаги «Завершение» и «Серия»                                  |
| Размер    | ширина экрана минус 2 × 28 px, высота 56 px, радиус 999 px          |
| Цвет      | фон `--c-line` (#D0D0D0), текст `--c-bg`, 20 px / 700               |
| Положение | низ — safe-area + 12 px; без подложки-полосы и разделительной линии |

#### Проверка перед PR (дополнительно к §5.1)

1. Скриншоты 393 px и 320 px каждого шага: шкала (без выбора и с выбором),
   текст (с клавиатурой), завершение (без отзыва и с выбранным), серия (1, 2 и 6 дней).
2. На 320 px ряд шкалы и кнопки отзыва не выход��т за `--mx-screen-x`.
3. Все шаги работают с клавиатурой и VoiceOver: шкала — `radiogroup`,
   отзыв — `radiogroup`, кнопки сверху имеют `aria-label`.

### 5.3. Серия и значки — геометрия Stoic, смысл Mentalix

Решен��е владельца от 23.09.2026. Эталон — видео Stoic 23.09.2026, кадры
`02`, `04`–`07`. Правила §5.1 (что берём / что не берём) действуют и здесь.
Механика уровней не меняется: `src/lib/series.js`, `src/lib/badges.js`
(MXL-STREAK-TIERS-001). Меняется вид и точки входа.

#### Правила серии

- Серия — число дней подряд с хотя бы одним пройденным чек-ином.
- До первого чек-ина в чипе **0**, а не 1.
- Серия не наказывает: пропуск просто обнуляет число, без текстов о потере.
- Показ серии и значков выключается в шторке (переключатели внизу вкладки
  «Значки»). Выключенная серия убирает чип с «Сегодня», но не удаляет данные.

#### Подсказка при первом показе

| Параметр   | Значение                                                                                                                       |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------ |
| Когда      | первый заход на «Сегодня» после онбординга; больше не показывается после закрытия или тапа по чипу                             |
| Положение  | под чипом, 8 px, левый край в 9 px от края экрана; перекрывает неделю и верх карточки                                          |
| Размер     | 286 × ~105 px, радиус 24 px, фон `--c-nav`, указатель-треугольник 10 px к центру чипа                                          |
| Содержимое | иконка-лампочка 16 px слева; текст 15 px / 400 / lh 1.1, `text-cream`, ключевое слово 700; крестик 18 px в правом верхнем углу |
| Текст      | «Это число — твоя **серия** чек-инов. Её можно скрыть. Нажми, чтобы посмотреть значки.»                                        |

#### Шторка «Серия и значки»

Открывается тапом по чипу. Шторка на всю высоту под safe-area-top, поверх
«Сегодня»; нижняя навигация скрыта. Движение — §6.

| Элемент         | Значение                                                                                                                                |
| --------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| Верхняя строка  | 8 px под нижней кромкой кнопок Telegram: по центру сегмент «Значки                                                                      | Статистика». В Telegram своя кнопка ✕ не рисуется — закрытие только нативной «Назад»; в web/PWA справа круг «закрыть» 38 px (фон `--c-card2`, рамка 1 px `--c-border`) |
| Сегмент         | 177 × 42 px, радиус 999 px, фон `--c-card2`, рамка 1 px `--c-border`; активный — плашка 80 × 34 px `--c-nav-active`; `.mx-type-segment` |
| Боковые отступы | `--mx-screen-x`                                                                                                                         |

**Вкладка «Значки»**

| Элемент          | Значение                                                                                                                                                                                                    |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Hero-блок        | 24 px под сегментом, ширина экрана, высота 300 px, радиус 24 px, фон `--c-card2`                                                                                                                            |
| Число            | по центру, верх 40 px от верха блока, «N.» 30 px / 700 `text-cream`; под ним «ЗНАЧКОВ ПОЛУЧЕНО» 12 px / 600, разрядка 0.2em, `text-muted`                                                                   |
| Сцена            | нижняя половина блока: линия горизонта и последний значок по центру (`CardSystemGlyph`), подпись 13 px / 600 + 12 px `text-muted` («Твой первый значок» / «Может, сегодня?»)                                |
| «Все →»          | справа, 16 px под блоком, 15 px / 500, `text-muted`                                                                                                                                                         |
| Заголовок списка | «СЛЕДУЮЩИЕ ЗНАЧКИ», по центру, 36 px под «Все», 11 px / 600, разрядка 0.2em, `text-muted`                                                                                                                   |
| Строка значка    | 16 px под заголовком; высота 82 px, зазор 7 px, радиус 24 px, фон `--c-card2`                                                                                                                               |
| В строке         | слева значок 48 px (12 px от края); название 15 px / 500 `text-cream`; условие 11 px `text-muted`; справа «N/M» 15 px / 500 `text-muted` и полоса прогресса 40 × 3 px (`--c-border`, заполнение `--c-line`) |
| Переключатели    | внизу вкладки, карточка радиус 24 px, фон `--c-card2`: «Показывать серию», «Показывать значки», строки 42 px, 17 px / 400, iOS-переключатель                                                                |

**Вкладка «Статистика»**

| Элемент         | Значение                                                                                                                                                                             |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Плитки-итоги    | 2 ряда, горизонтальная прокрутка столбцами; плитка — ширина экрана минус 2 × 16 минус 14 px (следующий столбец выглядывает), высота 71 px, зазор 7 px, радиус 24 px, фон `--c-card2` |
| В плитке        | по центру: число 22 px / 700 `text-cream`, под ним подпись 15 px / 500 `text-muted`                                                                                                  |
| Группа          | заголовок по центру 11 px / 600, разрядка 0.2em, `text-muted`, 40 px над карточкой; карточка радиус 24 px, фон `--c-card2`                                                           |
| Строка группы   | высота 37 px, отступы 20 px; слева 15 px / 400 `text-cream`, справа значение 15 px / 600 `text-cream`                                                                                |
| Группы Mentalix | «Серия» (текущая, лучшая, всего дней), «Чек-ины», «Практики», «Записи» — только из данных `buildSeriesViewModel`                                                                     |

#### Шторка значка (тап по строке)

| Элемент      | Значение                                                                                                                                        |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| Шторка       | снизу, верх ≈ 55% высоты экрана, скругление сверху 24 px, фон `--c-bg`; фон под ней затемнён `rgba(0,0,0,0.6)`                                  |
| Сцена        | верхние 71 px: горизонт и значок по центру; крестик 18 px справа сверху                                                                         |
| Надзаголовок | «ЗНАЧОК СЕРИИ» / «ЗНАЧОК», 11 px / 600, разрядка 0.2em, `text-muted`                                                                            |
| Заголовок    | 6 px ниже, 28 px / 700, `text-cream`                                                                                                            |
| Условие      | 15 px / 400 `text-muted`, по центру, 12 px ниже                                                                                                 |
| Прогресс     | «Твой прогресс» 15 px / 500 `text-cream` + «N/M» 15 px / 400 `text-muted`                                                                       |
| Кнопка       | ведёт в практику, которая двигает значок («Утренний чек-ин»): 177 × 45 px, радиус 999 px, фон `--c-card2`, 17 px / 600; низ — safe-area + 16 px |

#### Шторка «Новый значок»

Появляется на «Сегодня» через 400 ms после возврата из чек-ина, который
открыл значок. Геометрия — как у шторки значка (верх ≈ 52% высоты), отличия:

- надзаголовок «НОВЫЙ ЗНАЧОК»; заголовок — название значка;
- текст 15 px / 400 `text-muted` («Первый утренний чек-ин пройден!»);
- «Получен» 15 px / 500 `text-cream` + дата `ДД.ММ.ГГГГ` 15 px / 400 `text-muted`;
- кнопка «Поделиться» 76 × 45 px, фон `--c-card2`;
- закрывается крестиком, свайпом вниз и тапом по затемнению.

#### Проверка перед PR (дополнительно к §5.1)

1. Скриншоты 393 и 320 px: чип 0 и 12, подсказка, обе вкладки, шторка
   значка, «Новый значок».
2. Выключенная серия убирает чип; приветствие остаётся по центру.
3. Шторки закрываются кнопкой, свайпом вниз и Telegram «Назад».

## 6. Motion

Разрешены спокойные короткие переходы: `fadeIn`, `bloom`, единичный pulse, мягкое золотое свечение, float/sway для иллюстраций. `prefers-reduced-motion` отключает анимации и transitions.

Корень приложения и viewport не двигаются и не масштабируются: в Telegram
WKWebView это конфликтует с пересчётом safe area и клавиатуры и выглядит как
рывок. Появление обычного экрана — только opacity.

Исключение — отдельные слои поверх экрана: шторки, полноэкранный слой
чек-ина, шаги внутри флоу. Они двигаются `transform: translate` сами по
себе, а экран под ними стоит. Пока открыта клавиатура, сдвиги не
запускаются — только opacity.

### Жесты и переходы — эталон Stoic (видео 23.09.2026)

Замеры — `docs/references/stoic-video-2026-09-23.md`. Кривая «шторка» —
`cubic-bezier(0.32, 0.72, 0, 1)`: быстрый старт, мягкая доводка без
заметного перелёта.

| Переход                        | Движение                                                                                                                                                           | Время                                             |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------- |
| Нажатие карточки, кнопки, чипа | `scale(0.97)`, отпускание — обратно                                                                                                                                | 140–200 ms                                        |
| «Начать» → чек-ин              | чёрный слой въезжает снизу и закрывает «Сегодня»; вопрос и шкала доезжают снизу вверх (~80 px) с замедлением; низ экрана (кнопки) — opacity                        | слой 170 ms; контент 500 ms; низ с 370 ms, 150 ms |
| Шаг → следующий шаг            | текущий уходит влево, следующий приходит справа; «назад» — зеркально                                                                                               | 300 ms, кривая «шторка»                           |
| Тап по варианту шкалы          | обводка выбранного — сразу; в практике «Настроение» — явная «Далее» после выбора                                                                                   | обводка сразу; «Далее» активна сразу после выбора |
| Эмоции: свайп столбцов         | горизонтальный свайп пальцем, snap к ближайшему столбцу, соседний — 30% прозрачности                                                                               | по пальцу; доводка 250 ms                         |
| Появление экрана завершения    | иллюстрация и заголовок — сразу; блок отзыва и «Сохранить» — opacity 0→1 и сдвиг 12 px вверх                                                                       | задержка 500 ms, 300 ms                           |
| «Сохранить» → «Сегодня»        | слой чек-ина уезжает вниз; одновременно карточка дня переходит из A-активной в A-завершённую: содержимое crossfade, высота 455 → 233 px, секции ниже подтягиваются | слой 170 ms, ease-in; карточка 130 ms             |
| «Новый значок»                 | пауза, затем затемнение фона (opacity) и шторка снизу                                                                                                              | пауза 400 ms; 250 ms                              |
| Чип серии → шторка             | шторка снизу на всю высоту                                                                                                                                         | 270 ms + доводка ~150 ms                          |
| Закрытие шторки                | вниз; свайп вниз от верха шторки закрывает, если протащили > 25% высоты или отпустили быстро                                                                       | 200 ms, ease-in                                   |
| Число серии, число недели      | меняются без анимации в момент возврата на «Сегодня»                                                                                                               | —                                                 |

При `prefers-reduced-motion` все сдвиги заменяются opacity 150 ms, задержки
сохраняются (порядок появления остаётся понятным).

Анимация должна объяснять состояние или направлять внимание, а не становиться аттракционом.

## 7. Safe areas и Telegram

Все полноэкранные поверхности обязаны учитывать `--app-safe-top/right/bottom/left`. Нижние фиксированные элементы учитывают Telegram и iPhone safe areas. Проверка только в desktop browser недостаточна.

Когда Telegram разворачивает приложение на весь экран, он рисует свои контролы — «Закрыть» и меню — поверх веб-вью. Системная safe area их не покрывает. Поэтому в fullscreen к `--app-safe-top` добавляются 56 px. Экран, который этого не делает, получает собственные кнопки под контролами Telegram: визуально они на месте, но нажатия забирает Telegram.

На Today эти 56 px также являются единственным местом wordmark `MENTALIX`:
он расположен по центру между контролами Telegram и прокручивается вместе с
контентом. Приветствие начинается сразу после этой зоны.

Реализация вынесена в `src/lib/fullscreenSurface.js`. Любая новая полноэкранная поверхность использует его, а не собственные вычисления.

Отступы сверху и снизу принадлежат оболочке приложения. Горизонтальный отступ
экрана задаётся одним токеном `--mx-screen-x: 16px`; обёртки экранов используют
его, а не `px-*` классы, и не задают собственные `pt`/`pb`.

## 8. Зафиксированные расхождения

- Старые исторические документы и записи в `CHANGES.md` могут упоминать
  тёмные токены `#0A0A0A`, `#19191B`, `#232326`, `#F5F5F5`, `#D9B45B`;
  код использует значения из таблицы выше. `PRODUCT.md` конкретные токены
  сейчас не задаёт.
- Текущий фактический визуальный baseline определяется кодом и этим
  документом: основной шрифт — Onest, золотой акцент тёмной темы —
  `#EDBD60`. Это не означает, что финальная продуктовая палитра утверждена:
  она остаётся предметом product decision, пока `ROADMAP.md` и `TASKS.md`
  определяют её таким образом.
- `BottomNavigation` использовал hardcoded rgba-поверхности. Исправлено
  30.07.2026: поверхности и границы берутся из системных токенов.

Тени плавающих поверхностей вынесены в токены `--shadow-float` и
`--shadow-float-compact`.

- В коде существует полностью реализованная светлая тема
  (`[data-theme='light-preview']` в `src/index.css` — тёплая «бумажная» палитра,
  свой cognac-gold акцент, свои тени) — не мёртвый код, а сознательно скрытый
  dev/preview-режим, включаемый только через `?light-preview=1` в локальной
  разработке или non-production Preview (см. `src/App.jsx`). В Production выключена
  по умолчанию. Обновление 2026: принято решение сделать её полноценным
  пользовательским переключателем в Настройках — см. отдельный тикет на
  реализацию.

### Выровнено с эталоном 23.09.2026

- Шторка «Серия и значки» (§5.3): в Telegram своя кнопка ✕ убрана — закрытие
  только нативной «Назад»; отступ сегмента от кнопок Telegram сокращён с 20 px
  до 8 px. Склонение счётчиков «Статистики» исправлено (`src/lib/pluralize.js`).
- Блок 1 — «Сегодня» (§5.1): выбраный день в календаре недели, шапка, плитка
  практики, pill настроения приведены к спецификации.
- Блок 2 — текстовые шаги чек-ина (§5.2): «Пропустить» убран с текстовых шагов,
  вопрос выровнен влево по спецификации.
- Блок 3 — завершение разбора (§5.2): «Сохранить» — главная кнопка, «Следопыт» —
  второстепенная текстовая, карточки отзыва приведены к спецификации.
- Блок 5 — экран истории сегодняшнего чек-ина (`History.jsx`): нативная кнопка
  «Назад» Telegram через `BackButton`-контракт (своя ссылка «Назад» убрана);
  отступы `--mx-screen-x` (16 px) на карточке; «СЕГОДНЯШНИЙ ЧЕК-ИН» вместо
  «CHECK-IN»; ответы настроения с заглавной буквы без технического префикса
  «настроение:»; текстовые ответы разбора — отдельные подзаголовки вопросов
  («Что получилось?», «Что было трудно?», «Какой вывод забираешь?») с ответами
  под ними, вместо склеенной строки в «Уроки дня».
- Блок 6 — «check-in» → «чек-ин» во всех пользовательских текстах (Analytics,
  CheckIn, History, PrivacyNotice, Settings, AiPrivacyControls, UI-lab).
  Идентификаторы, ключи API, имена событий, маршруты и комментарии не затронуты.
- Redo сегодняшнего чек-ина (§5.2): кнопка «…» в шапке детального экрана
  сегодняшней записи, меню «Пройти утро/разбор заново», подтверждение, пустые
  поля, без серии. Сохранение через PUT /checkin/today с `review_completed:
true` для разбора.

### Расхождения с эталоном 23.09.2026 (код ещё не догнал)

- Шаг «Эмоции» — сет��а 2 колонки; по §5.2 — столбец pill по уровням со свайпом.
- Практика «Настроение» (§5.2): реализована. Бэкенд-контракт
  (`POST/GET /api/mood-practices`, PR #81 mentalix-bot) согласован и в проде.
  Решения владельца: автоперехода на шкале лиц нет — явная «Далее» после
  выбора; дата не редактируется; контекст необязательный (work/home/
  relationships/health/study/other или null); дыхание необязательное,
  `breathing_completed` фиксирует факт; практика завершается после
  успешного сохранения, при ошибке — повтор без потери данных; серия и
  чек-ин не затрагиваются. Тексты вступления определены в §5.2.
- Переходы §6 (слой чек-ина снизу, шаги вбок, сжатие карточки, шторки) не реализованы.
