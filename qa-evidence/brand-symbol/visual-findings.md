# MXL-BRAND-SYMBOL-BOOK-QUILL — визуальные findings

Снимки сделаны локально на viewport 390×844 через Chromium preview:

- `brand-symbol-dark.png`: тёмная тема загружается без ошибок; нижняя навигация отображает центральную вкладку «Диалог» с новой линейной иконкой книги, layout не переполняется.
- `brand-symbol-light.png`: light-preview загружается без ошибок; контраст и layout сохраняются, центральная вкладка «Диалог» остаётся видимой.
- Снимки Today показывают текущую hero-иллюстрацию `DayArc`, а не старый MazeLogo; это ожидаемое фактическое состояние после read-only аудита.

Полный визуальный gate для splash/AppLock и активного состояния центральной вкладки требует отдельного ручного прохода владельца на Preview/Telegram/iPhone; эти локальные снимки не заменяют real-device gate.
