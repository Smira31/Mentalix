---
status: historical
last_verified: 2026-09-11
---
# Stoic video reference → Mentalix visual contract

Статус: **REFERENCE / APPROVED DIRECTION**, не production-реализация.  
Дата фиксации: 09.09.2026.  
Ведущая задача текущего цикла: [Issue #526](https://github.com/Smira31/Mentalix/issues/526) — редизайн вкладки «Библиотека».  
Следующая задача: [Issue #527](https://github.com/Smira31/Mentalix/issues/527) — вкладка «Прогресс».

Документ переводит пять видео-референсов в язык Mentalix. Он фиксирует визуальные принципы и границы, но не делает Stoic источником продуктовой логики, текстов, данных или бренда. Фактические контракты по-прежнему определяются текущим кодом `main`, `PRODUCT.md`, `DESIGN_SYSTEM.md`, `ARCHITECTURE.md` и `AI_RULES.md`.

## 1. Источники и покрытие

| Файл           | Длительность |     Размер | Основная поверхность                                          | Задача Mentalix                           |
| -------------- | -----------: | ---------: | ------------------------------------------------------------- | ----------------------------------------- |
| `IMG_6008.MP4` |      89,43 с | 576 × 1280 | Explore / библиотека, подборки, коллекции и detail            | **#526, текущий цикл**                    |
| `IMG_6009.MP4` |      18,37 с | 576 × 1280 | History: дни/недели, запись, фильтр, поиск, achievement sheet | Отдельный будущий scope; не входит в #526 |
| `IMG_6010.MP4` |      30,27 с | 576 × 1280 | Trends: период, график, insight-карточки, empty states        | **#527, только после #526**               |
| `IMG_6012.MP4` |      23,00 с | 576 × 1280 | Профиль, настройки, достижения и сводная статистика           | Отдельный будущий scope; частично #527    |
| `IMG_6013.MP4` |     105,23 с | 576 × 1280 | Домашние практики, темы, редактор, история и прогресс         | Сквозной reference; разделить по задачам  |

Видео не добавляются в Git: это локальные референсы владельца. Наблюдаемые состояния и таймкоды ниже являются переносимым текстовым evidence для следующих агентов.

## 2. Общая визуальная грамматика пяти видео

### 2.1. Композиция

- Чёрный фон экрана и спокойные графитовые поверхности без декоративных градиентов.
- Один крупный lowercase-заголовок экрана; справа — только контекстные действия.
- Контент строится ярусами: заголовок → основной блок → тематические секции → карточки.
- Карточки преимущественно крупные, с радиусом 24–28 px, тонкой границей или разницей поверхности вместо тяжёлой тени.
- Сетки используют две колонки с одинаковым ритмом и редкими асимметричными иллюстрациями.
- Горизонтальные последовательности показывают край следующей карточки; жест реализуется нативным overflow + `scroll-snap`, без document-level touch handlers.
- Bottom navigation остаётся плавающей поверхностью над контентом; её контракт принадлежит `App.jsx` и не меняется экраном.

### 2.2. Типографика Mentalix

- Не копировать гарнитуру Stoic. Основной шрифт Mentalix — `Onest`; caps-лейблы — `Manrope` через `font-label`.
- Заголовок экрана: текущий `mx-type-page`, lowercase, Onest.
- Заголовки карточек: Onest 600/700, без чрезмерного увеличения.
- Эйбраузы и категории: `font-label`, 10–11 px, uppercase, увеличенный tracking.
- Вторичный текст использует `text-muted`/`text-faint`, а не статическую opacity.
- Все product-facing строки — только на русском.

### 2.3. Цвет и иллюстрации

- Использовать только токены Mentalix: `--c-bg`, `--c-card`, `--c-card2`, `--c-text`, `--c-muted`, `--c-faint`, `--c-border`.
- Значимый акцент — Gold или Azure по семантике Mentalix; фиолетовый Stoic и premium-градиент не переносить.
- Иллюстрации переводятся в `CardSystemGlyph`/`SemanticGlyph`: тонкая контурная геометрия, 3–7 крупных форм, одна смысловая точка-акцент.
- Не копировать перо Stoic, фирменные персонажи, конкретные обложки, premium-бейджи и узнаваемые иллюстрации.

### 2.4. Motion и платформенное поведение

- Переходы целого экрана — opacity; не двигать и не масштабировать viewport.
- Нажатие карточки: 140–240 ms через конкретные `transform`/`opacity`, без `transition: all`.
- Горизонтальные rail и paging работают через CSS scroll snap.
- `prefers-reduced-motion` оставляет полностью читаемую статичную композицию.
- Сохраняются Telegram safe-area, 56 px зоны системных контролов и нижний резерв навигации.

## 3. `IMG_6008` — контракт для #526 «Библиотека»

### 3.1. Наблюдения по видео

- `00:01–00:05`: premium/paywall sheet с фиксированной CTA-зоной. Это evidence геометрии sheet, но не scope #526.
- `00:08–00:15`: полноэкранная дыхательная практика с одной центральной анимацией и одной нижней CTA. Функция уже существует в Mentalix; в Library не переносится.
- `00:20–00:31`: Explore с крупной темой недели, горизонтальным вопросом, точками пагинации, CTA и началом библиотеки ниже.
- `00:32–00:43`: detail коллекции — центрированный eyebrow, заголовок и описание; ниже большой горизонтальный rail материалов, соседняя карточка видна справа.
- `00:44–00:56`: обзор коллекций — плотная двухколоночная сетка, единый тип карточек, счётчик прогресса внизу.
- `00:58–01:11`: тематическая сетка — карточки разной смысловой насыщенности, иллюстрация занимает свободную нижнюю зону, шеврон закреплён справа снизу.
- `01:15–01:29`: detail тематической подборки — заголовок и пояснение над двухколоночной сеткой материалов; locked/premium состояние показано без изменения геометрии карточки.

### 3.2. Уже существующие функции Mentalix, которые обязаны сохраниться

| Поверхность         | Текущий контракт                                                                   | Что можно изменить визуально                                        | Что нельзя менять в #526                                                                     |
| ------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| Статьи              | `fetchArticles`, поиск, сортировка, loading/error/empty, открытие reader, источник | Композицию landing, сетку/rail, плотность карточки, иерархию текста | API `/articles`, содержимое статей, открытие reader и внешний источник                       |
| Направленные записи | Telegram-only catalog, поиск, категории, draft/complete sessions, архив            | Landing-карточку, список шаблонов, визуальную группировку           | Telegram gate, create/edit/delete, versioning, ответы, persistence и fullscreen writing flow |
| Личный шаблон       | Создание и редактирование до 12 шагов                                              | Только presentation текущих контролов в отдельном позднем проходе   | Типы шагов, validation, API и семантика сохранения                                           |
| Практикумы          | Existing `Courses` CRUD/status/notes, вкладка помечена «скоро»                     | Только disabled/soon-представление на landing                       | Не включать функцию, не менять backend, не объявлять готовой                                 |
| Reader              | Article detail + дата + длительность + первоисточник                               | Типографический ритм и visual shell                                 | Текст, ссылка, навигация назад и доступность                                                 |

### 3.3. Предлагаемая структура UI Lab #526

Первый UI Lab проверяет **одну проблему**: пользователь должен быстро понять, что хранится в Библиотеке, и открыть нужный существующий тип материала.

1. **Header**
   - `библиотека.` слева;
   - круглая кнопка поиска справа;
   - поиск раскрывается в строку внутри потока, не создаёт новый fullscreen overlay.
2. **Featured rail «Продолжить»**
   - показывается только при наличии незавершённой направленной записи или материала в процессе;
   - если данных нет, ярус полностью отсутствует — фиктивный прогресс не рисовать.
3. **Ярус «Для тебя»**
   - реальные статьи из существующего массива, без новой персонализации;
   - крупная первая карточка и видимый край следующей;
   - карточка открывает существующий reader.
4. **Секция «Коллекции»**
   - две активные входные карточки: «Статьи» и «Направленные записи»;
   - третья карточка «Практикумы» остаётся disabled с подписью «СКОРО»;
   - в UI Lab допустимы fixtures только для геометрии, production обязан использовать реальные состояния.
5. **Каталог выбранного типа**
   - центрированный eyebrow + заголовок + короткое описание;
   - статьи: реальный поиск и текущие article cards в новой сетке/rail;
   - записи: существующие поиск, категории, продолжение, архив и шаблоны;
   - возврат через существующий `BackButton`/локальное состояние, без router package.

### 3.4. Состояния

- `loading`: skeleton той же геометрии; не заменять экран одной строкой.
- `error`: компактный встроенный блок + текущая команда «Повторить».
- `empty`: объяснить, что появится здесь, и показать только существующее доступное действие.
- `search empty`: «Ничего не найдено» + способ очистить запрос.
- `disabled`: «Практикумы · СКОРО», без клика, haptic и ложного открытия.
- `web fallback`: статьи работают; направленные записи честно показывают существующее Telegram-only ограничение.

### 3.5. Файлы ожидаемой реализации

- `src/components/ui-lab/LibraryExperiment.jsx` — Preview-only composition.
- `src/components/ui-lab/LibraryExperiment.css` — все новые стили под локальным modifier/root.
- `src/components/ui-lab/UiLab.jsx`, `uiLabCatalog.js` — маршрут `?ui_lab=library`.
- отдельный UI Lab checker и targeted contract tests.
- production `src/screens/Library.jsx`, `Articles.jsx`, `GuidedJournals.jsx`, `Courses.jsx` **не меняются до owner gate UI Lab**.

## 4. `IMG_6009` — сохранённый контракт «Истории»

Этот блок зафиксирован для будущего, но не входит в #526 и не разрешает новый PR.

### Наблюдения

- `00:00–00:03`: записи сгруппированы по дням; сверху переключатель масштаба, фильтр и поиск.
- `00:02–00:04`: detail записи — свободная вертикальная композиция без карточки вокруг всего контента.
- `00:05–00:11`: карточки различной высоты в зависимости от содержания; mood tags остаются внутри записи.
- `00:08–00:10`: achievement открывается как bottom sheet поверх затемнённой History, а не как новая вкладка.
- `00:12–00:17`: переключение `Days → Weeks`; недели группируются по месяцам и показывают краткие счётчики типов записей.

### Mentalix interpretation

- Сохранить уже существующую группировку истории и реальные типы записей.
- Возможный будущий проход: day/week presentation, поиск/фильтр и detail без общей тяжёлой оболочки.
- Не копировать achievement sharing, если такого product contract нет.
- Не объединять с #526 и не менять persistence/API без отдельной задачи.

## 5. `IMG_6010` — сохранённый контракт #527 «Прогресс»

Не начинать до закрытия #526.

### Наблюдения

- `00:00–00:05`: period selector `Weeks / Months / Years`, крупный заголовок и один главный line chart.
- `00:06–00:12`: секция общих наблюдений, карточка ведущей активности и календарь состояния.
- `00:12–00:18`: отдельный ярус эмоций; insight-карточки сохраняют одинаковую геометрию даже при отсутствии данных.
- `00:18–00:27`: типы активности разделены на самостоятельные карточки с локальным переключателем метрики.
- Внизу закреплён period navigator; он не должен перекрывать bottom navigation и safe-area.

### Mentalix interpretation

- Использовать реальные `api.analytics.get`, check-ins, rituals и ascezas.
- Сохранить честный порог данных и текущие caveat-формулировки; не создавать причинные выводы.
- Не переносить HealthKit, daylight, mindful minutes или иные отсутствующие источники.
- Period selector ограничить реально поддержанными периодами; `Years` не показывать без данных/API.
- Empty states должны объяснять, какое существующее действие создаст данные.

## 6. `IMG_6012` — профиль, достижения и сводная статистика

Этот reference не входит в #526. Он уточняет будущую структуру профиля и частично помогает #527, но не разрешает добавлять новую механику достижений.

### Наблюдения

- `00:00–00:03`: профиль открывается как самостоятельная поверхность с компактным header и закрытием; настройки собраны в короткие тематические группы.
- `00:03–00:10`: верх профиля содержит крупные промо-блоки, затем персональные настройки и account-секции. Premium, gifting и newsletter являются чужой продуктовой логикой и не переносятся.
- `00:12–00:18`: переключатель `Badges / Stats` закреплён сверху; главный achievement-card отделён от списка следующих достижений.
- `00:14–00:18`: будущие достижения показывают название, условие и числовой прогресс в одном горизонтальном ряду; скрытие streak/badges управляется отдельными toggles.
- `00:19–00:22`: сводная статистика разбита на смысловые группы — записи, mindfulness, технические метрики — вместо одного длинного смешанного списка.

### Mentalix interpretation

- В текущем `Profile.jsx` уже есть реальные `stats`, текущая/лучшая серия и путь пользователя. Их можно позже перегруппировать визуально, не меняя вычисления и API.
- Для #527 допустимо переиспользовать паттерн сегментированного переключателя и смысловых групп, но только для существующих данных `api.analytics.get`, check-ins, rituals, ascezas и themes.
- Не добавлять систему бейджей, уровни за годы использования, шаринг, premium, gifting, newsletter, Apple Health или переключатели видимости без отдельного product contract.
- Технические метрики вроде размера базы и числа обновлений не являются пользовательской ценностью Mentalix и не должны попадать в основной профиль.

## 7. `IMG_6013` — сквозной reference поведения приложения

Видео объединяет несколько поверхностей. Это не одна задача и не разрешение переносить их в один PR.

### Наблюдения по таймкодам

- `00:00–00:05`: домашний экран показывает friendly reminder о backup и пустой блок персональных практик. iCloud backup не переносится; полезен только паттерн встроенного, закрываемого напоминания.
- `00:05–00:29`: настройка «своих практик» открывается поверх home. Пустое состояние даёт два действия: создать своё и добавить из библиотеки. Каталог использует горизонтальные chips-категории, компактные строки, семантическую иконку и `+` справа; недоступные элементы сохраняют ту же геометрию.
- `00:30–00:43`: home построен как прокручиваемый набор самостоятельных ярусов: текущая тема, горизонтальный rail карточек и локальная настройка блока. Край соседней карточки всегда виден.
- `00:44–00:50`: CTA карточки открывает bottom sheet выбора способа записи: пустая страница, случайный вопрос или конкретная подсказка. Затем пользователь возвращается в существующий editor.
- `00:52–00:57`: экран цитаты использует один доминирующий объект, минимум chrome и три контекстных действия; это отдельный reference чтения, не часть #526.
- `00:58–01:27`: библиотека тем содержит текущую тему, случайный выбор, фильтры статуса и вертикальный каталог. Карточка темы показывает краткое описание, прогресс и preview первого вопроса, но не раскрывает весь контент заранее.
- `01:17–01:22`: editor использует крупный вопрос, поясняющий prompt, свободное поле записи и keyboard-safe dock. Это подтверждает ранее зафиксированный `visualViewport`/fullscreen контракт Mentalix.
- `01:28–01:32`: History в состоянии малого количества данных остаётся разреженной и не заполняется декоративными пустыми карточками.
- `01:33–01:45`: Progress показывает честные data-threshold состояния, месячный навигатор, календарные точки и отдельные карточки по типам активности; одинаковая геометрия сохраняется при `No data yet`.

### Mentalix interpretation по существующим функциям

| Reference-паттерн                  | Реальная опора Mentalix                                                                 | Решение                                                                                             |
| ---------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| Добавить практику из каталога      | Каталог Practices, allowlist и существующие flow                                        | Не переносить в #526; рассмотреть отдельной задачей персонализации после аудита реального контракта |
| Создать собственный journal-шаблон | `GuidedJournals.jsx` и `api.journalTemplates` уже поддерживают private template builder | В #526 сохранить функцию; менять только presentation после owner gate                               |
| Тема недели и вопросы              | `api.themes`, `PracticeCatalogV2`, реальный reflect flow                                | Сохранить текущие четыре дня/вопроса и русскую копию; не создавать второй источник данных           |
| Выбор способа записи               | Существующий Journal и guided flow                                                      | Возможен поздний UX-pass; не добавлять новый editor или способ хранения                             |
| История с фильтрами                | Текущая user-scoped History                                                             | Вынести в отдельный scope; не смешивать с Library/Progress                                          |
| Календарь и insight-карточки       | `Analytics.jsx`, реальные check-ins/rituals/ascezas и порог достаточности               | Использовать в #527 без причинных выводов и вымышленных метрик                                      |
| Backup reminder                    | Privacy export есть; cloud backup отсутствует                                           | Не обещать cloud/iCloud; допустима только честная ссылка на существующий экспорт                    |

### Новые системные правила, подтверждённые видео

- Один экран может содержать несколько самостоятельных инструментов, но каждый ярус должен иметь один главный CTA.
- Каталог для выбора действия использует компактные строки; каталог для исследования контента — крупные карточки. Эти два типа не смешиваются.
- Bottom sheet применяется для короткого решения без потери контекста; полноценный каталог и editor остаются отдельными поверхностями.
- Прогресс показывается рядом с объектом, к которому относится, а не как общий декоративный процент.
- Sparse/empty состояние сохраняет воздух и объясняет следующий реальный шаг; фиктивные данные не подставляются.

## 8. Не переносим из референсов

- брендинг, тексты, английский язык и типографику Stoic;
- premium/paywall механику, цены и free trial;
- HealthKit и отсутствующие источники данных;
- sharing achievements;
- новые категории/темы, которых нет в данных Mentalix;
- новые backend endpoints, router, глобальный state manager;
- document-level touch handlers;
- фиолетовую палитру, градиенты и буквальные Stoic-иллюстрации.

## 9. Acceptance gate для текущего UI Lab #526

- Маршрут Preview-only: `?ui_lab=library`.
- На 390 × 844 видны header, первый content rail и начало «Коллекций»; bottom navigation ничего не перекрывает.
- На 375 × 812 и 320 × 568 нет clipping, горизонтального overflow страницы и обрезанного текста.
- Rail прокручивается пальцем и snap-ится; следующий элемент виден до свайпа.
- Активные карточки открывают соответствующий demo/detail state; «Практикумы» не активны.
- Проверены loading/error/empty/search-empty/web-restriction состояния.
- Используются Onest/Manrope и токены Mentalix; глобальные production-стили не изменены.
- `npm run check:core`, `npm run ux:check`, targeted UI Lab checker и `git diff --check` проходят.
- После локального self-check нужен owner gate на canonical `mentalix-preview`; только затем разрешён отдельный production-integration diff.

## 10. Rollback и scope lock

- Текущий этап меняет только UI Lab и этот reference contract.
- Production Library, API, backend, навигация и Vercel-конфигурация не меняются.
- Rollback — удалить UI Lab route/components/tests и этот документ одним revert PR; пользовательские данные не затрагиваются.
