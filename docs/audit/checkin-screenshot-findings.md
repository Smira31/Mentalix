# Check-In screenshot findings

The local preview capture shows the default Today CTA, the sequential mood scale, the guided journal editor with add/format/deepen controls, and the completion screen with feedback choices. The screenshots were captured at a 390×844 mobile viewport against the local Vite app using `?demo=1` so the preview fixture could authenticate without a real account.
