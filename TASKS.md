# Mentalix — задачи

Последнее обновление: 14.08.2026

Верхняя часть `TASKS.md` — единственный источник актуальной работы.
Завершённые и устаревшие пункты сохраняются ниже в `Completed / Archived`.
`ROADMAP.md` — порядок крупных продуктовых этапов.

Обозначения:

- `[ ]` — предстоит;
- `[x]` — выполнено;
- `Частично` — основа существует, требуется доводка;
- `Блокер` — работа зависит от внешнего решения или материала.

## Завершено 10.08.2026 — научный обзор литературы для 11 карточек проблем

- [x] **MXL-LIT-001 — Оформить научный обзор литературы в карточки 11 проблем**
  - Созданы `docs/problems/motivaciya.md`, `strah-oshibki.md`, `strah-ocenki.md`, `trevoga.md`, `vygoranie.md`, `samokritika.md`, `perfekcionizm.md`, `chuzhoe-mnenie.md`, `nizkaya-samoocenka.md`, `poterya-napravleniya.md`, `nehvatka-energii.md` по `problem_template.md`.
  - Каждый раздел «Источники» содержит полную научную атрибуцию (автор/год/журнал/DOI, PMID или PMC ID); неполные атрибуции из исходного материала отмечены явно.
  - `docs/problems/PROBLEMS.md` дополнен ссылками на карточки и разделом о научном обзоре; статус проблем и приоритизация не изменены.
  - В `docs/core/PRODUCT_DECISIONS.md` добавлено «Обновление 10.08.2026» к `MXL-DEC-007` с разделением двух уровней доказательности проекта.
  - Критерий пяти проблемных интервью на проблему не закрыт ни для одной из 11 проблем; статус «проверено» не устанавливался.

## Завершено 10.08.2026 — почистить ветки GitHub с Ahead 0

- [x] **Почистить ветки GitHub с Ahead 0**
  - Примечание: этот пункт не был ранее формально заведён отдельной строкой в `TASKS.md` (проверено — по «Ahead»/«почистить ветки» совпадений в истории файла не найдено); фиксируется здесь как выполненный по прямому запросу владельца.
  - Составлен полный список веток `origin` с ahead/behind относительно `main` (`git rev-list --left-right --count`) и сверен со статусами PR (`gh pr list --state all`).
  - Удалены 6 веток с `origin`, чьё содержимое уже полностью в `main` или сознательно устарело: `agent/disable-forced-evening-review` (PR #2, merged), `agent/fix-mentalix-build` (PR #3, merged), `agent/mentalix-development-system-v1` (PR #1, merged), `agent/update-project-status-2026-07-29` (PR #4, merged), `docs/methodology-v0.1` (PR #5, merged) — все пять Ahead 0; `agent/grover-methodology` (PR #6, closed без слияния владельцем — содержимое (`docs/methodology/PRINCIPLES.md`, `docs/methodology/books/tim-grover.md`) уже присутствует в `main` в более полной версии, подтверждено по комментарию владельца в PR и сверкой файлов).
  - Не тронуты: ветки с открытыми PR (`agent/update-mentalix-core-philosophy` #11, `codex/ui-reference-catalog` #21, `codex/card-directions-lab` #26), активные worktree (`codex/card-system-docs`, `codex/practice-motion-kit` — обе с реальными ahead-коммитами) и ветки без ahead=0 без открытого PR (`codex/problem-led-methodology`, `codex/rituals-asceza-art`) — решение по ним не запрашивалось и не принималось.
  - Squash-мёрженные ветки с формально ненулевым ahead (например `agent/mentalix-core-v0-1`, `codex/mentalix-ui-motion`) не тронуты — вне периметра этой задачи (критерий был именно Ahead 0).

## Выпущено 09.08.2026 — орбитальная карточка ритуалов

- [x] **MXL-RITUAL-ORBITS — Вернуть прежний орбитальный знак и оживить его**
  - Геометрия прежней орбитальной композиции сохранена; золотая точка и три слоя овалов вращаются с разными периодами, средний слой — в обратную сторону.
  - Аскеза оставлена без изменений до отдельного утверждения новой метафоры физического разрыва или поломки привычного паттерна.

## Завершено 29.07.2026 — Development System и baseline

- [x] **MXL-DS1 — Внедрить Mentalix Development System v1**
  - Добавлены PRODUCT, DESIGN_SYSTEM, ARCHITECTURE, ROADMAP, AI_RULES и REFERENCE_WORKFLOW.
  - Документация слита в `main` через PR #1.
- [x] **MXL-BUILD — Восстановить production-сборку**
  - Закрыта пропущенная функция `Chat` в `Mentalix.jsx`.
  - Vercel build подтверждён, PR #3 слит.
- [x] **MXL-EVENING — Отключить принудительный вечерний режим**
  - `FORCE_EVENING_REVIEW=false`.
  - Вечерний анализ включается по `review_hour`, PR #2 слит.
- [x] **MXL-SMOKE — Проверить пять вкладок и web core loop**
  - Проверены Сегодня, Практики, Наставник, Библиотека и Тренды.
  - Созданы и отмечены тестовый ритуал и тестовая аскеза.
  - Завершён вечерний анализ из семи шагов.
  - Следопыт получил данные дня и вернул ответ.
  - Аналитика подхватила настроение, ритуал и аскезу.
- [x] **MXL-IOS — Проверить Telegram Mini App на реальном iPhone**
  - Пользователь подтвердил fullscreen, safe areas, клавиатуру, навигацию и повторное открытие без критических проблем.

## Завершено 08.08.2026 — техническая уборка репозитория

- [x] **Удалить неиспользуемые .tsx-дубликаты**
  - Удалены `src/screens/Today.tsx` и `src/components/MorningPilotCard.tsx` (остатки брошенной попытки перехода на TypeScript, импортов на них не было).
  - `npm run build` подтверждён после удаления.
- [x] **Исправить описание lint-скрипта в ARCHITECTURE.md**
  - П. 4 раздела рисков теперь верно отражает наличие `npm run lint` (ESLint) при отсутствии typecheck.
- [x] **Перевести AGENTS.md на формат для AI-агентов**
  - Подключается через новый `CLAUDE.md`; `CONTEXT.md` и `СОСТОЯНИЕ.md` архивированы в `docs/archive/`.
- [x] **Добавить токен --c-faint в таблицу DESIGN_SYSTEM.md**
  - В таблице «Тёмная тема» (раздел 2) не хватало строки для `--c-faint`, хотя токен используется в `src/index.css` и упомянут в разделе «Шкала текста».
  - Commit: `9398951`.
- [x] **Починить favicon.ico (отдавал 404)**
  - В `public/` не было файла иконки, в `index.html` не было `<link rel="icon">`.
  - Добавлены `public/favicon.ico` и `public/favicon.svg` (упрощённый символ лабиринта из `MazeLogo.jsx`, существующие токены `--c-gold`/`--c-bg`, новых цветов не вводилось), ссылки прописаны в `index.html`.
  - `npm run build` подтверждён; `dist/favicon.ico` и `dist/favicon.svg` отданы 200 из `vite preview`.
  - PR #12 (`fix/favicon-404`) смёржен в `main` (squash, `b192a21`), ветка удалена.
- [x] **MXL-WEB-CHECKIN-BTN — Починить кнопку завершения check-in в браузере**
  - Причина бага: `CheckIn.jsx` отдавал действия шагов только Telegram `MainButton`/`SecondaryButton` (`telegram.hooks.js`) — вне Telegram эти хуки no-op, кнопки в DOM не было ни на одном шаге, включая финальный экран.
  - Фикс: в `CheckIn.jsx` добавлена веб-версия кнопки (`webActionBar`, `cta-pill`), рендерится только при `platformName !== 'telegram'`, использует уже вычисленные `mainAction`/`skipAction`. Telegram-путь не менялся.
  - `npm run build` подтверждён; ручная проверка в браузере пройдена (desktop) — кнопка видна и работает.
  - PR #13 (`fix/checkin-web-finish-button`) смёржен в `main` (squash, `97b59fb`), ветка удалена.
- [x] **MXL-WEB-BACKBTN — Починить кнопку «Назад» в браузере на 13 экранах**
  - Причина бага: `BackButton.jsx` решал, рисовать ли DOM-кнопку, по `Boolean(window.Telegram?.WebApp?.BackButton)` — этот объект есть всегда (создаётся `@twa-dev/sdk` безусловно), проверка была true и в вебе тоже, поэтому кнопка нигде не рисовалась в браузере.
  - Нативный Telegram `BackButton` (через `useBackButton`) в реальном Telegram работает исправно — это подтверждено версионной проверкой внутри SDK, баг был только в веб-фолбэке.
  - Фикс: условие заменено на `platformName === 'telegram'` (тот же признак, что и у `WebActionBar`). `useBackButton`/Telegram-путь не менялись.
  - `npm run build` подтверждён. Ручная проверка пройдена на всех 13 экранах (`Today.jsx`, `App.jsx`, `ThemeScreen.jsx`, `Rituals.jsx`, `Ascezas.jsx`, `mentalix/Conversation.jsx`, `mentalix/JournalStart.jsx`, `Settings.jsx`, `QuoteView.jsx`, `BrainTrainer.jsx`, `Breathing.jsx`, `Articles.jsx`, `Practices.jsx`) в браузере (desktop и мобильный viewport) и в самом Telegram — регрессии нативной кнопки нет, багов не найдено.
  - PR #15 (`fix/backbutton-web`) смёржен в `main` (squash, `d6929da`), ветка удалена.
- [x] **MXL-WEB-ACTIONBAR — Проверить веб-кнопки на 4 экранах после рефакторинга WebActionBar**
  - Логика веб-кнопки из `CheckIn.jsx` (`MXL-WEB-CHECKIN-BTN`) вынесена в переиспользуемый `src/components/WebActionBar.jsx` и применена на трёх экранах с той же причиной бага (`useMainButton`/`useSecondaryButton` без веб-эквивалента): `Rituals.jsx` (`CreateRitualScreen`), `Ascezas.jsx` (`CreateAscezaScreen`), `ThemeScreen.jsx` (все три состояния — `intro`/`day`/`review`).
  - `npm run build` подтверждён.
  - Ручная проверка пройдена вживую на реальном backend: `ThemeScreen.jsx` (все три состояния — «Начать», «Обдумал»/«Обновить мысль», «Закрыть тему»), `Rituals.jsx` (создание ритуала с реальным сохранением через API), `Ascezas.jsx` (принятие аскезы с реальным сохранением через API), `CheckIn.jsx` (check-in и вечерний анализ целиком после рефакторинга на `WebActionBar`) — багов не найдено.

## Сейчас

- [x] **MXL-MERGE-BATCH-001 — Мёрж трёх готовых draft PR (#56, #55, #57)**
  - По прямому запросу владельца три готовых draft PR смёржены по очереди в `main` строго в порядке #56 → #55 → #57, каждый через: `git pull origin main` в ветку PR, `npm run build`, `gh pr ready`, `gh pr merge --squash --delete-branch`.
  - **#56 (MXL-018, удаление неиспользуемого `src/components/Icons.jsx`):** конфликт при `git pull origin main` в `CHANGES.md` (независимые записи в верху файла, без смыслового противоречия) — разрешён вручную, обе записи оставлены, запись PR сверху. Build прошёл. Смёржен squash-коммитом `4c060f9`, ветка `codex/remove-unused-icons` удалена.
  - **#55 (MXL-UI-003, полное отображение заголовка «Нейротренажёр» в сетке Практик):** конфликт при pull в `CHANGES.md` и `TASKS.md` — в `TASKS.md` разрешён по уже заявленному в документе порядку исправлений («шапка → CTA AI → доступность toggle → карточка Практик»), то есть запись `MXL-UI-004` оставлена выше `MXL-UI-003`. Build прошёл. Смёржен squash-коммитом `bb07969`, ветка `codex/mxl-ui-practices-title` удалена.
  - **#57 (MXL-017, разделение frontend bundle на динамические чанки через `React.lazy`/`Suspense`):** ветка `codex/split-frontend-bundle` была занята отдельным git worktree (`mxl-cycle3-current`, чужая рабочая копия Codex Desktop) с незакоммиченной правкой `TASKS.md` — правка сохранена отдельным коммитом (не отброшена), работа продолжена в том же worktree. При pull — конфликт в `CHANGES.md` и `TASKS.md`: обе стороны редактировали одни и те же пункты `MXL-017`/`MXL-018` по-разному (ветка PR ещё не знала про уже смёрженный #56, а `main` — про #57); разрешён объединением обоих актуальных блоков `[x]` без дублей. Build прошёл, предупреждение Vite о чанке >500 kB исчезло (стартовый чанк `828 kB` → `334.74 kB`). Смёржен squash-коммитом `3e9bb14`. `gh pr merge --delete-branch` не смог сам удалить ветку и переключить worktree на `main` (та же блокировка — `main` занят в `frontend-deploy`); ветка `codex/split-frontend-bundle` удалена на remote отдельной командой `git push origin --delete`; локальный worktree `mxl-cycle3-current` не трогался.
  - После каждого мержа выполнен `git checkout main; git pull origin main` в рабочей папке `work/frontend-deploy`; финальный `main` — commit `3e9bb14`.
  - **Подтверждено 14.08.2026 (владелец, реальный iPhone/Telegram):** заголовок «Нейротренажёр» в сетке Практик (MXL-UI-003) не обрезается; ленивая загрузка экранов `Practices`, `Analytics`, `Mentalix`, `Profile`, `Settings`, `Library` (MXL-017) открывается без зависаний и пустых экранов. Открытых вопросов по MXL-017/MXL-UI-003/MXL-018 не осталось, все три задачи полностью закрыты по правилу `AI_RULES.md` §6.1.

- [x] **MXL-DOCS-CLEANUP-001 — Ревизия правки AGENTS.md и уборка рабочей папки**
  - Владелец сообщил, что применённый ранее patch «съел» пустую строку-разделитель перед абзацем про напоминание о pre-mortem в `AGENTS.md`. Проверка `git diff`/построчного вывода файла показала, что пустая строка на месте с обеих сторон абзаца (после «нельзя менять без согласования.» и перед `docs/archive/CONTEXT.md`); правка не потребовалась, diff показан владельцу как есть и подтверждён.
  - Причина расхождения: в рабочей папке лежали два патч-файла от предыдущей попытки — `agents-fix.patch` (сломанная кодировка, UTF-16LE) и `agents-fix-utf8.patch` (корректный UTF-8, уже применён к рабочему дереву). Оба файла-мусора удалены.
  - `graphify-out/` определён как сгенерированный вывод скилла graphify (маркер `.graphify_root`, кэш, `graph.json`/`graph.html`/`manifest.json`), не сторонний инструмент. Добавлен в `.gitignore` по прямому решению владельца.
  - **Закрыто:** прямой commit в `main` 14.08.2026 по запросу владельца, без отдельной ветки/PR.

- [x] **MXL-IOS-AUTOZOOM-001 — Убрать iOS auto-zoom на полях ввода (гипотеза причины «двойного текста»)**
  - Гипотеза владельца: поля форм с `font-size < 16px` вызывают автозум iOS Safari/Telegram WebView при фокусе — возможная причина и утечки нижней панели, и самого бага с призрачным текстом «Создать ритуал» (см. `MXL-RITUAL-CARD-001`, диагностика по `start_param`, PR #45).
  - Проверен тег viewport (`index.html`): `width=device-width, initial-scale=1, viewport-fit=cover` — ни `maximum-scale`, ни `user-scalable` нет вообще; тег не пытается управлять зумом, противоречия с гипотезой нет.
  - Полный аудит: 32 `<input>`/`<textarea>` в 13 файлах. Найдено и исправлено 9 полей с `font-size < 16px` → `text-[16px]`: `inputCls` в `Rituals.jsx`/`Ascezas.jsx` (по 5 полей формы каждый), `BreakContextSheet` textarea в `Ascezas.jsx`, поиск в `Articles.jsx`, 4 поля в `Courses.jsx`, 3 поля в `Path.jsx`, поле в `QuotesManager.jsx`, email в `WebAuthScreen.jsx`. Остальные 23 поля уже были ≥16px, не трогались.
  - **Подтверждено 11.08.2026:** владелец проверил на реальном устройстве после этого фикса — зум и утечка нижней панели ушли. Причина второго симптома `MXL-RITUAL-CARD-001` (дублирующийся текст «Создать ритуал») закрыта этим фиксом; диагностический код (`ghostDebug`) удалён отдельно, см. запись `MXL-RITUAL-CARD-001` ниже.
  - `npx eslint` по изменённым файлам и полный `npm run lint` — 0 ошибок, 31 предупреждение (прежний базовый уровень, новых нет); `npm run build` проходит.
  - **Закрыто:** PR #46 смёржен squash-коммитом `8bf0191` в `main` по прямому запросу владельца 11.08.2026; ветка `fix/ios-input-autozoom` удалена (remote и локально). Деплой на `mentalix.vercel.app` подтверждён (GitHub commit status `Vercel: success`).

- [x] **MXL-PRACTICES-LOCK-001 — Временно заблокировать вход в Нейротренажёр/Дыхание/Фокус, пометка «Скоро»**
  - Запрос владельца: три карточки практик на экране «Практики» — Нейротренажёр, Дыхание, Фокус — заблокировать от захода внутрь (без удаления/изменения функционала и экранов внутри), показать пометку «Скоро»; карточки остаются на месте и видны.
  - Найден и переиспользован готовый паттерн: проп `soon` в `src/components/PracticeCard.jsx` (уже используется карточкой «Медитации» в этом же файле) — `disabled`/ранний `return` в `onClick` до вызова `onOpen` (тап гарантированно ничего не открывает), бейдж «Скоро», притушенная иллюстрация (`opacity-35`) и текст (`text-muted`), без tap-scale анимации.
  - Владелец выбрал вариант «переиспользовать `soon` как есть» (тот же визуал, что уже у «Медитации») из двух предложенных на согласовании — второй вариант (бейдж без притушения, потребовал бы нового пропа в `PracticeCard.jsx`) не выбран.
  - Реализация — 3 строки: добавлен bare-проп `soon` к трём существующим `<PracticeCard>` (`src/screens/Practices.jsx:208-217/219-228/230-239` — Нейротренажёр/Дыхание/Фокус). `onOpen` (`setSub('brain')`/`setSub('breathing')`/`setSub('focus')`) не тронут — навигация и так блокируется `disabled`+ранним `return` внутри `PracticeCard`, поэтому снять блокировку позже — удалить три слова `soon`, экраны `BrainTrainer`/`Breathing`/`Focus` и их код не менялись и не удалялись.
  - `npx eslint src/screens/Practices.jsx` — 0 ошибок, 1 предупреждение (прежнее, не про изменённые карточки); полный `npm run lint` — 0 ошибок, 31 предупреждение (прежний базовый уровень). `npm run build` проходит.
  - **Закрыто:** PR #40 смёржен squash-коммитом `5a68aa0` в `main` по прямому запросу владельца 11.08.2026; ветка `feature/practices-soon-lock` удалена (remote и локально).

- [x] **MXL-NEURO-DETAIL-ANIM-001 — Убрать анимацию на внутреннем экране Нейротренажёра**
  - Владелец решил: убрать саму анимацию (`CardSystemGlyph kind="neuro-synapse"`), но оставить карточку-обёртку (`rounded-[24px]`, фон/рамка, `h-[164px]`) на месте — пустой, без анимированного контента.
  - `src/screens/BrainTrainer.jsx:498-506`: убран рендер `<CardSystemGlyph kind="neuro-synapse" />` и ветка `<SemanticGlyph kind="neuro" .../>` целиком (обе решались одним и тем же флагом `cardSystemPreviewEnabled` — раз контента больше нет, разбирать по флагу стало нечего). Div-обёртка и её className-тернарник (`h-[164px]`/transparent+border при `cardSystemPreviewEnabled=true` vs `h-[116px]`/`bg-artbed`+border при `false`) не тронуты — сознательно, владелец просил убрать анимацию, а не логику размеров обёртки; если в итоге нужен единый фиксированный стиль без зависимости от флага — отдельное решение.
  - Импорт `CardSystemGlyph` удалён как более не используемый в файле. Импорт `SemanticGlyph` остался — используется отдельно и без изменений в карточках самих упражнений (`kind={ex.kind}` на `:518`).
  - `npx eslint src/screens/BrainTrainer.jsx` — 0 ошибок, 6 предупреждений (все прежние, не про удалённый блок); полный `npm run lint` — 0 ошибок, 31 предупреждение (прежний базовый уровень). `npm run build` проходит.
  - **Закрыто:** PR #39 смёржен squash-коммитом `60a2852` в `main` по прямому запросу владельца 11.08.2026; ветка `fix/neuro-detail-remove-animation` удалена (remote и локально).

- [x] **MXL-WEB-HAPTIC — Убрать предупреждения Telegram HapticFeedback в web**
  - Разбор кода: `platform.haptic(style)` — единственная точка вызова хаптики (~100 мест по экранам/компонентам), маршрутизируется через `src/platform/index.js` по признаку `window.Telegram?.WebApp?.initData`. В вебе (`src/platform/web.adapter.js:28`) `haptic()` — честный no-op, ничего не пишет в консоль. В Telegram (`src/platform/telegram.adapter.js:39-45`) вызывает `WebApp.HapticFeedback` из `@twa-dev/sdk`, где найден потенциальный источник предупреждения — версионный гейт `HapticFeedback is not supported in version 6.0` (`node_modules/@twa-dev/sdk/src/telegram-web-apps.js:1183`, дефолт `webAppVersion = '6.0'` до получения `tgWebAppVersion` от реального Telegram-запуска).
  - Живая проверка владельцем: `mentalix.vercel.app` открыт через Menu Button настоящего Telegram-бота на iPhone — вибрация при действии с ритуалом ощущается, предупреждений/проблем не обнаружено.
  - Вывод: код работает штатно на реальном пути запуска (Telegram Menu Button → настоящий Telegram-клиент с актуальной версией Bot API); гипотетический сценарий с версионным гейтом либо не воспроизводится в проде, либо относится только к нестандартным сценариям тестирования (старые Telegram-клиенты/desktop), не к обычному вебу или мобильному Telegram.
  - Код не менялся, изменений не потребовалось — задача закрыта по результату живой проверки, без PR (веток/кода не создавалось).

- [x] **MXL-PWA-ICONS — Добавить недостающие PWA-иконки**
  - Спецификация (заведена аудитом 08.08.2026, см. запись ниже в «Completed / Archived» и `CHANGES.md`) сверена на актуальность перед генерацией — факты не устарели: `public/icons/` по-прежнему не существовала, все три PNG из `manifest.json` отдавали 404, `<link rel="manifest">` в `index.html` отсутствовал.
  - Источник символа подтверждён владельцем: тот же дизайн, что `public/favicon.svg`/`src/components/MazeLogo.jsx` (чёрный диск, золотое кольцо-лабиринт, золотая точка входа), новых элементов не вводилось.
  - Сгенерированы программно из SVG (Node + `sharp`, растеризация в рамках сессии, инструмент временный — не добавлен в `package.json`/`package-lock.json` проекта, использовался только в scratch-окружении): `public/icons/icon-192.png` (192×192, `purpose: any`), `public/icons/icon-512.png` (512×512, `purpose: any`) — из плоского SVG-варианта (тот же диск, что у favicon, прозрачные углы вне круга); `public/icons/icon-maskable-512.png` (512×512, `purpose: maskable`) и `public/apple-touch-icon.png` (180×180, без альфа-канала — подтверждено `sharp.metadata()`: `channels: 3, hasAlpha: false`) — из отдельного SVG-варианта: сплошная чёрная заливка на весь квадрат без прозрачности (проверено попиксельно по углам и середине кромки — везде `[0,0,0,255]`), символ уменьшен в 0.82 раза, чтобы уместиться в безопасную зону maskable-иконок (~центральные 80% холста).
  - Владелец подтвердил решить несостыковку цвета в пользу актуального фона: `manifest.json` — `background_color`/`theme_color` изменены с `#0E211D` на `#000000` (актуальный `--c-bg` из `DESIGN_SYSTEM.md`/`src/index.css`).
  - `index.html`: добавлены `<link rel="apple-touch-icon" href="/apple-touch-icon.png">` и `<link rel="manifest" href="/manifest.json">` — без них ни один из файлов реально не подключался браузером/iOS.
  - Проверка: `manifest.json` — валидный JSON (`node -e "require('./public/manifest.json')"`). `npm run build` проходит. `npm run preview` + `curl` на все пять путей (`manifest.json`, `icons/icon-192.png`, `icons/icon-512.png`, `icons/icon-maskable-512.png`, `apple-touch-icon.png`) — везде `200`, 404 не воспроизводится. Отданный `index.html` содержит оба новых `<link>`, отданный `manifest.json` содержит `#000000`.
  - **Подтверждено 12.08.2026:** живая установка «Добавить на экран» в Telegram на iPhone проверена и подтверждена рабочей владельцем. Открытых пунктов проверки по задаче не осталось.
  - **Закрыто:** PR #37 смёржен squash-коммитом `94bd4ee` в `main` по прямому запросу владельца 11.08.2026; ветка `feature/pwa-icons` удалена (remote и локально). Задача полностью закрыта по правилу `AI_RULES.md` §6.1.

- [x] **MXL-DOCS-001 — Ужать AGENTS.md, убрать дублирование с другими md-файлами**
  - `AGENTS.md` — единственный автозагружаемый документ проекта (Claude Code через `@AGENTS.md` в `CLAUDE.md`, Codex — напрямую); остальные документы читаются по требованию через «Documentation map» внутри него. Найдено, что несколько разделов дословно или почти дословно дублировали нормативные документы, которые `AGENTS.md` сам же предписывает читать.
  - Убраны разделы `## Architecture` (дерево `src/`, абзац про роутер/state-слой — дублировали `ARCHITECTURE.md` §2–5, §7), `### Platform layer` (дублировал `AI_RULES.md` §9 «Платформа», включая историю про восемь копий `haptic`), `### Fullscreen surfaces` (дублировал `ARCHITECTURE.md` §8), `### Design tokens` (дублировал `DESIGN_SYSTEM.md` §2/§4 — и прямо нарушал собственное правило проекта `AI_RULES.md §9` «Документы»: значения токенов должны жить только в `DESIGN_SYSTEM.md`), `### Other invariants (from AI_RULES.md §9)` (дословная копия того же §9, уже помеченная в заголовке как взятая оттуда).
  - Сокращены: абзац `## Project` (убраны версии стека и состав backend-стека — точные версии в `ARCHITECTURE.md` §1, риск разойтись с кодом при апдейте зависимостей), описание `AI_RULES.md` в «Documentation map» (было построчным повтором §2/§5/§6.1, оставлена одна фраза «binding, не paraphrase»), пункт «Gotchas» про `.tsx`-дубликаты (убраны дата и коммит удаления — это работа `CHANGES.md`/`TASKS.md` по правилу «Журналы» из `AI_RULES.md §9`, оставлено только правило-на-будущее).
  - Не тронуты: `## Language`, укороченный `## Project` (граница frontend/backend и запрет выдумывать API — нигде больше не сформулирован как инструкция агенту), `## Commands`, `## Documentation map` (сам механизм), `## Pre-mortem before big changes` (добавлено недавним прямым запросом владельца, явно исключено из сокращения), пункт про `vercel.json`/`/api`-rewrite в Gotchas (нигде не продублирован).
  - Итог: `AGENTS.md` 160 строк → 76 (−53%). Изменения только документационные, код и `npm run build` не затронуты.
  - План/diff был показан владельцу до коммита, подтверждён без правок.
  - **Закрыто:** PR #36 смёржен squash-коммитом `8edaff2` в `main` по прямому запросу владельца 11.08.2026; ветка `docs/shrink-agents-md` удалена (remote и локально).

- [x] **MXL-LINT-002 — eslint-plugin-react + react/jsx-uses-vars (закрывает отложенный пункт MXL-LINT-001)**
  - Установлен `eslint-plugin-react` (`devDependencies`), в `eslint.config.js` подключён плагин и включено `'react/jsx-uses-vars': 'error'`.
  - Убраны ровно три ложных `no-unused-vars` (`Icon` в `QuickAdd.jsx`, `Art` в `ArchetypeShowcase.jsx`, `PersonaArt` в `JournalStart.jsx`) — деструктурированные параметры компонентов, используемые только внутри JSX, которые core-правило не засчитывало использованием.
  - `npm run lint`: 34 → 31 предупреждение (0 ошибок оба раза), остальные предупреждения не изменились. `npm run build` проходит.
  - **Закрыто:** PR #35 смёржен squash-коммитом `233f915` в `main` по прямому запросу владельца 11.08.2026; ветка `feature/eslint-plugin-react-jsx-uses-vars` удалена (remote и локально).

- [x] **MXL-LINT-001 — Инструменты форматирования и линтинга фронтенда (по аналогии с бэкендом)**
  - Сверена фактическая структура репозитория: `package.json`/`src/`/`eslint.config.js` в корне `frontend-deploy`, вложенной `frontend/` нет.
  - Добавлены `.prettierrc` (`semi: false` — под фактический стиль фронтенда, не скопирован буквально от `mentalix-bot`; `endOfLine: "auto"` — иначе конфликт с CRLF рабочих копий на Windows), `.editorconfig`, `.prettierignore`, скрипт `"format": "prettier --write ."`.
  - `eslint.config.js`: подключены `eslint-plugin-prettier`/`eslint-config-prettier`; правило `prettier/prettier` оставлено `'off'` — см. discrepancy ниже.
  - Husky + lint-staged: `.husky/pre-commit` → `npx lint-staged`; на staged `*.{js,jsx}` — `eslint --fix` + `prettier --write`, на staged `*.{json,css,md}` — `prettier --write`.
  - Точечно исправлен реальный неиспользуемый параметр (`SubscriptionManager`: `user` → `user: _user`). Три похожих предупреждения (`Icon`/`Art`/`PersonaArt`) не тронуты — ложное срабатывание `no-unused-vars` для параметров, используемых только как имя JSX-компонента; чинится только через `eslint-plugin-react`, который не входит в согласованный список зависимостей для этой задачи.
  - **Discrepancy, зафиксирован явно:** пробный `prettier --write .` по всему репозиторию дал diff ~16 000 строк — в коде намеренно используется многострочный стиль (по одному идентификатору деструктуризации на строку), который Prettier не воспроизводит и всегда схлопывает в одну строку. Разовое форматирование всего дерева не выполнялось — решение оставлено владельцу; сейчас `prettier/prettier` выключен в ESLint, а `format`/`lint-staged` форматируют только то, что реально трогают (staged-файлы), не весь репозиторий разом.
  - `npx eslint .` — 0 ошибок, 34 предупреждения (тот же базовый уровень, что и до задачи). `npm run build` проходит.
  - Во время работы обнаружен незакоммиченный WIP другой параллельной сессии прямо в этой рабочей копии (объединение «Точки внимания» с героем «Сегодня» + правки `AGENTS.md`/`AI_RULES.md`/`docs/**`); он не тронут и сохранён отдельным `git stash` на `main` («other agent WIP found on main — today focus hero + docs (preserved, not mine)»), не входит в эту задачу и не в PR.
  - **Закрыто:** PR #32 смёржен squash-коммитом `e9dbf03` в `main` по прямому запросу владельца 09.08.2026; ветка `codex/frontend-lint-prettier-husky` удалена (remote и локально). После мёржа повторно проверены `npx eslint .` (0/34, без изменений) и `npm run build` на `main` — оба проходят.

- [x] **MXL-FOCUS-001 — «Разгрузить голову»: выбор одного дела из нескольких конкурирующих**
  - Базовый флоу смёржен в `main` (PR #29, squash `ca7d09e`).
  - Реализация причины «Перегрузка» из таблицы `ne-mogu-nachat.md` (`MXL-PRB-002`), в рамках риск-флага, зафиксированного обновлением `MXL-DEC-006` от 09.08.2026 (разработка разрешена до статуса «проверено»).
  - Точка входа на экране «Сегодня» — компактная пилюля `TodayFocusCard`; тап открывает `TodayFocusFlow` (полноэкранный флоу `useFullscreenSurface`): ввод списка дел одним текстом (по одному на строку) → тап на пункт списка выбирает точку внимания дня.
  - Выбранное дело показывается в `TodayFocusCard` тем же визуальным приёмом, что и приоритетный пункт в `MorningPilotCard` (золотая рамка), остальные дела сворачиваются под «Остальное (N)», доступна ссылка «выбрать другое».
  - `MorningPilotCard` сворачивается в уже существующий у него compact-вид «Утро учтено» (тот же, что при исходах «Не успеваю»/«Нет сил») при наличии выбранной точки дня — новый визуальный паттерн не создавался, переиспользован существующий.
  - Хранение — `localStorage`, per-day (`localDayId` из `src/lib/morningPilot.js`, тот же механизм авто-сброса на новый день), ключ `mx-today-focus-v1:<userId>`, retain 14 дней; backend и API не менялись.
  - **Цикл 0A** (по аудиту Кодекса, PR #30, ветка `feature/today-focus-cycle-0a`): font-size поля ввода `15px → 16px` (iOS Safari не зумит при фокусе, сверено с `CheckIn.jsx`); закрытие клавиатуры через `blur` + сброс скролла шага `pick` в `top: 0` при переходе `input → pick`, синхронизировано через `useEffect` от `step`, без document-level touch handlers и произвольных таймеров; `clearTodayFocusPick` теперь сохраняет `items` и чистит только `picked`/`picked_at` — добавлено действие «убрать фокус» в `TodayFocusCard` (презентационный, хранилище не импортирует напрямую), `MorningPilotCard` автоматически разворачивается обратно; увеличена невидимая hit-area у «Изменить список»/«выбрать другое»/«убрать фокус» без изменения визуальной композиции. Строка «ТЕСТ — лишнее обновление страницы» — не фронтовый текст, приходит в `next.title` из имени тестовой записи ритуала/аскезы в БД; фронтовый фильтр сознательно не добавлялся, требуется удаление записи через интерфейс или проверка бэкенда.
  - **Цикл 1** (по аудиту Кодекса, PR #31, ветка `feature/today-focus-firststep`): после выбора дела — необязательный шаг `'firstStep'` («Как начнёшь? Первый шаг, который займёт не больше пяти минут» — тон `ne-mogu-nachat.md`) с редактируемым полем; вторичная кнопка «Подсказать шаг» вызывает `api.mentalix.send(userId, prompt, 'kompas')` — тот же путь, что диалоги с AI-персонами, без нового эндпоинта — и подставляет ответ в то же поле, ничего не сохраняется без явного подтверждения. «Сохранить шаг» пишет `firstStep` новой `saveTodayFocusFirstStep` (тот же patch-паттерн, что `clearTodayFocusPick` из 0A). При ошибке AI или закрытии экрана посреди запроса (`mountedRef`-guard от setState после размонтирования) `picked` не теряется — он уже сохранён на шаге `pick`, флоу не блокируется. Обмен с AI осознанно оседает в постоянной истории чата с Наставником (согласовано ранее). `TodayFocusCard`: при наличии `firstStep` он становится основным текстом карточки («Первый шаг · до 5 минут»), `picked` — вторичным контекстом более мелким шрифтом; без `firstStep` карточка не меняется.
  - `npx eslint` (0 ошибок, 1 предупреждение `react-hooks/set-state-in-effect` — уже существующий в `MorningPilotCard.jsx`/`Today.jsx` паттерн, не новое) и `npm run build` проходят.
  - Живая проверка Циклов 0A и 1 пройдена и подтверждена владельцем 09.08.2026 на реальном iPhone в Telegram: ввод списка, выбор/снятие фокуса, сворачивание `MorningPilotCard`, переход `input → pick` после клавиатуры, AI-подсказка первого шага и её отображение в карточке — все пункты сработали корректно, багов не найдено.
  - **Цикл 0B** (объединение «Точки внимания» с героем «Сегодня», план согласован с владельцем 09.08.2026, PR #33, ветка `feature/today-focus-cycle-0b`): выбранный фокус (`todayFocus.picked`) теперь заменяет обычный ritual/asceza-hero (`NextActionReveal`/isEmpty/all-done в `Today.jsx`) новым `FocusNextAction` (`TodayMotionExperiment.jsx`) — но только когда `todayState === 'dayInProgress'` и включён production-вариант (`motionExperimentEnabled`, т.е. не dev-only `?today_compare=1&today_variant=before`); чек-ин, вечерний разбор и закрытый день хиро не трогает. Фокус перекрывает и `isEmpty` (пустой каталог ритуалов), и «все практики закрыты» — он не привязан к статусу ритуалов. До `firstStep` — компактный «Точка внимания» с `picked` и CTA «Определить первый шаг» (открывает `TodayFocusFlow` сразу на шаге `firstStep`, минуя повторный выбор); после — «Один следующий шаг» с `firstStep` как главным текстом и `picked` контекстом мельче под ним. «Остальное» в hero не переносится — доступно только через «изменить» (открывает флоу на шаге `pick` с полным списком). Пока фокус показан в hero, отдельный блок `TodayFocusCard` над hero скрыт (не дублируется); в `checkinPending`/`reviewPending` он остаётся как раньше, в `dayClosed` — новый `readOnly`-режим `TodayFocusCard` (проп `readOnly`, скрывает «изменить»/«убрать», просто показывает, что было в фокусе).
  - Исправлен баг из аудита: `TodayFocusFlow` получил проп `initialPicked` — вход в шаг `firstStep` напрямую из hero (минуя `pick`) раньше оставлял локальный `picked` равным `null`, из-за чего AI-промпт «Подсказать шаг» подставлял `null` вместо реального дела.
  - `MorningPilotCard`: из compact-состояния «Утро учтено» убрана строка «Фокус дня уже выбран» — она дублировала то, что теперь крупно показано в самом hero прямо под этой карточкой; осталось только «Утро учтено» + нейтральная фраза про ритуалы в «Практики».
  - Backend, API, `todayFocus.js` (хранение) не менялись.
  - `npx eslint` (0 ошибок, 3 предупреждения `react-hooks/set-state-in-effect` — уже существующие в `MorningPilotCard.jsx`/`Today.jsx`, не новые) и `npm run build` проходят.
  - **Закрыто:** PR #33 смёржен squash-коммитом `77a7e59` в `main` по прямому запросу владельца 09.08.2026; ветка `feature/today-focus-cycle-0b` удалена (remote и локально). Локальный `main` синхронизирован с `origin/main` (`git reset --hard origin/main`) — предыдущий несквошированный локальный коммит `36c5570` заменён итоговым сквош-коммитом истории.
  - **Промежуточная проверка Claude Code 09.08.2026 (браузер, не Telegram/iPhone):** `npm run dev` (мобильный вьюпорт 390×844) и отдельно production-сборка (`npm run build` + `npm run preview`, тестовый веб-аккаунт) на реальном backend. Подтверждено на production-сборке: компакт-«Точка внимания» до `firstStep`; прямой вход в `firstStep` из hero через «Определить первый шаг» (минуя повторный выбор); AI-подсказка получает правильное выбранное дело (баг `initialPicked` действительно не воспроизводится); «изменить» открывает полный список; «убрать» возвращает обычный герой; фокус перекрывает `isEmpty` (пустой каталог ритуалов); `readOnly`-карточка в закрытом дне показывает первый шаг и дело без «изменить»/«убрать»; в `checkinPending`/`reviewPending` герой не подменяется, карточка остаётся как раньше. Не проверено в браузере: «все практики закрыты + выбранный фокус» (логика идентична `isEmpty`-ветке, отдельно не гонялось), дедуп текста в `MorningPilotCard` (не попал в утреннее окно), вьюпорт 320×568.
  - **Побочная находка (не баг Цикла 0B, не чинилось):** в dev-режиме (`npm run dev`) кнопка «Подсказать шаг» виснет в состоянии загрузки, хотя запрос к бэкенду реально проходит (`200 OK`) — причина в React StrictMode (включён только в dev), который дважды прогоняет монтирование и обнуляет `mountedRef` в `TodayFocusFlow.jsx` (паттерн из Цикла 1, не новый код). В production-сборке эффекта нет, подтверждено дважды. К сведению на будущее, если кто-то столкнётся с тем же в dev.
  - Живая проверка в реальном Telegram на iPhone начата 09.08.2026, приостановлена на экране «Как начнёшь?» перед проверкой «Подсказать шаг», продолжена и завершена 10.08.2026.
  - **Закрыто (полная верификация 10.08.2026, владелец, реальный iPhone/Telegram):** подтверждены все оставшиеся пункты — «Подсказать шаг» ссылается на выбранное дело и не виснет; «сохранить шаг» и вид hero корректны; «изменить» открывает полный список; «убрать» возвращает обычный герой; `isEmpty`/все практики закрыты + выбранный фокус — поведение как раньше; `readOnly`-показ в закрытом дне подтверждён; дедуп текста в `MorningPilotCard` не воспроизводится; вьюпорт 320×568 — корректно. Багов не найдено. Задача полностью закрыта по правилу `AI_RULES.md` §6.1; открытых пунктов проверки по MXL-FOCUS-001 не осталось.

- [x] **MXL-CARD-SYSTEM-001 — Выпустить выбранную систему карточек на реальные экраны**
  - Пять направлений Card Lab перенесены в существующие экраны: каталог «Практики», «Сегодня», детали ритуалов/аскез, дыхание, фокус, нейротренажёр и карусель Наставника.
  - Новый общий `CardSystemGlyph` использует спокойные циклы `8.8–13.5s`, одну золотую точку и `prefers-reduced-motion`; новый вариант дыхания не затрагивает функциональную фазовую анимацию практики `4-7-8`.
  - Backend, API, данные, тексты, таймеры, навигация и карточки статей не менялись. Новая система включена по умолчанию; `?card_system=0` сохранён как быстрый визуальный откат к прежнему варианту.
  - Локально проверены реальные авторизованные экраны «Сегодня», «Практики», дыхание, фокус и Наставник; сборка и точечный ESLint проходят.
  - Владелец проверил Preview и отдельно разрешил залить вариант в приложение; PR №27 разрешено перевести из Draft и слить в `main`.

- [x] **MXL-CARD-LAB-001 — Сравнить пять направлений карточек Mentalix в отдельном Preview**
  - Добавлен лабораторный маршрут `?card_lab=1` с пятью переключаемыми композициями: «Тихая сетка», «Смысловая мозаика», «Карточка → экран», «Живая линза» и «История слоями».
  - Реальные экраны не менялись. Маршрут доступен только локально и в Vercel Preview; production-сборка исключает код и стили лаборатории.
  - Сохранён одобренный знак аскезы из Motion Kit. Дыхание получило новый спокойный знак с симметричными дугами и одной золотой точкой вместо прежней апертуры.
  - Проверены переключение вариантов, раскрытие/сворачивание карточки, выбор роли, остановка движения, мобильная ширина `390 × 844`, отсутствие горизонтального переполнения и ошибок консоли.
  - Владелец утвердил распределение направлений по реальным экранам; перенос выполнен в задаче `MXL-CARD-SYSTEM-001`, отдельное подтверждение выпуска в `main` получено после нового Preview.

- [x] **MXL-MOTION-KIT-001 — Выбрать анимации практик в тестовом Motion Kit**
  - Добавлен dev-only маршрут `?motion_kit=1` с 17 медленными смысловыми анимациями для практик, переходов, утренних ритуалов и аскез.
  - Реальные экраны не менялись; маршрут включается только в development и Vercel Preview, а Vercel Production не содержит код и стили Motion Kit.
  - Проверены выбор карточки, общий стоп/старт, `prefers-reduced-motion`, ESLint, production-сборка и отсутствие лабораторных маркеров в `dist`.
  - По прямому решению владельца подготовлен второй цикл семи вариантов: `asceza-boundary`, `neuro-synapse`, `breath-aperture`, `focus-convergence`, `meditation-contours`, `path-corridor`, `energy-field`. «Начать практику» оставлена без изменений как эталон.
  - Владелец визуально утвердил второй цикл и отдельно разрешил merge PR #23 в `main`. Перенос выбранных карточек из Motion Kit в реальные production-экраны остаётся отдельной будущей задачей.

- [x] **MXL-VERCEL-REVIEW-001 — Обработать согласованный пакет визуального фидбека Vercel Preview**
  - Источник: Preview ветки `codex/mentalix-ui-motion`; комментарии привязаны к старому deployment и автоматически в новый Preview не перенесутся.
  - Все комментарии Vercel оставить открытыми до проверки исправлений; `Resolve` нажимать только после визуального подтверждения владельца.
  - **Цикл 1 — онбординг** (один пользовательский сценарий):
    - [x] Первый экран: замедлить движение орбит и точек примерно вдвое, убрать рывки и резкий перезапуск цикла, использовать мягкое ускорение и замедление. Остальной интерфейс не менять. Реализовано локально 09.08.2026: путь один раз дорисовывается за `1600ms`, точка мягко проявляется после основной части пути, весь вступительный блок появляется за `520ms`; бесконечной декоративной анимации нет. [Комментарий Vercel](https://vercel.com/smiraandre2-8311s-projects/mentalix/c/i55ai9jEMxie?s=1)
    - [x] Первый экран: перенести лабиринт выше заголовка и увеличить примерно на 40% относительно текущего размера. Реализовано локально 09.08.2026: размер адаптивный до `266 × 266` CSS px, отдельная верхняя зона занимает до `38dvh`; на `390 × 844` и `320 × 568` обрезания и переполнения нет. [Комментарий Vercel](https://vercel.com/smiraandre2-8311s-projects/mentalix/c/C_BfPC829m3-?s=1)
  - Цикл 1 реализован и проверен локально; до перехода к циклу 2 требуется визуальное подтверждение владельца и позднее проверка на реальном iPhone внутри Telegram.
  - **Цикл 2 — экран «Сегодня»** (один пользовательский сценарий):
    - [x] В раскрытой панели «Нить дня» уменьшить именно высоту примерно вдвое, сохранив ширину, четыре этапа, подписи и открытие/сворачивание. Прямую шкалу заменить тонкой плавной асимметричной волной; пройденная часть — золотая, будущая — приглушённо-серая. Реализовано локально: компактная панель сохраняет четыре смысловых узла и раскрывается из узкого триггера слева от приветствия. [Комментарий Vercel](https://vercel.com/smiraandre2-8311s-projects/mentalix/c/3RJD95htlPP3?s=1)
    - [x] Удалить подпись «день закрывают, а не бросают» под приветствием без замены; после удаления аккуратно сократить расстояние до календарной недели. Кнопку «Нить», приветствие, настройки и календарь не менять. Реализовано локально: удалена вся динамическая подпись времени суток, остальные элементы шапки сохранены. [Комментарий Vercel](https://vercel.com/smiraandre2-8311s-projects/mentalix/c/BWvHtz8tViYU?s=1)
    - [x] Карточке «Тема недели» назначить рамку, идентичную раскрытой панели «Нить дня»: тот же фактический токен цвета, прозрачность, толщина и радиус. Фон, размер, текст и внутренние отступы не менять; золотую обводку не добавлять. Реализовано локально через общий фактический токен `--c-border`, толщину `1px` и радиус `24px`; содержимое карточки не менялось. [Комментарий 1](https://vercel.com/smiraandre2-8311s-projects/mentalix/c/qzi5xFJ8uEov?s=1), [уточнённая метка карточки](https://vercel.com/smiraandre2-8311s-projects/mentalix/c/Ae6uF1jaPyKa?s=1)
  - **Цикл 3 — карточки «Наставника»** (единая композиция трёх персон):
    - [x] Увеличить смысловую анимацию так, чтобы верхняя визуальная зона занимала примерно `40%` высоты карточки, а оставшиеся `60%` были отданы названию, описанию и действию. Одинаковую пропорцию применить к Собеседнику, Наставнику и Следопыту, не меняя внешний размер карточек и горизонтальную карусель. Локально уже используется адаптивная зона `basis-[42%]`; требуется визуальная проверка в новом Preview. [Комментарий Vercel](https://vercel.com/smiraandre2-8311s-projects/mentalix/c/GmI9mzMxbWwj?s=1)
    - [x] Растянуть сам смысловой рисунок по доступной верхней зоне и не помещать его в отдельную внутреннюю рамку: у карточки должна остаться только одна внешняя оболочка, без эффекта «рамка в рамке». Локальный `SemanticGlyph` уже заполняет верхнюю зону и не имеет собственной рамки; требуется визуальная проверка в новом Preview. [Комментарий Vercel](https://vercel.com/smiraandre2-8311s-projects/mentalix/c/wWeH7gcoCtWz?s=1)
  - Все три визуальных цикла реализованы по прямому разрешению владельца 09.08.2026, опубликованы в commit `0d420fc`, draft PR #20 и новом Vercel Preview. Backend и новую продуктовую логику в пакет не включать.
  - Проверка каждого цикла: `npm run lint`, `npm run build`, `git diff --check`, мобильный viewport, новый Vercel Preview; финально — реальный iPhone внутри Telegram.
  - Commit, push и draft PR прямо разрешены владельцем и выполнены.
  - **Устаревшая запись, исправлено 11.08.2026:** этот же блок ранее утверждал «задачу закрывать только после проверки владельцем... merge — только после отдельного прямого разрешения» — на деле draft PR #20 был переведён из draft и смёржен в `main` напрямую (без ветки-форка, коммит `b26816d`) ещё **09.08.2026**, самим владельцем через GitHub, в тот же день, что и создание PR. Расхождение обнаружено и зафиксировано по правилу «не выбирать молча при расхождении документа с кодом» (`AI_RULES.md`); статус ниже актуализирован по результату отдельной живой проверки, а не по этой (более неактуальной) заметке.
  - **Живая проверка владельцем 11.08.2026 (реальный iPhone, Menu Button настоящего Telegram-бота):** циклы 2 и 3 подтверждены без замечаний — панель «Нить дня», карточка «Тема недели», три карточки Наставника соответствуют спецификации. По циклу 1 единственное замечание: анимация дорисовки пути лабиринта субъективно ощущается слишком быстрой (при спецификации и факте по коду `~1600ms`).
  - **Правка по замечанию цикла 1:** `motionDuration` дорисовки пути в `src/screens/Onboarding.jsx` увеличен с `1600` до `2400` мс (середина согласованного диапазона `2200–2500`мс). Остальные тайминги не менялись: появление точки «ты здесь» триггерится от `motionDuration * 0.68` в `MazeLogo.jsx` — это намеренная связь («точка проявляется после основной части пути»), а не побочный эффект, и она смещается вместе с длительностью пути естественным образом; сама длительность фейда точки жёстко закэплена `Math.min(520, motionDuration)`; появление всего вступительного блока за `520ms` — отдельная CSS-анимация в `Onboarding.css` (`.mx-onboarding-intro.mx-onboarding-step`), с `motionDuration` не связана вообще. `npx eslint src/screens/Onboarding.jsx` и полный `npm run lint` — 0 ошибок, 31 предупреждение (прежний базовый уровень, новых нет); `npm run build` проходит.
  - **Закрыто:** PR #38 смёржен squash-коммитом `4d4912e` в `main` по прямому запросу владельца 11.08.2026; ветка `fix/onboarding-maze-timing` удалена (remote и локально). Циклы 1–3 приняты владельцем по живой проверке на реальном iPhone/Telegram, точечная правка тайминга цикла 1 в `main` — задача полностью закрыта.

- [ ] **MXL-REMINDERS-002 — Спроектировать напоминания по модели будильника**
  - Запрос владельца: пользователь сам выбирает время, после чего напоминание повторяется автоматически и может быть изменено или выключено. [Комментарий Vercel](https://vercel.com/smiraandre2-8311s-projects/mentalix/c/i7floY1m1Vqn?s=1)
  - Текущее подтверждённое состояние frontend по документации: доступны фиксированные часы напоминания; end-to-end поведение приватного backend в этой сессии не проверялось.
  - Предлагаемый UX для отдельного согласования: выбор часов и минут как в будильнике, режим повторения, состояние «включено», изменение/выключение и явный часовой пояс.
  - Frontend может отрисовать настройку и запросить разрешение Telegram на сообщения, но отправка при закрытом Mini App требует хранения расписания и фонового запуска в приватном backend/bot.
  - **Блокер до реализации:** владелец должен выбрать повторение по умолчанию — ежедневно или только в выбранные дни; Claude Code должен проверить фактический API-контракт в приватном backend. Ничего не выдумывать и не менять backend из этого репозитория.
  - **Решено 14.08.2026 (`MXL-DEC-012`):** повторение по умолчанию — ежедневное, как в стандартном будильнике, с возможностью пользователю изменить дни или выключить. Блокер по выбору режима повторения снят.
  - Задача остаётся открытой для реализации: фактический API-контракт приватного backend по-прежнему не проверен — это делается перед началом работы, в код пока не входить.
  - Не включать эту функцию в визуальный пакет `MXL-VERCEL-REVIEW-001`.

- [x] **MXL-UI-LAB-001 — Завершить смысловую геометрию SemanticGlyph**
  - По визуальному фидбеку переработаны `neuro`, общий `asceza`, `mentor` и `pathfinder`: сеть стала центральным соединением, аскеза — удержанным выбором, Наставник — калибровкой направления, Следопыт — последовательностью дискретных следов.
  - UI Lab и реальные карточки используют один `SemanticGlyph`; отдельные копии SVG не создавались.
  - В вариант `21` UI Lab добавлена контрольная карточка общей аскезы; варианты `22` и `23` показывают обновлённые нейротренажёр, Наставника и Следопыта.
  - `prefers-reduced-motion` сохраняет статичное итоговое состояние без пространственного движения.
  - `npx eslint src/components/SemanticGlyph.jsx src/components/ui-lab/UiExperiments.jsx` и `npm run build` проходят; статический монтаж до/после проверен.
  - Пользователь согласовал перенос: `SemanticGlyph` подключён к реальным карточкам, а утверждённый Today motion-вариант включён по умолчанию; `?today_compare=1` оставлен для dev-сравнения.
  - Production-сборка не содержит маркеров `UiExperiments`/`ui_lab`; ESLint, `npm run build` и `git diff --check` проходят.
  - **Закрыто 11.08.2026 (живая проверка владельцем):**
    - `?ui_lab=1` (варианты `21–23`, через dev-сервер, Safari на iPhone): аскезы — «Отказ от алкоголя»/«Отказ от курения»/контрольная «Осознанный отказ»; практики `neuro`/`breath`/`focus`/`meditation`; роли `mentor`/`companion`/`pathfinder` — все соответствуют описанию.
    - Реальные карточки в проде (`mentalix.vercel.app` через Telegram): экран «Аскезы» — карточка `kind="asceza"`, анимация и знак отображаются корректно; экран «Нейротренажёр» (`BrainTrainer`, список) — карточка `kind="neuro"`, анимация нейронов с золотой точкой на месте.
    - Экран «Сегодня» и карточки Наставника (`mentor`/`companion`/`pathfinder`) — покрыты ранее закрытой `MXL-VERCEL-REVIEW-001` (циклы 2–3), повторно не перепроверялись.
    - Все четыре переработанных `kind` (`asceza`, `neuro`, `mentor`/`companion`/`pathfinder`) подтверждены живьём владельцем. Код в рамках этой проверки не менялся.
    - Пре-мортем ниже (направление вариантов `05–18`, объединение `SemanticGlyph`/`CardSystemGlyph`) этим пунктом не закрывается — его открытые вопросы уже вынесены в отдельную задачу `MXL-GLYPH-UNIFY-001` (см. ниже по файлу), которая остаётся открытой самостоятельно.
  - 10.08.2026: направление смешанного языка (геометрический гид как неподвижная база + органическая форма поверх неё при переходах, референсы `05/09/12/14/16/18`) зафиксировано владельцем как `MXL-DEC-009` в `docs/core/PRODUCT_DECISIONS.md`; общее motion-правило перехода состояний (`420–520ms`, допустимые `transform`/`opacity`/`stroke`) добавлено в `MENTALIX_SEMANTIC_MOTION.md` §4.1. Изменение документационное, код не трогали. Открыто: читаемость на 320×568 и стоимость переноса органических вариантов в прод ещё не проверены.
  - **Pre-mortem 10.08.2026 — тигры до выбора финального направления:**
    - Сначала устранить конкуренцию источников истины: production по умолчанию уже использует и `SemanticGlyph`, и отдельный `CardSystemGlyph`, хотя `MENTALIX_SEMANTIC_MOTION.md` требует одного реестра. Составить карту потребителей и решить, какой компонент остаётся каноническим; φ не вводить как третью систему.
    - Не выбирать золотое сечение по обещанию «объективно красивой» пропорции. Зафиксировать допустимую роль φ как скрытой конструкционной сетки; видимая спираль проходит дальше только если без объяснения передаёт конкретный глагол функции лучше текущей геометрии.
    - До решения провести слепое сравнение трёх равных вертикальных срезов — текущая космическая грамматика, φ и гибрид — на трёх разных глаголах (`сузить`, `раскрыть`, `услышать`). Критерий: минимум 4 из 5 участников без подписей называют целевой глагол или близкий синоним; объяснение про Fibonacci/1.618 до ответа запрещено.
    - Обеспечить честную мобильную проверку: `?ui_lab=1` сейчас доступен только в `import.meta.env.DEV`, поэтому обычный Vercel Preview и реальный Telegram/iPhone его не показывают. Подготовить preview-only сравнение либо эквивалентный интерактивный артефакт и проверить все три направления на реальном iPhone до выбора.
    - Заранее задать бюджет выразительности для ежедневного интерфейса: не более одной активной смысловой анимации в первом viewport и одной золотой смысловой точки на иллюстрацию; статичный вариант обязан сохранять смысл при `prefers-reduced-motion`. На iPhone проверить плавность, отвлечение в 30-секундном сценарии и отсутствие ожидания окончания анимации.
    - Ограничить объём решения одним hero и тремя репрезентативными знаками; не начинать массовую миграцию карточек до прохождения слепого теста. Неудачный вариант должен удаляться без изменения данных, API, навигации и продуктовых сценариев.

- [x] **MXL-GLYPH-UNIFY-001 — Объединить SemanticGlyph и CardSystemGlyph в единый реестр глифов**
  - **Статус: полностью закрыто 13.08.2026 — Цикл 3 подтверждён владельцем на iPhone/Telegram и смёржен через PR #50.**
  - Владелец решил 10.08.2026 (`MXL-DEC-009`, обновление): правило §4.1 (`MENTALIX_SEMANTIC_MOTION.md`) распространяется на оба глиф-компонента. `SemanticGlyph.jsx`/`.css` и `CardSystemGlyph.jsx`/`.css` независимо дублируют одну архитектуру («гид-крестовина + kind-based SVG», собственный `Guide()` и собственный namespace классов в каждом) и одновременно используются на одних экранах (`Ascezas`, `Breathing`, `Focus`, `BrainTrainer`, `Practices`, `Today`) — два конкурирующих источника истины вместо одного, вопреки `MENTALIX_SEMANTIC_MOTION.md` §5.
  - Оценка стоимости переноса (см. отчёт в этом чате 10.08.2026): 9 production-экранов используют `SemanticGlyph` (прямо или через `practice-art/*`/`PersonaArt.jsx`), ни один пока не нарушает §4.1 (у обоих компонентов сейчас только ambient-циклы, тайминга `420–520ms` нет ни в одном); ни один из вариантов UI Lab `05/09/12/14/16/18` не готов к переносу как есть — все module-private внутри `UiExperiments.jsx`, без использования `SemanticGlyph`.
  - **Цикл 1 — разморозка предпосылки (без кода):** картирование всех потребителей `SemanticGlyph` и `CardSystemGlyph` (какой экран что и зачем рендерит), план объединения в один реестр — какой компонент/API становится каноническим, как мигрируют существующие `kind`, что происходит с дублирующимися `Guide()`/CSS-namespace. Результат — согласованный с владельцем план, без изменения кода.
    - **Выполнено 12.08.2026:** картирование — [`docs/glyph-unify-mapping.md`](docs/glyph-unify-mapping.md); pre-mortem по риску «застряло на полдороге» — [`docs/glyph-unify-premortem.md`](docs/glyph-unify-premortem.md). Ключевая находка картирования: реальных потребителей **11**, а не 6 названных изначально (дополнительно `Rituals.jsx`, `PersonaPicker.jsx`, `PersonaArt.jsx`/`JournalStart.jsx`, `ArticleCover.jsx`, обёртки `practice-art/*`).
  - **Предусловия до старта Цикла 2 (из pre-mortem 12.08.2026):**
    1. Жёсткий чекпоинт между Циклом 2 и Циклом 3 — Цикл 3 не начинается без письменного подтверждения по каждому пилотному варианту: направление подтверждено блайнд-тестом (или явно отложено), все реальные потребители (11, не 6) проверены на mobile + light/dark + Telegram, объём оставшейся работы оценён в часах.
    2. Правило «один экран — один статус за коммит»: каждый коммит переносит ровно один экран целиком (включая обёртки типа `practice-art/*`), без промежуточных состояний между коммитами (часть карточек экрана — на новом реестре, часть — на старом).
    3. Явные dev-ошибки вместо silent fallback на дефолтный `kind`: в едином реестре при `import.meta.env.DEV` неизвестный `kind` бросает ошибку/`console.error` с именем экрана-источника, а не молча рендерит generic-заглушку (как сейчас `Today.jsx` с несуществующим `kind="path-corridor"`).
  - **Архитектурные решения (владелец, 12.08.2026):**
    1. `SemanticGlyph.jsx` — канонический реестр глифов. `CardSystemGlyph.jsx` поглощается: его 5 `kind` (`asceza-boundary`, `neuro-synapse`, `breath-flow`, `focus-convergence`, `meditation-contours`) переносятся в `SemanticGlyph.jsx` как геометрия соответствующих уже существующих там `kind`; `CardSystemGlyph.jsx`/`.css` и `cardSystemPreviewEnabled` по итогу удаляются, не остаются параллельным путём.
    2. `MotionKitGlyph` (dev-only, `PracticeMotionKit.jsx`) в объединение **не включается** — остаётся отдельным dev-инструментом, вне периметра этой задачи; собственная будущая задача, не Цикл 2/3.
  - **Цикл 2 — пилот (план согласован 12.08.2026, см. [`docs/glyph-unify-plan.md`](docs/glyph-unify-plan.md)):** сначала инфраструктура — dev-ошибка на неизвестный `kind` (предусловие 3) и фикс `Today.jsx`/`kind="path-corridor"` (тот же визуал, явное имя вместо случайного `default`); затем перенос двух `kind` — `asceza-boundary` → `SemanticGlyph kind="asceza"` (`Ascezas.jsx` + карточка «Аскезы» в `Practices.jsx`) и `breath-flow` → `kind="breath"` (`Breathing.jsx` + карточка «Дыхание»). Один экран — один коммит (предусловие 2). Живая проверка на реальном iPhone в Telegram (`AI_RULES.md` §5). Не начинать до отдельного разрешения владельца приступить к реализации.
  - **Цикл 2 выполнен и закрыт 13.08.2026 — владелец дал разрешение 13.08.2026, реализация в ветке `feature/glyph-unify-cycle2`, строго по плану, три коммита:**
    1. Инфраструктура — `SemanticGlyph.jsx`: неизвестный `kind` теперь `console.error` в `import.meta.env.DEV` (опциональный проп `debugSource`, не ретрофичен на существующие вызовы), плюс новый явный `kind="next-step"` — перенесённая geometрия безымянного `default` (`path-wall`) из `CardSystemGlyph.jsx`, тот же визуал. `Today.jsx` переведён с `CardSystemGlyph kind="path-corridor"` на `SemanticGlyph kind="next-step"`. Commit `3d7eb00`.
    2. Перенос `asceza-boundary` → `kind="asceza"`: geometрия/CSS/анимация перенесены в namespace `mx-semantic-glyph__`, старая geometрия `asceza` (`choice-boundary`/`choice-path`/`temptation-path`, keyframe `mx-semantic-decline`) удалена — нулевые потребители, проверено `grep` перед удалением. `Ascezas.jsx` (карточка + пустое состояние) и `Practices.jsx` (только карточка «Аскезы») переведены на безусловный `SemanticGlyph`, импорт `CardSystemGlyph` убран из `Ascezas.jsx`. Commit `6e18221`.
    3. Перенос `breath-flow` → `kind="breath"`: аналогично, старая geometрия `breath` (`breath-left`/`breath-right`/`axis`, keyframes `mx-semantic-breathe-left/right`) удалена — нулевые потребители (кроме несвязанного `mx-lab-system-card__` в `UiExperiments.css`, вне периметра задачи). `Breathing.jsx` (оба места — intro и done) и `Practices.jsx` (только карточка «Дыхание») переведены на безусловный `SemanticGlyph`, импорт `CardSystemGlyph` убран из `Breathing.jsx`. Commit `a950cab`.
    - После каждого коммита: `npm run build` чисто, `npx eslint` по изменённым файлам чисто, полный `npm run lint` — 0 ошибок, 31 предупреждение (прежний базовый уровень, новых нет).
    - `CardSystemGlyph.jsx`/`.css` не тронуты ни одним из трёх коммитов (проверено `git diff` по обоим файлам между `main` и веткой — пустой) — удаление только в Цикле 3, как и решено.
    - **Живая проверка на iPhone подтверждена владельцем 13.08.2026 для обоих kind** (`asceza-boundary`/`6e18221` и `breath-flow`/`a950cab`) — Цикл 2 закрыт.
    - PR в `main`: см. ниже.
    - **Важно: это закрывает только Цикл 2, не чекпоинт перед Циклом 3.** Предусловие 1 из pre-mortem (все реальные потребители — 11, не 6 — проверены на mobile + light/dark + Telegram, объём оставшейся работы оценён в часах) не выполнялось в рамках этого пилота — проверены вживую только конкретные экраны двух перенесённых `kind` (`Ascezas.jsx`, `Practices.jsx` карточки «Аскезы»/«Дыхание», `Breathing.jsx`, `Today.jsx`), не все 11 потребителей реестра. Цикл 3 не начинать без отдельного прохождения этого чекпоинта.
  - **Чекпоинт перед Циклом 3 закрыт 13.08.2026 по явному решению владельца:** направление подтверждено; доступные реальные поверхности проверены в web fallback на `320×568` без горизонтального переполнения; прежнее требование light/dark снято как неприменимое к dark-only продукту; остаток оценён в **8–12 часов**. `Focus`, `BrainTrainer`, `Breathing` и `Meditations` закрыты в production пометкой «Скоро», поэтому для затронутых скрытых экранов обязательна отдельная preview-проверка до merge Цикла 3. Полный протокол и ограничения — [`docs/glyph-unify-checkpoint.md`](docs/glyph-unify-checkpoint.md).
  - **Цикл 3 — перенос остальных:** `focus-convergence`, `neuro-synapse`, `meditation-contours` (`Focus.jsx`, карточки «Фокус»/«Нейротренажёр»/«Медитация» в `Practices.jsx`), уборка всех оставшихся зависимостей от `cardSystemPreviewEnabled`, удаление `CardSystemGlyph.jsx`/`.css`/`cardSystem.js`. Hero-глиф `BrainTrainer` не переносится и не возвращается: он удалён отдельным решением владельца; остаётся только унификация пустой обёртки.
  - **Цикл 3 реализован 13.08.2026 — поэкранно, без изменения продуктовой логики:**
    1. `focus-convergence`, `neuro-synapse`, `meditation-contours` перенесены в `SemanticGlyph kind="focus"/"neuro"/"meditation"` вместе с прежней геометрией, токенами и reduced-motion поведением. Commit `eff930d`.
    2. `Focus.jsx` переведён на безусловный `SemanticGlyph`. Commit `f7fdb41`.
    3. `Practices.jsx` и его `PracticeCard` переведены на безусловный production-layout и `NeuroArt`/`FocusArt`/`MeditationArt`; gating «Скоро» сохранён. Commit `3b18f9b`.
    4. Остаточные CSS-тернарники убраны отдельными экранными коммитами: `Ascezas` (`3426c64`), `Rituals` (`d7d3bb1`), `Breathing` (`964006f`), `BrainTrainer` (`69075db`), `PersonaPicker` (`9a03d1a`), `Today` (`9962791`). Везде закреплена уже включённая по умолчанию Card System-композиция; удалённый hero BrainTrainer не возвращался.
    5. `CardSystemGlyph.jsx`/`.css` и `src/lib/cardSystem.js` удалены после проверки нулевого числа production-потребителей. `src/components/CardSystem.css` сохранён: это действующие layout-классы карточек, а не второй glyph-реестр. `?card_system=0` больше ничего не переключает. Commit `dad4589`.
    6. Проверки: `npm run build` успешно; `npm run lint` — 0 ошибок, 31 прежнее предупреждение; `rg "CardSystemGlyph|cardSystemPreviewEnabled|systemPreview" src` — 0 совпадений; `git diff --check` чисто. Mobile visual QA в web fallback на `320×568`: каталог, `Focus`, `Breathing`, `BrainTrainer`, `Today`, `PersonaPicker` без горизонтального переполнения; неизвестных `SemanticGlyph kind` нет. Временный dev-доступ к экранам «Скоро» удалён до коммитов.
    7. **Живая проверка закрыта 13.08.2026:** владелец подтвердил на реальном iPhone в Telegram, что доступные пользователю каталог «Практики», Today и PersonaPicker отображаются корректно. Скрытые Focus/Breathing/BrainTrainer были проверены через временный локальный dev-доступ, которого намеренно нет в ветке; production-gating «Скоро» не снимался. PR #50 смёржен в `main` после подтверждения.
  - **Связанные документы:** [`docs/glyph-unify-mapping.md`](docs/glyph-unify-mapping.md), [`docs/glyph-unify-premortem.md`](docs/glyph-unify-premortem.md), [`docs/glyph-unify-plan.md`](docs/glyph-unify-plan.md), [`docs/glyph-unify-checkpoint.md`](docs/glyph-unify-checkpoint.md), [`docs/core/PRODUCT_DECISIONS.md`](docs/core/PRODUCT_DECISIONS.md) (`MXL-DEC-009`), [`MENTALIX_SEMANTIC_MOTION.md`](MENTALIX_SEMANTIC_MOTION.md) §4.1, `MXL-UI-LAB-001` (pre-mortem от 10.08.2026, где риск дублирования впервые зафиксирован).

- [x] **MXL-RSCH-001 — Провести первое проблемное интервью владельца «Не могу начать»**
  - `P01` завершено, согласие получено, результат обезличен.
  - Владелец разрешил персональный пилот без ожидания дополнительной выборки (`MXL-DEC-007`).

- [x] **MXL-PILOT-001 — Пройти семидневный тест утреннего пилота**
  - Реализация завершена и в `main` (см. Completed / Archived).
  - Семидневный личный тест пройден; владелец подтвердил результат полевого теста.
  - Решение: оставить как есть, без изменений пилота (`docs/engineering/MORNING_PILOT.md`, раздел «Проверка»).

- [x] **MXL-PILOT-002 — Проверить вечерний цикл из двух функций**
  - Функция 1: до открытия показать три аналитических вопроса и три пункта гордости.
  - Функция 2: после закрытия дня повторно открывать разбор с сохранёнными ответами.
  - Реализовано, в main (PR #10); mobile/web-проверка и персональный production-тест пройдены.
  - Решение: оставить как есть, без изменений цикла.

- [ ] **MXL-RSCH-002 — Расширить проверку перед обобщением на аудиторию**
  - Не является блокером персонального пилота.
  - Потребуется перед статусом «проверено» и массовыми утверждениями об эффективности.
  - По обновлению `MXL-DEC-007` (09.08.2026) допускается анализ проблем из открытых источников (Reddit, тематические форумы) как альтернатива живым интервью при ограничении по времени у владельца — осознанно более слабая доказательность, статус «проверено» по-прежнему не даёт.
  - Промежуточный материал зафиксирован 09.08.2026 в [`docs/problems/ne-mogu-nachat.md`](docs/problems/ne-mogu-nachat.md#материал-mxl-rsch-002-анализ-открытых-источников-09082026): 4 находки из анализа Reddit/форумов про прокрастинацию и ADHD task-initiation (выполнен в чате Cowork, вне Claude Code). Статус `MXL-PRB-002` и рейтинг в `PROBLEMS.md` не менялись — критерий (пять проблемных интервью) не закрыт.
  - Обновление `MXL-DEC-006` (09.08.2026): владелец осознанно разрешил разработку функционала по вертикали `MXL-PRB-002` до статуса «проверено», несмотря на незакрытый критерий (1 живое интервью + 4 находки из открытых источников вместо пяти интервью) — решение с повышенным риском несоответствия функции реальной проблеме аудитории. Ограничено только `MXL-PRB-002`; для остальных 14 проблем карты правило «не разрабатывать функцию до малого теста» действует без изменений.
  - Обновление `MXL-DEC-007` (10.08.2026): метод анализа открытых источников расширен с `MXL-PRB-002` на `MXL-PRB-015`, `MXL-PRB-003`, `MXL-PRB-001`. Промежуточные материалы (по 4 находки каждый, анализ 10.08.2026 в чате Cowork) зафиксированы в [`docs/problems/peregruzka.md`](docs/problems/peregruzka.md), [`docs/problems/brosayu-na-polputi.md`](docs/problems/brosayu-na-polputi.md), [`docs/problems/prokrastinaciya.md`](docs/problems/prokrastinaciya.md); ссылки добавлены в `PROBLEMS.md`. Статус и приоритет всех трёх не менялись — критерий пяти интервью не закрыт. Разработка функционала по ним по-прежнему не начинается (`MXL-DEC-006` ограничен только `MXL-PRB-002`).
  - Второе обновление `MXL-DEC-007` (10.08.2026): для остальных одиннадцати проблем карты (`MXL-PRB-004`–`014`) введён отдельный, более высокий уровень доказательности — научный обзор рецензируемой литературы (PubMed, мета-анализы, систематические обзоры), явно отделённый от анализа открытых источников. Промежуточные материалы (по 2–3 находки с полной атрибуцией автор/год/журнал/DOI на каждую) зафиксированы 10.08.2026 в одиннадцати новых карточках `docs/problems/*.md` (`zavisimost-ot-motivacii.md`, `strah-oshibki.md`, `strah-otsenki.md`, `trevoga.md`, `vygoranie.md`, `samokritika.md`, `perfekcionizm.md`, `zavisimost-ot-chuzhogo-mneniya.md`, `nizkaya-samootsenka.md`, `poterya-napravleniya.md`, `nehvatka-energii.md`); ссылки добавлены в `PROBLEMS.md`. Статус «проверено» не присвоен ни одной из одиннадцати — критерий пяти проблемных интервью не закрыт. Разработка функционала по ним по-прежнему не начинается (`MXL-DEC-006` ограничен только `MXL-PRB-002`).

- [ ] **MXL-UI — Провести системный UI-аудит экран за экраном**
  - **Аудит завершён 13.08.2026:** один статический проход и bounded web-runtime проход на `320 × 568`; отчёт и pre-mortem — [`docs/ui-audit-2026-08-13.md`](docs/ui-audit-2026-08-13.md).
  - P0 не найдено. Открыты пять P1: пересечения мобильной шапки, CTA AI-персоны под bottom nav, обрезание «Нейротренажёр», безымянные toggle в Настройках, обход `useFullscreenSurface` в Conversation/CheckIn.
  - [x] **MXL-UI-001 — устранить пересечения мобильной шапки:** на ширине `<360px` шапка «Сегодня» резервирует фактические `78px` под «Нить дня», а кнопка «Назад» в Настройках компактно показывает только стрелку при сохранённом `aria-label="Назад"`. DOM-измерения на `320 × 568` подтверждают нулевое пересечение обеих пар элементов и отсутствие горизонтального overflow; на `390 × 844` обычная кнопка с подписью сохранена. Живая проверка владельцем в Telegram/iPhone 13.08.2026 пройдена: оба изменения соответствуют ожидаемому результату.
  - [x] **MXL-UI-002 — вернуть CTA AI-персоны выше bottom nav:** на viewport высотой до `650px` сохранена 42%-зона глифа, уплотнены только вертикальные интервалы, а две стартовые фразы уложены в одну двухколоночную строку с минимальной высотой цели `44px`. На `320 × 568` кнопка «Говорить» у всех трёх персон полностью находится внутри карточки и имеет `0px` пересечения с навигацией; на `390 × 844` исходные отступы и flex-композиция сохранены. Живая проверка владельцем в Telegram/iPhone 13.08.2026 пройдена.
  - [x] **MXL-UI-004 — добавить доступную семантику переключателям Настроек:** оба визуально безымянных toggle получили `type="button"`, `role="switch"`, синхронный `aria-checked` и русские доступные имена «Напоминание от бота» / «Блокировка приложения». Визуал и обработчики не менялись; требуется финальная проверка владельцем в Telegram/iPhone перед закрытием цикла.
  - [x] **MXL-UI-003 — показать «Нейротренажёр» полностью в сетке Практик:** до `360px` длинное слово получает единственную смысловую точку переноса, а типографика уплотняется только у этой карточки; на `390px` исходная однострочная композиция сохранена. Временный локальный стенд на фактических production-классах подтвердил для всех шести карточек на `320 × 568` и `390 × 844`: содержимое помещается внутри карточек, горизонтальный overflow карточек и документа равен `0px`. Текст, сетка, размеры, иллюстрации и поведение не менялись; требуется финальная проверка владельцем в Telegram/iPhone перед merge. **Подтверждено 14.08.2026:** владелец проверил на реальном iPhone в Telegram после merge PR #55 (`bb07969`) — заголовок не обрезается, см. `MXL-MERGE-BATCH-001`.
  - Порядок исправлений: шапка → CTA AI → доступность toggle → карточка Практик → fullscreen-консолидация по одному экрану.
  - **Pre-mortem Tigers:** не расширять аудит в редизайн; не считать static-only экраны проверенными живьём; не смешивать продуктовые решения с исправлениями; один связанный симптом — один короткий цикл; Telegram-sensitive изменения не мержить без реального iPhone gate.
  - Задача закрывается после пяти циклов и итоговой живой regression-проверки владельцем; до этого статус остаётся открытым.
- [ ] **MXL-EVENTS — Добавить продуктовые события core loop без записи личных текстов**
- [ ] **MXL-USERS — Провести семидневный тест на 5–10 пользователях**

## Бэклог: AI-экраны

- [x] **MXL-VOICE-001 — Добавить голосовой ввод в AI-диалоги**
  - Frontend записывает до 60 секунд (`MediaRecorder`, авто-стоп по таймауту), расшифровка через `api.mentalix.transcribe`.
  - Расшифрованный текст отправляется в чат автоматически тем же путём, что обычное сообщение (`onSend` → `api.mentalix.send`), без остановки в поле ввода и без нажатия кнопки. Если пользователь уже печатал вручную до записи — голосовое сообщение уходит отдельным сообщением, ручной черновик не трогается.
  - Ошибки расшифровки (код `VOICE_*` / HTTP-статус) отображаются под полем ввода, ничего не отправляется при сбое.
  - Backend использует существующий GigaChat API; аудиофайл удаляется после распознавания и не сохраняется в Mentalix.
  - Единая кнопка микрофон/отправка (press-and-hold запись) и coach mark при первом открытии диалога — `main` (PR #24, `53b0917`).
  - Микро-анимации: смена иконки — fade+scale ~200ms, тактильный scale 0.97–0.985 на удержании кнопки, ответ наставника — slide-up+fade 360ms; `prefers-reduced-motion` сводит все три к простому fade — `main` (PR #25, `a658358`).
  - Автоотправка — `main` (PR #22, `0ad7cfa`).
  - Подтверждено на реальном iPhone в Telegram: press-and-hold запись, разрешение микрофона, автоотправка и все три анимации работают корректно на прод-домене.

- [ ] **MXL-001 — Довести три AI-экрана по референсам**
  - Частично: существуют Собеседник, Наставник и Следопыт.
  - По runtime-решению Следопыт открывается сразу в диалоге без отдельной иллюстрированной стартовой карточки.
  - Композиция сообщений Собеседника выровнена; требуется проверка на реальном iPhone.
  - Осталось: сверить композицию, размеры, вертикальные интервалы и состояния по референсам.

- [ ] **MXL-002 — Проверить размеры и safe area AI-экранов на iPhone**
  - Глобальный Telegram Fullscreen Mode и safe areas реализованы.
  - Осталось проверить каждый AI-экран на реальном iPhone после свежего деплоя.
  - Контент не должен попадать под status bar и плавающие кнопки Telegram.

- [ ] **MXL-005 — Довести диалог до журнального формата**
  - Частично: базовый журнальный формат реализован.
  - Осталось: длинные ответы, разделители, даты, интервалы, история и автопрокрутка.
  - Проверить повторное открытие диалога и восстановление истории.

- [ ] **MXL-006 — Зафиксировать финальную типографику AI-экранов**
  - Утвердить размеры заголовков, основного текста, подписей и поля ввода.
  - Проверить переносы длинного текста на небольшом iPhone.

## Следующий этап

- [ ] **MXL-007 — Пройти основной UX-сценарий экрана «Сегодня»**
  - Утренний чек-ин.
  - Следующий рекомендуемый шаг.
  - Переход в ритуал или аскезу.
  - Переход к AI.
  - Вечерний анализ.

- [ ] **MXL-008 — Доработать раздел «Путь»**
  - Уточнить назначение и основной сценарий.
  - Проверить прогресс, состояния и переходы.

- [ ] **MXL-009 — Доработать аналитику и визуальные закономерности**
  - Emotion check-in уже реализован и передаётся через API.
  - В Трендах уже есть блок «Частые чувства».
  - Проверить существующие проценты, корреляции и инсайты.
  - Определить, какие закономерности действительно полезны пользователю.
  - Проверить корректность агрегации и отображения эмоций на реальных данных.

## Проверка перед первым публичным MVP

- [ ] **MXL-010 — Пройти полный end-to-end цикл**
  - [ ] чек-ин;
  - [ ] следующий шаг;
  - [ ] ритуал или аскеза;
  - [ ] диалог с AI;
  - [ ] вечерний анализ;
  - [ ] переход к Следопыту;
  - [ ] переход на следующий день;
  - [ ] сохранение и повторное открытие данных.

- [ ] **MXL-011 — Финальный тест на реальном iPhone**
  - [ ] Telegram fullscreen;
  - [ ] верхняя и нижняя safe area;
  - [ ] клавиатура;
  - [ ] прокрутка;
  - [ ] navbar;
  - [ ] длинные AI-ответы;
  - [ ] переходы между экранами.

- [ ] **MXL-012 — Контрольная проверка Android**
  - Fullscreen должен включаться при поддержке.
  - При отсутствии fullscreen приложение остаётся в expanded-режиме.

## Позже

- [ ] **MXL-013 — Подключить frontend статей к существующему backend API**
- [ ] **MXL-014 — Определить backend и сценарий для Медитаций**
- [ ] **MXL-015 — Определить таксономию категорий тем недели**
- [ ] **MXL-016 — Сформировать банк «Мыслей дня»**
- [x] **MXL-017 — Разбить крупный frontend bundle на динамические чанки**
  - Выполнено 14.08.2026 в отдельной ветке `codex/split-frontend-bundle`, commit `079a82d`, draft PR #57.
  - Через `React.lazy`/`Suspense` отложена загрузка шести не стартовых экранов: `Practices`, `Analytics`, `Mentalix`, `Profile`, `Settings`, `Library`. Стартовые `Today`, `WebAuthScreen`, `Onboarding` и `AppLock` оставлены синхронными, чтобы не ухудшать первый экран и авторизацию.
  - Стартовый JS-чанк уменьшен с `828,63 kB` (`239,12 kB` gzip) до `334,74 kB` (`101,16 kB` gzip): на `493,89 kB` (`59,6%`) без gzip и на `137,96 kB` (`57,7%`) с gzip. Самый крупный новый чанк — `Analytics`, `389,21 kB`; предупреждение Vite о чанке больше `500 kB` исчезло.
  - Проверено: `npm run lint` — 0 ошибок, 31 существующее предупреждение; `npm run build` — успешно; `git diff --check` — успешно. Web fallback на `320 × 568` и `390 × 844` показывает форму входа без горизонтального переполнения и зависшего fallback; сообщения SDK о недоступном CloudStorage в web fallback находятся вне затронутого кода. API, данные, маршрутизация, Telegram-platform и зависимости не менялись.
  - Перед merge нужен короткий проход авторизованных переходов по вкладкам в Vercel Preview: локально без отправки email OTP доступны только сборка и стартовый web fallback.
  - **Закрыто:** PR #57 смёржен squash-коммитом `3e9bb14` в `main` по прямому запросу владельца 14.08.2026. **Подтверждено 14.08.2026:** живая проверка владельцем на реальном iPhone в Telegram — все шесть отложенных экранов открываются без зависаний и пустых экранов. Открытых пунктов проверки по MXL-017 не осталось, см. `MXL-MERGE-BATCH-001`.
- [x] **MXL-018 — Удалить подтверждённый неиспользуемый код**
  - Удалён `src/components/Icons.jsx`: точный поиск по всему отслеживаемому дереву не нашёл ни одного импорта или потребителя; production-поведение и визуал не менялись. `npm run lint`, `npm run build` и `git diff --check` проходят; публикация ведётся отдельным draft PR.
  - **Закрыто:** PR #56 смёржен squash-коммитом `4c060f9` в `main` по прямому запросу владельца 14.08.2026. Изменение чисто технического удаления мёртвого кода, живой проверки на устройстве не требовало; открытых пунктов по MXL-018 не осталось, см. `MXL-MERGE-BATCH-001`.

- [ ] **MXL-ORPHAN-RITUALS-001 — Почистить осиротевшие ритуалы и рассмотреть FK-ограничение**
  - Найдено при проверке `MXL-DEFAULT-CONTENT-001` 13.08.2026: два ритуала (`id 15` «Физическая культура», `id 16` «Молитва»), `created_at 2026-08-11 20:42`, висят на `user_id=8348817724` (`andre_smi3`) — остатки более раннего ручного REST-теста сидирования из коммита `e41f71e` («POST /api/rituals x3 ... фейковый user_id ... DELETE x6»); судя по всему, «фейковый» id тогда случайно совпал с реальным Telegram-id тестового аккаунта, и delete подчистил не все записи (`ascezas` подчищены полностью, 2 из ритуалов — нет).
  - [x] **Почищено 14.08.2026:** подключение к прод-БД тем же способом, что и при диагностике `MXL-DEFAULT-CONTENT-001` (Railway-проект `supportive-curiosity`, сервис `Postgres`, публичный proxy `DATABASE_PUBLIC_URL` через `psql`). `SELECT * FROM rituals WHERE id IN (15, 16);` показан владельцу до удаления — подтвердил ровно ожидаемые две строки (пустые `goal`/`min_version`/`optimal_version`/`skip_consequence`, `created_at 2026-08-11 20:42`). Первая попытка `DELETE FROM rituals WHERE id IN (15, 16);` упала на FK `ritual_logs_ritual_id_fkey` — обнаружены 2 незадокументированные зависимые строки в `ritual_logs` (`id 61 → ritual_id 15`, `id 62 → ritual_id 16`, обе `date 2026-08-13`, `level='optimal'`, остатки того же ручного теста); транзакция откатилась полностью, `rituals` не пострадала. По решению владельца выполнено в одной транзакции: `DELETE FROM ritual_logs WHERE id IN (61, 62); DELETE FROM rituals WHERE id IN (15, 16);` — `COMMIT` подтверждён. Повторный `SELECT` по обеим таблицам — 0 строк.
  - **Открыто:** рассмотреть добавление FK-ограничения `rituals.user_id → users.id` (сейчас в схеме `rituals` есть только PK и индекс по `user_id`, внешнего ключа на `users` нет — поэтому осиротевшая запись с несуществующим на тот момент `user_id` вообще смогла появиться молча, без ошибки на уровне БД). По прямому запросу владельца не трогать сейчас — отдельное решение, задача остаётся открытой только из-за этого пункта.

- [ ] **MXL-STRICTMODE-PERSONA-001 — `readPendingMentor()` теряет persona/draft в dev под React StrictMode (не чинить, зафиксировано)**
  - Найдено при проверке `MXL-EMOTION-STEP-002` 13.08.2026. `readPendingMentor()` (`src/screens/mentalix/personas.js`) вызывает `sessionStorage.removeItem(...)` как побочный эффект прямо внутри функции, переданной в `useState(() => readPendingMentor())` (`src/screens/Mentalix.jsx`). В dev (`npm run dev`) `main.jsx` оборачивает приложение в `<StrictMode>`, который намеренно вызывает инициализатор `useState` дважды — второй вызов находит sessionStorage уже пустым (после первого) и возвращает `{persona: null}`, из-за чего вместо прямого перехода в диалог показывается `PersonaPicker`.
  - Влияет одинаково на оба handoff-пути, использующих этот паттерн (`openScout()`/Следопыт из вечернего разбора и новый `openListener()`/Собеседник из `MXL-EMOTION-STEP-002`) — не специфично для новой задачи.
  - **Не воспроизводится в проде** — `StrictMode` не активен вне dev-сборки; проверено вручную через `npm run build` + `npm run preview`: handoff отрабатывает штатно, персона открывается сразу с предзаполненным драфтом.
  - Не исправлять сейчас (по прямому решению владельца) — только зафиксировано как ловушка для будущих dev/Playwright-проверок этого конкретного флоу: если тест на dev-сервере вдруг видит `PersonaPicker` вместо ожидаемой персоны, это не обязательно баг, а известная особенность StrictMode. Фикс при желании: убрать side-effect из тела инициализатора `useState`, перенести `sessionStorage.removeItem` в `useEffect`.

## Заблокировано

- [ ] **MXL-019 — Заменить лабиринт**
  - Блокер: нужен финальный SVG или PNG.
  - Фигура должна оставаться одной непрерывной линией для заполнения прогрессом.

- [ ] **MXL-020 — Оплата курсов**
  - Блокер: требуется окончательное решение по провайдеру и схеме оплаты.
  - Цифровой контент внутри Telegram требует учёта правил Telegram Stars.

## Требует продуктового решения

- [ ] **MXL-021 — Зафиксировать палитру в `PRODUCT.md`**
  - Решить: текущий абсолютный чёрный или прежняя более тёплая тёмная палитра.
  - **Решение принято 14.08.2026 (`MXL-DEC-010`):** перейти с абсолютного чёрного `#000000` на тёплый тёмный фон — снижение усталости глаз и более спокойное ощущение вечером. Статус: решение принято, нужны конкретные токены — конкретные hex-значения ещё не выбраны, код/токены не менялись.
  - **Варианты на выбор владельцу** (сверены с текущими токенами `DESIGN_SYSTEM.md` §2: `--c-bg: #000000`, `--c-card: #090909`, `--c-card2: #111111`, `--c-gold: #EDBD60`, `--c-text: #F3F3F3`):
    - **Вариант 1 — «Тёплый уголь» (минимальный сдвиг):** `#050403` (rgb 5 4 3). Едва заметно теплее чистого чёрного, гарантированно темнее текущей карточки `#090909` — иерархия слоёв «фон → карточка» не потребует правки. Эффект «меньше усталости» будет минимальным.
    - **Вариант 2 — «Тёплая ночь» (заметный, но сдержанный сдвиг):** `#0A0806` (rgb 10 8 6). Тёплый подтон уже читается невооружённым глазом; по светлоте почти сравнивается с текущим `--c-card` (`#090909`) — при выборе этого варианта карточки (`--c-card`/`--c-card2`) тоже придётся немного поднять, чтобы сохранить видимое разделение слоёв.
    - **Вариант 3 — «Тёплый эспрессо» (выраженный тёплый фон):** `#120F0B` (rgb 18 15 11). Самый «вечерний»/уютный вариант. Светлее текущего `--c-card`, поэтому весь стек тёмных токенов (`--c-card`, `--c-card2`, возможно `--c-border`) потребует согласованного пересчёта — это уже отдельная, более крупная визуальная задача, а не замена одного токена.
    - Во всех трёх вариантах контраст с `--c-text: #F3F3F3` и золотым акцентом `--c-gold: #EDBD60` остаётся практически неизменным (фон по-прежнему околочёрный) — риска нечитаемости текста/CTA нет ни в одном варианте.
  - Ничего из перечисленного не внедрено в код; выбор конкретного hex и (при вариантах 2/3) согласованный пересчёт `--c-card`/`--c-card2` — отдельный шаг после решения владельца.
  - **Выбрано 14.08.2026 (`MXL-DEC-010`):** Вариант 1 — `#050403`. Статус: выбран конкретный токен, требуется визуальная проверка перед внедрением в прод.
  - Реализована временная dev/Preview-only проверка (не production, не смёржено в `main`): флаг `?bg_preview=1` в `src/main.jsx` (доступен так же, как `?card_lab=1`/`?motion_kit=1` — `import.meta.env.DEV || VERCEL_ENV === 'preview'`), подменяет `--c-bg` на `5 4 3` поверх обычного `<App />`, то есть виден на реальных экранах, не в изолированной лаборатории. `src/index.css`/`DESIGN_SYSTEM.md` не менялись — production-значение `--c-bg` остаётся `#000000` до отдельного мержа. Локальный `npm run lint` (0 ошибок, 31 предупреждение — прежний базовый уровень) и `npm run build` проходят; в собранном production-бандле (`VERCEL_ENV` пуст) `bg_preview` отсутствует — подтверждено grep по `dist/assets/*.js`, dead-code elimination сработал.
  - Ветка `feature/mxl-021-warm-bg-preview`, draft PR [#58](https://github.com/Smira31/Mentalix/pull/58). Vercel Preview: `https://mentalix-git-feature-mxl-021-e4b680-smiraandre2-8311s-projects.vercel.app/?bg_preview=1` (для проверки без нового фона — тот же адрес без параметра). Preview защищён Vercel SSO — открывается только под вашим логином.
  - **Не мержить в `main` без отдельного подтверждения владельца после живого просмотра на реальном устройстве.**

- [ ] **MXL-022 — Решить архитектуру публикации статей**
  - Вариант 1: статьи загружаются через backend API.
  - Вариант 2: публикация статьи выполняется commit в репозиторий.
  - **Решение принято 14.08.2026 (`MXL-DEC-011`):** Вариант 1 — backend API, не git-commit. Причина: владелец планирует публиковать статьи часто и самостоятельно, без участия разработчика на каждую статью. Задача разблокирована для проектирования/реализации; сама реализация в эту правку не входит.
  - **Что потребуется дальше** (не реализовано, только зафиксировано как следующий шаг):
    - админ-эндпоинт создания статьи в `mentalix-bot` (backend);
    - форма создания/редактирования статьи на фронте;
    - доступ к форме/эндпоинту ограничен только владельцем.

- [x] **MXL-ASCEZA-CARD-001 — Убрать категории из карточки создания новой аскезы**
  - Владельцу не нравилось наличие категорий при создании новой аскезы — предложение убрать категории из формы создания.
  - **Решение принято и реализовано 11.08.2026 («безопасный компромисс»):** секция выбора категории (кнопки-иконки `CATEGORIES`) убрана из UI формы создания (`src/screens/Ascezas.jsx`, `CreateAscezaScreen`). Поле `category` в `draft`/state оставлено со значением по умолчанию `'psycho'` и продолжает отправляться на backend без изменений — пользователь его больше не видит и не выбирает, ничего не сломано на стороне API. `CATEGORIES`/`categoryMeta` не удалены — всё ещё используются для отображения категории на карточках уже существующих аскез (`AscezaCard`, `Ascezas.jsx:275,383`).
  - Пятым полем добавлено `relapse_cost` («Цена срыва») после `replacement`, тот же `inputCls`/layout, что у остальных четырёх полей.
  - **Backend-контракт проверен до правки кода, не вслепую:** прямым POST на прод (`https://mentalix-bot-production.up.railway.app/api/ascezas`) с фейковым `user_id=-999999001` подтверждено, что `relapse_cost` принимался (200), но молча терялся — ни в ответе на создание, ни в `GET`-списке поля не было; тестовая запись сразу удалена, повторным `GET` подтверждена очистка.
  - **Правка backend в отдельном репозитории `mentalix-bot`** (не в этом репозитории): найдено, что обе локальные копии-кандидаты (`work/backend-original-audit`, `work/backend-prod-sync`) — статичные снимки без `.git`, обе с устаревшим `ascezas.py`; локальный git-клон `mentalix-bot-git-sync` разошёлся с `origin/main` (3 своих коммита, 40 позади) и завален незакоммиченными правками — не использовался. Работа велась в свежем клоне `origin/main`. Добавлено: `models.py` — `Asceza.relapse_cost = Column(Text, nullable=True)` (тот же тип/nullable, что у `reason`/`trigger`/`replacement`); `main.py` — idempotent `ALTER TABLE ascezas ADD COLUMN IF NOT EXISTS relapse_cost TEXT` в существующий startup-блок self-миграций (Alembic в репозитории не используется); `ascezas.py` — поле в `CreateAscezaPayload`, передача в конструктор `Asceza`, включено в `_serialize()`. Документации по аналогии с `AI_RULES.md` в `mentalix-bot` нет — в репозитории нет ни одного `.md`-файла, отдельный журнал там не ведётся.
  - **Backend закрыт:** PR [`Smira31/mentalix-bot#1`](https://github.com/Smira31/mentalix-bot/pull/1) смёржен squash-коммитом `0afc3f0` в `main` по прямому запросу владельца 11.08.2026; ветка `feature/asceza-relapse-cost` удалена (remote и локально). Деплой на Railway (`mentalix-bot-production`) подтверждён (commit status `success`, явный контекст `mentalix-bot-production.up.railway.app`). Повторный живой curl-тест после деплоя: `relapse_cost` теперь возвращается и при создании, и в `GET`-списке; тестовая запись создана и сразу удалена, повторным `GET` подтверждена очистка.
  - Frontend-часть (см. ниже) реализована и проверена (lint/build), backend-контракт подтверждён рабочим на проде — блокер снят.
  - **Побочный фикс 11.08.2026:** тот же баг с пустыми зонами, что в `MXL-RITUAL-CARD-001` — дословно скопированный `min-h-full justify-center` в `src/screens/Ascezas.jsx:510` (форма создания аскезы). Починено тем же способом, в одном PR с ритуалами.
  - **Связана с `MXL-RITUAL-CARD-001`:** карточки создания ритуала и создания аскезы должны в итоге стать идентичны по структуре и содержанию (единый компонент/паттерн, а не два разных интерфейса) — учитывать при проектировании решения по обеим задачам.
  - **Закрыто (только этот layout-фикс):** PR #41 смёржен squash-коммитом `fd73ad3` в `main` по прямому запросу владельца 11.08.2026; ветка `fix/ritual-asceza-form-centering` удалена (remote и локально). Сама задача остаётся открытой — решение по содержанию не принято.
  - **Побочный эффект визуальной доработки 11.08.2026 (см. `MXL-RITUAL-CARD-001`):** счётчик серии в карточке аскезы (`StreakBar`, `Ascezas.jsx:321`) — общий компонент с ритуалами; правка «число дней подряд → римские цифры» применилась и здесь автоматически, отдельно не трогалось. **Закрыто:** PR #42 смёржен squash-коммитом `0096c10` в `main` 11.08.2026; ветка `feature/ritual-card-visual-polish` удалена.
  - **Закрыто полностью (frontend + backend):** PR #44 смёржен squash-коммитом `fb19809` в `main` по прямому запросу владельца 11.08.2026; ветка `feature/asceza-drop-categories-add-relapse-cost` удалена (remote и локально). Деплой на `mentalix.vercel.app` подтверждён (GitHub commit status `Vercel: success`; в отданном продакшен-бандле найден новый плейсхолдер «Цена срыва», класс сетки категорий `grid-cols-5 gap-1.5` отсутствует). Backend (`mentalix-bot#1`) смёржен и задеплоен на Railway ранее, живой curl-тест подтвердил сохранение `relapse_cost`. Задача `MXL-ASCEZA-CARD-001` полностью закрыта.

## Completed / Archived

- [x] **MXL-RITUAL-CARD-001 — Пересмотреть карточку создания нового ритуала (закрыто 13.08.2026)**
  - Владельцу не нравится текущий функционал экрана/карточки создания нового ритуала, в частности поле описания — нужно продумать новую формулировку и структуру поля. Решение по содержанию/полям не принято, требует отдельного обсуждения — эта часть задачи остаётся открытой.
  - **Побочный фикс 11.08.2026 (по итогам диагностики, отдельно от решения по содержанию):** владелец прислал скриншот с реального iPhone — большие пустые зоны сверху/снизу формы. Разобрано и подтверждено визуальной диагностикой (временная ветка `debug/ritual-card-ghost-button`, удалена после подтверждения): причина — `min-h-full justify-center` на обёртке формы (`src/screens/Rituals.jsx:252`) центрировал невысокий контент (5 инпутов) по вертикали внутри всей доступной высоты fullscreen-экрана. Убран `min-h-full justify-center`, оставлен `w-full max-w-md mx-auto px-5 pb-8 flex flex-col` — форма встаёт сразу под шапкой. `npx eslint`/полный `npm run lint` — 0 ошибок, 31 предупреждение (прежний уровень); `npm run build` проходит.
  - **Второй симптом закрыт 11.08.2026 (дублирующийся/полупрозрачный текст «Создать ритуал» над заголовком):** причина подтверждена — iOS auto-zoom Safari/Telegram WebView при фокусе на поле с `font-size < 16px` (см. `MXL-IOS-AUTOZOOM-001`, PR #46, смёржен `8bf0191`). После правки шрифтов владелец подтвердил на реальном устройстве: зум и утечка нижней панели ушли.
  - Диагностика (временная ветка через PR #45, `start_param=ritualghost`, гейт `isRitualGhostDebugEnabled`/`DebugLabel`/`RitualDebugOverlay` в `src/screens/Rituals.jsx`, экспорт `TG_CONTROLS_HEIGHT`/`viewportHeight` в `src/lib/fullscreenSurface.js`) удалена полностью — оба файла сверены построчно с состоянием до PR #45, `fullscreenSurface.js` побайтово совпадает. `npx eslint`/полный `npm run lint` — 0 ошибок, 31 предупреждение (прежний уровень); `npm run build` проходит.
  - **Закрыто:** PR #47 смёржен squash-коммитом `b3f0a9c` в `main` по прямому запросу владельца 11.08.2026; ветка `chore/remove-ritual-ghost-debug` удалена (remote и локально). Деплой на `mentalix.vercel.app` подтверждён (`Vercel: success`; строка `ritualghost` в продакшен-бандле отсутствует — диагностика реально убрана с прода). `MXL-RITUAL-CARD-001` (второй симптом) полностью закрыта.
  - **Связана с `MXL-ASCEZA-CARD-001`:** карточки создания ритуала и создания аскезы должны в итоге стать идентичны по структуре и содержанию (единый компонент/паттерн, а не два разных интерфейса) — учитывать при проектировании решения по обеим задачам.
  - **Повторное подтверждение ghost-text 13.08.2026 (второй симптом).** Диагностика по запросу владельца: гипотеза про `visualViewport.resize`/зазор в `FULLSCREEN_SHELL_CLASS` без `bottom-0` рассматривалась как возможная альтернативная/дополнительная причина; временная ветка `debug/viewport-resize-log` (console.log в `useFullscreenSurface` + eruda через CDN за `?viewport_debug=1`, без новых npm-зависимостей) задеплоена на отдельный Vercel preview для живой проверки, **не мержилась в `main`**. Владелец проверил вживую на реальном iPhone 13.08.2026: **баг не воспроизводится**, подтверждая закрытие из записи выше (11.08.2026, `MXL-IOS-AUTOZOOM-001`). Ветка `debug/viewport-resize-log` удалена (remote и локально) сразу после проверки, `main` не затронут. _Примечание: запись ниже от 11.08.2026 (визуальная доработка, п. 5) на тот момент ошибочно называла этот симптом нерешённым — расхождение снято этой записью, не правкой старой._
  - **Закрыто (только этот layout-фикс):** PR #41 смёржен squash-коммитом `fd73ad3` в `main` по прямому запросу владельца 11.08.2026; ветка `fix/ritual-asceza-form-centering` удалена (remote и локально). Сама задача остаётся открытой — решение по содержанию не принято.
  - **Визуальная доработка карточки — закрыто 11.08.2026** (`src/screens/Rituals.jsx`), по прямому запросу владельца:
    1. Карточка ритуала в горизонтальной ленте растянута по реальной оставшейся высоте: убран хрупкий расчёт `CARD_HEIGHT` через `calc(100dvh - ... - 290px)` с магической константой (тот же класс проблемы, что и в форме выше) — вместо него настоящий flex: корневой `<div>` экрана стал `flex-1 flex flex-col`, шапка и подзаголовок — `shrink-0`, горизонтальный трек карточек — `flex-1 min-h-0`, а сама карточка и кнопка «+ Новый ритуал» получили `h-full` (класс `CARD_HEIGHT_CLASS`). Прежние `min-h-[380px]`/`max-h-[560px]` сохранены как защитные пороги (нижний — от сжатия в тесноте, верхний — от чрезмерного растяжения на широком экране Telegram Desktop, как было прежде).
    2. Золотая иконка-монограмма «M» вверху карточки убрана (компонент `Monogram` и его вызов удалены целиком, больше нигде не использовался).
    3. Кнопка «Удалить» в карточке была `bg-red-900/60` — не токен проекта. Заменена на `bg-cream/15` — тот же нейтральный акцент, что уже используется в самой карточке для активного состояния кнопки «Минимум»; без красного/деструктивного оттенка.
    4. Счётчик серии — общий компонент `src/components/StreakBar.jsx`, используется и в ритуалах, и в аскезах (`Ascezas.jsx:321`). Число дней подряд заменено на римские цифры (добавлен `toRoman()`, стандартная вычитающая нотация, проверена на 1–2026); слово «день/дня/дней» и вся остальная логика не менялись. Правка в одном общем месте — применилась к обоим экранам автоматически.
    5. Экран создания ритуала (`CreateRitualScreen`) — растянуть заголовок/поля через `justify-center`/`min-h-full` означало бы откатить только что смёрженный фикс `MXL-RITUAL-CARD-001`/PR #41 (та же математика, тот же баг с пустыми зонами вернулся бы). Кнопка «Создать ритуал» физически вне этого блока (нативная `MainButton` или `WebActionBar`), поэтому «дотянуться» до неё flex-выравниванием контента нельзя. Растолкованное как «форма не должна выглядеть сжатой»: увеличены внутренние отступы — `mb-5`→`mb-8` и `pt-2`→`pt-4` у шапки, `space-y-2`→`space-y-4` между полями. Сама компоновка (`flex flex-col`, без `justify-center`) не менялась.
    - `npx eslint src/screens/Rituals.jsx src/components/StreakBar.jsx` — чисто; полный `npm run lint` — 0 ошибок, 31 предупреждение (прежний уровень); `npm run build` проходит.
    - **Закрыто:** PR #42 смёржен squash-коммитом `0096c10` в `main` по прямому запросу владельца 11.08.2026; ветка `feature/ritual-card-visual-polish` удалена (remote и локально). Деплой на `mentalix.vercel.app` подтверждён (GitHub commit status `Vercel: success`, в отданном бандле найден маркер новой вёрстки карточки `min-h-[380px] max-h-[560px]`).
    - Все пять пунктов визуальной доработки закрыты. Открытые продуктовые вопросы по `MXL-RITUAL-CARD-001` не затронуты этим закрытием: решение по полю описания карточки, а также второй симптом (дублирующийся текст «Создать ритуал» над заголовком) остаются нерешёнными — см. выше.
  - **Обновлены плейсхолдеры формы создания ритуала — закрыто 11.08.2026** (`src/screens/Rituals.jsx`, `CreateRitualScreen`), по прямому запросу владельца: только текст, поля и `state` keys не менялись.
    - `name`: «Название ритуала» — без изменений.
    - `goal`: «Зачем он нужен» → «Почему это важно для тебя».
    - `min_version`: «Минимум» → «Меньше нельзя».
    - `optimal_version`: «Оптимум» → «На полную».
    - `skip_consequence`: «Что теряется при пропуске» → «Цена пропуска».
    - `npx eslint src/screens/Rituals.jsx` — чисто; полный `npm run lint` — 0 ошибок, 31 предупреждение (прежний уровень); `npm run build` проходит.
    - Это частичный шаг к открытому вопросу «формулировка и структура поля описания» из первой строки задачи — сама структура формы (пять однострочных `input`, без категории/иконки) не менялась, решён только текст. Задача `MXL-RITUAL-CARD-001` в целом остаётся открытой.
    - **Закрыто:** PR #43 смёржен squash-коммитом `5f3dce7` в `main` по прямому запросу владельца 11.08.2026; ветка `feature/ritual-form-placeholders` удалена (remote и локально). Деплой на `mentalix.vercel.app` подтверждён (GitHub commit status `Vercel: success`; все четыре новых текста плейсхолдеров найдены в отданном продакшен-бандле, старый текст отсутствует).
  - **Продуктовое решение по содержанию карточки принято 13.08.2026 (владелец) — задача закрыта полностью.** Форма остаётся как есть: 5 полей (`name`/`goal`/`min_version`/`optimal_version`/`skip_consequence`), обязательно только `name`, все поля — однострочные `input`, без `textarea`, без `maxLength`, без счётчика символов. Отдельное поле описания не добавляется. Код не менялся — это чисто продуктовое решение, зафиксированное здесь. Открытый вопрос «формулировка и структура поля описания» из первой строки задачи закрыт: структура остаётся текущей, дальнейшего обсуждения не требует. `MXL-RITUAL-CARD-001` полностью закрыта — оба симптома (пустые зоны формы, ghost-text) и решение по содержанию.

- [x] **MXL-EMOTION-STEP-002 — Эмоция → один микро-шаг (закрыто 13.08.2026)**
  - Источник задачи: `ROADMAP.md` → «Идеи по итогам конкурентного анализа» (Stoic/Reflectly/Daylio/How We Feel), пункт 2.
  - Реализация: `src/screens/CheckIn.jsx` — при выборе одной из трёх тяжёлых эмоций (`тревожно`/`подавлен`/`страшно`, константа `HEAVY_EMOTIONS`) на шаге эмоций (`isEmotionStep`) сразу под чипами появляется ссылка «Поговорить об этом с Собеседником →». Новая функция `openListener()` — тот же handoff-паттерн, что существующий `openScout()` (sessionStorage `MENTOR_PERSONA_KEY`/`MENTOR_DRAFT_KEY` → `tab=mentor`), но на персону `mayak` (Собеседник) с новым универсальным драфтом `EMOTION_TALK_PROMPT` вместо персоны `dnevnik`/`DAY_REVIEW_PROMPT`. `openScout()`/вечерний разбор не изменены — отдельная функция, не параметризация существующей.
  - Текст драфта утверждён владельцем: «Сейчас тяжело — не хочу делать вид, что всё в порядке. Хочу просто сказать вслух, что чувствую.»
  - Commit реализации: `49ab119` (ветка `feature/emotion-microstep`).
  - Проверка: `npm run build` чисто; `npx eslint src/screens/CheckIn.jsx` — 0 ошибок, 0 warning; вживую через Playwright — нейтральная эмоция (ссылки нет) и тяжёлая эмоция (ссылка появляется, клик → `?tab=mentor`); сам handoff (персона + предзаполненный драфт) проверен на production-сборке (`npm run build` + `npm run preview`) — отработал корректно. Известная особенность dev-окружения (не баг) зафиксирована отдельно — `MXL-STRICTMODE-PERSONA-001` в «Позже».

- [x] **MXL-PIN-001 — PIN/биометрия на приложение (закрыто 13.08.2026, живой тест на iPhone подтверждён)**
  - Источник задачи: `ROADMAP.md` → «Идеи по итогам конкурентного анализа» (Stoic/Reflectly/Daylio/How We Feel), пункт 1.
  - Реализация: `src/lib/appLock.js` (PIN — хеш+соль SHA-256, только локальное хранение, не синхронизируется), `src/screens/AppLock.jsx` + `AppLock.css` (один экран на режимы `unlock`/`setup`, fullscreen-портал по контракту `CheckIn`/`QuoteView`), `src/platform/telegram.hooks.js` (обёртка `biometric` над `WebApp.BiometricManager`, Telegram-only, надстройка поверх PIN — не замена), `src/App.jsx` (гейт рендера после web-auth, повторная блокировка на `visibilitychange` при каждом возврате из фона), `src/screens/Settings.jsx` (тумблер «Блокировка приложения» в секции «Основные»). Синхронизируется только флаг «включено» (`mx-app-lock-enabled` через `useSynced`), сам PIN — нет.
  - Commit реализации: `60bd42c`.
  - Проверка до живого теста: `npm run build` чисто; `npm run lint` — 0 ошибок, 31 warning (прежний базовый уровень, новых нет, подтверждено построчно по `eslint --format json`); вживую через Playwright на веб-пути — setup (ввод дважды, несовпадение даёт shake и возврат на первый шаг), холодный старт с блокировкой, неверный/верный PIN, повторная блокировка при эмуляции `visibilitychange`, выключение в Settings.
  - **Живой тест на iPhone в Telegram (13.08.2026) — подтверждено владельцем:** настройка через Настройки → Основные → «Блокировка приложения», ввод PIN дважды, экран блокировки на холодном старте и при возврате из фона, биометрия (Face ID/Touch ID) через `AppLock` — всё работает. Это закрывает единственный пункт, не проверенный Playwright (реальный `BiometricManager`-промпт и safe-area в развёрнутом Telegram-fullscreen).
  - Задача закрыта полностью — и PIN, и биометрия подтверждены живым тестом.

- [x] **MXL-DEFAULT-CONTENT-001 — Дефолтные ритуалы/аскезы для нового пользователя (закрыто 13.08.2026, root cause подтверждён)**
  - Код сидирования (3 ритуала + 3 аскезы в `bot/handlers_user.py:cmd_start`, внутри `if user is None:`) готов, смёржен и задеплоен — `mentalix-bot` PR #2, squash-коммит `e41f71e`, Railway-сервис `accurate-expression`. Доводки код не потребовал.
  - **Не подтверждалось живым тестом (11–12.08).** При попытке проверить на реальном втором Telegram-аккаунте (`andre_smi3`) обнаружен посторонний баг инфраструктуры, не связанный с самим кодом сидирования: `TelegramConflictError`/`409 Conflict` на `getUpdates` — неизвестный процесс где-то перехватывал апдейты бота (включая тестовые `/start`) раньше продового Railway-инстанса. Источник не найден напрямую: на компьютере владельца процесса нет (ни Python, ни Docker, ни WSL); второй Railway-проект (`genuine-rebirth`) проверен — там только голая БД Postgres, второго бота нет; `DATABASE_URL` бота и backend совпадают; роутеры бота (`handlers_admin`/`handlers_user`) подключены корректно, перехватывающих `/start` без фильтра обработчиков нет.
  - **Устранение:** по решению владельца токен `@Mentalix_club_bot` отозван и перевыпущен через `@BotFather`, обновлён в Railway на обоих сервисах (`accurate-expression` и `mentalix-bot`), оба передеплоены. Проверкой через ~2 минуты подтверждено: старый токен отвечает `401 Unauthorized` (revoke применился с задержкой), новый токен на `getUpdates` возвращает `{"ok":true,"result":[]}` без конфликта.
  - **Живой тест 12.08.2026 после устранения конфликта токена — сидирование по-прежнему не сработало.** Владелец отправил `/start` с `andre_smi3` повторно (новый чистый токен, старый подтверждённо мёртв — `401`). Прямой запрос в БД: `SELECT ... FROM users WHERE username = 'andre_smi3' OR id = 8348817724;` → **0 строк**. На тот момент причина отсутствия сидирования оставалась не до конца ясной.
  - **Проверка `getWebhookInfo` (13.08.2026): webhook не установлен.** `curl https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo` с текущим токеном (сверен по обоим Railway-сервисам, `accurate-expression` и `mentalix-bot` — идентичен) → `{"url":"","pending_update_count":0,...}`. `url` пустой — вебхука нет, апдейты никто не перехватывает через webhook-путь, бот действительно работает через polling (`getUpdates`). Гипотеза «перехват через webhook» исключена как отдельная причина.
  - **Живой мониторинг + повторный тест 13.08.2026 — сидирование воспроизведено успешно.** Логи `accurate-expression` опрашивались в реальном времени (`railway logs --service accurate-expression`) на момент отправки `/start` — конфликтов `getUpdates` рядом с обработкой апдейта не зафиксировано. Владелец отправил `/start` с `andre_smi3`; прямой запрос в БД подтвердил результат: `users` — новая строка (`id=8348817724`, `created_at=2026-08-13 05:42:21`); `ascezas` — 3 строки с тем же timestamp (Алкоголь/Никотин/Думскроллинг, `category='psycho'`, `relapse_cost` заполнен); `rituals` — 3 новые строки с тем же timestamp (Холодный душ/Тишина и благодарность утром/Физическая нагрузка).
  - **Root cause подтверждён: конфликт токена `getUpdates` был единственной причиной** отсутствия сидирования 12.08 — после перевыпуска токена функция воспроизводится штатно при живом `/start`. Задача закрыта.
  - Побочная находка при проверке БД, не относится к сегодняшнему сидированию: 2 осиротевших ритуала (`id 15, 16`, дата `2026-08-11`, остатки более раннего ручного REST-теста из того же коммита `e41f71e`) под тем же `user_id` — вынесены отдельной задачей `MXL-ORPHAN-RITUALS-001` (см. «Позже»).

- [x] **Исправить дёрганые переходы между вкладками**
  - Причина: `key={overlay || tab}` на контейнере контента в `App.jsx` заставлял React пересобирать поддерево при каждом переключении вкладки — заново проигрывалась `animate-fade-in` и заново монтировались экраны со своими `useEffect`-fetch без общего кеша данных.
  - Фикс: `key={overlay || 'main'}` — пересборка и повторная анимация/fetch теперь только при открытии/закрытии оверлея (профиль, настройки), не при переключении вкладок.
  - `npm run build` подтверждён; `ARCHITECTURE.md` §8 обновлён под фактическое поведение анимации.

- [x] **MXL-UI-ART-001 — Унифицировать иллюстрации всех карточек**
  - Все 16 общих SVG-мотивов (`src/components/Motif.jsx`) и три карточки AI-персон (`src/screens/mentalix/art/PersonaCardArt.jsx`) переведены на бело-серую техническую линейную графику и не более одной золотой смысловой точки — проверено по каждому файлу против правила `DESIGN_SYSTEM.md` §5, расхождений не найдено.
  - Единый стиль распространяется на ритуалы, аскезы, статьи, достижения, онбординг и пустые состояния через общий компонент, без изменения продуктовой логики.
  - Работа уже в `main` (не в отдельной ветке): commits `af595b3`, `7c324b5`, `b70f8bb`, `4cd3ed5`, `46c4889`, `3fd84a5`, `40ed3f4`, `3dc6d19`.

- [x] **MXL-CORE-001 — Создать Mentalix Core v0.1**
  - Нормативный слой `docs/core/` создан и связан с существующей методологией.
  - Корпус из 14 книг сверён владельцем и оформлен в реестр и методологические разборы v0.1.
  - Карта из 15 проблем объединена в пять кластеров и предварительно приоритизирована.
  - Содержательное review вертикали `MXL-PRB-002 — Не могу начать` завершено; проведено первое проблемное интервью (`MXL-RSCH-001`).
  - Все восемь решений `MXL-DEC-001`–`MXL-DEC-008` в `docs/core/PRODUCT_DECISIONS.md` переведены в статус «принято» — работа по каждому уже построена и используется в `main` (Core-слой, review вертикали «Не могу начать», пилоты `MORNING_PILOT`/`EVENING_REVIEW_PILOT`, правило двух функций в `AI_RULES.md`).
  - Дальнейшая проверка законов-гипотез источниками, экспертной оценкой и пользовательскими тестами продолжается в рамках `MXL-RSCH-002`, а не блокирует статус Core v0.1.
  - Работа уже в `main`: PR #7 «Перестроить методологию вокруг проблем пользователя» (ветка `agent/mentalix-core-v0-1`), смёржен 02.08.2026.

- [x] **MXL-PILOT-001 — Создать персональный утренний пилот**
  - Утром экран «Сегодня» показывает до трёх незавершённых ритуалов до основного сценария и выделяет первый шаг.
  - Сон, усталость и нехватка времени учтены нейтральными исходами без стыда, серии и принуждения.
  - Показ и категория выбора хранятся локально 14 дней без названий ритуалов и личного текста; backend и API не менялись.
  - Карточка остаётся доступной после перехода в ритуалы и возвращения на экран «Сегодня».
  - Карточка гаснет строго в 12:00 по локальному времени независимо от того, принято решение или нет — осознанное продуктовое решение, подтверждено владельцем. Более раннее описание «не исчезает после полудня» относилось к промежуточному поведению до `d1424ac` и было неточным.
  - Работа уже в `main`: PR #8 «Добавить персональный утренний пилот», PR #9 «Не скрывать утренний пилот после перехода», commit `d1424ac` (гашение карточки в 12:00, 07.08.2026).
  - Осталось (см. «Сейчас», тот же ID `MXL-PILOT-001`): проверить деплой в Telegram на реальном iPhone и пройти семидневный личный тест.

- [x] **MXL-033 — Упростить вход в настройки и профиль**
  - Шестерёнка на Today ведёт прямо в настройки.
  - Профиль открывается первой строкой настроек и возвращается системной Telegram Back.
  - Wordmark Mentalix расположен между верхними контролами Telegram и уходит при прокрутке.

- [x] **MXL-032 — Оставить только тёмную тему приложения**
  - Удалены автоматический дневной режим, переключатель и светлые CSS-токены.
  - Telegram chrome всегда синхронизируется с тёмным фоном Mentalix.

- [x] **MXL-003 — Закрепить поле ввода снизу**
  - На уровне кода диалог использует полноэкранную fixed-поверхность,
    отдельный нижний composer, `visualViewport` и safe-area inset.
  - Проверка поведения клавиатуры на реальном iPhone остаётся в MXL-002
    и MXL-011.

- [x] **MXL-004 — Скрывать основной navbar внутри выбранной AI-персоны**
  - На экране выбора персон navbar виден.
  - Внутри выбранной персоны `BottomNavigation` не рендерится.
  - При возврате к выбору персон navbar восстанавливается.

- [x] **MXL-023 — Реализовать Telegram Fullscreen Mode**
  - `ready()`, `expand()`, fullscreen с безопасным fallback.
  - Commit: `2b8afd4`.

- [x] **MXL-024 — Добавить Telegram/iPhone safe areas**
  - Верхняя, нижняя и боковые safe areas.
  - Нижняя навигация учитывает нижний inset.
  - Commits: `a120ede`, `8a700d0`, `e87a571`.

- [x] **MXL-025 — Добавить viewport-fit=cover**
  - Commit: `2b8afd4`.

- [x] **MXL-026 — Сохранить запуск в обычном браузере**
  - Telegram API вызывается через безопасные проверки.
  - Production-сборка и локальный browser smoke test прошли.

- [x] **MXL-027 — Сделать журнальный экран Следопыта**
  - Commit: `3f3054c`.

- [x] **MXL-028 — Сделать стартовые журнальные экраны трёх AI-персон**
  - Commit: `3c73e74`.

- [x] **MXL-029 — Связать вечерний разбор дня со Следопытом**
  - Commit: `e4ebc67`.

- [x] **MXL-030 — Исправить вечерний анализ дня**
  - Исправлено падение на выборе эмоции.
  - Уроки и достижения сохраняются отдельно.
  - Commits: `a65e0e8`, `e2fa357`.

- [x] **MXL-031 — Добавить причину срыва аскезы**
  - Причина передаётся из интерфейса в API.
  - Commits: `2c86062`, `de5bbb3`, `fd6e867`.

## Правило обновления

После каждой рабочей сессии:

1. обновить дату наверху;
2. перенести завершённые задачи в «Выполнено»;
3. добавить номер commit;
4. зафиксировать новые задачи и блокеры;
5. не хранить параллельный статус только в чатах.

## Передача между агентами

Обновлять после каждой значимой сессии.
`AGENTS.md` — постоянные правила проекта.
`TASKS.md` — текущее состояние и передача работы.

**Codex Desktop, 14.08.2026 (MXL-017):** работа из изолированного worktree `mxl-cycle3-current`, ветка `codex/split-frontend-bundle` от `origin/main` `82ffa97`. Шесть не стартовых экранов разделены на динамические чанки; lint/build и мобильный web fallback пройдены. Основная рабочая копия с чужими незакоммиченными изменениями не затрагивалась. Перед merge нужен короткий проход авторизованных вкладок в Vercel Preview; `main` без прямого разрешения не менять.

Рабочая папка Claude Code: `work/frontend-deploy`
Рабочая папка текущей сессии Codex Desktop: отдельный worktree `mentalix-card-production`
Последнее обновление: `2026-08-09`
Последний агент: Codex
Текущая ветка: `codex/card-directions-production`, создана от `origin/main` `a658358`.
Последний подтверждённый base commit: `a658358`; Card Lab перенесён отдельным commit `c35bd3`.
Публикация: владелец проверил Vercel Preview и 09.08.2026 прямо разрешил залить Card System в приложение; PR №27 разрешено слить в `main`.
Незакоммиченные изменения: нет после выпускного commit; перед новой работой обязательно повторно проверить `git status`.
Перед работой из другой копии репозитория обязательно сверить ветку, последний commit и `git status`; основная рабочая копия содержит параллельные изменения других агентов, поэтому текущая UI-работа изолирована в отдельном worktree.

**Codex Desktop, 13.08.2026 (MXL-UI-003):** актуальная рабочая папка этой задачи — отдельный worktree `mxl-cycle3-current`; ветка `codex/mxl-ui-practices-title` создана от `origin/main` `82ffa97`. Scope ограничен `PracticeCard`, карточкой «Нейротренажёр», `CHANGES.md` и `TASKS.md`; временный QA-стенд удалён. `npm run lint` завершён без ошибок (31 существующее предупреждение вне scope), `npm run build` и `git diff --check` проходят. Следующий gate — draft PR/Vercel Preview и живая проверка владельцем в Telegram/iPhone; merge без подтверждения не выполнять. Независимый draft с `MXL-UI-004` остаётся в ветке `codex/mxl-ui-settings-a11y` и в этот diff не включён.

**Claude Code, 09.08.2026 (MXL-LINT-001):** во время работы обнаружено, что основная рабочая копия (`work/frontend-deploy`) была без предупреждения переключена на `main` другим процессом прямо во время сессии (см. `git reflog show HEAD` — checkout `codex/frontend-lint-prettier-husky → main`), с большим незакоммиченным WIP (Today hero + `AGENTS.md`/`AI_RULES.md`/`docs/**`). Этот WIP не мой, не тронут и сохранён отдельным `git stash` **на `main`**: `other agent WIP found on main - today focus hero + docs (preserved, not mine)` — проверить `git stash list` и явно `git stash pop`/`apply`, он не применится сам. PR #32 смёржен squash-коммитом `e9dbf03` в `main` 09.08.2026, ветка `codex/frontend-lint-prettier-husky` удалена. Текущая ветка Claude Code: `main`. Перед следующей работой в этой папке — обязательно `git stash list` и `git status`, не полагаться только на этот файл.

**Claude Code, 09.08.2026 (следующая сессия, MXL-FOCUS-001 Цикл 0B):** тот самый стэш из заметки выше — это WIP по Циклу 0B, не «чужой» (моя же более ранняя сессия). Восстановлен на `main` через `git checkout main` + `git stash pop`; единственный конфликт (в `CHANGES.md`, обе стороны добавляли раздел сразу после заголовка — MXL-LINT-001 и Цикл 0B) разрешён по прямому указанию владельца: сначала запись про линтер (уже была в `main`), ниже — запись про Цикл 0B. Резолюция закоммичена, стэш после успешного коммита удалён (`git stash list` пуст). Незакоммиченных изменений после этого нет — `AGENTS.md`/`AI_RULES.md`/`docs/**`, упомянутые выше как «WIP», были ложным следом (пробный прогон Prettier по всему дереву от MXL-LINT-001, не входит ни в PR #32, ни в Цикл 0B) — см. `git status` перед следующей работой, не полагаться на эту запись как на актуальный статус рабочего дерева.

**Кто что делает**

- Claude (чат/Cowork) — планирование, анализ, сверка Git-статуса и ревью.
- Claude Code — изменение production-кода, Git-операции, сборка, коммиты и PR; строго из `work/frontend-deploy`.
- Codex — дизайн и UI-эксперименты в `?ui_lab=1`; перенос экспериментов в production только после согласования.

**Сделано**

- Выбранная система карточек перенесена в реальные компоненты и включена по умолчанию: каталог практик, «Сегодня», детали ритуалов/аскез, дыхание, фокус, нейротренажёр и AI-персоны. Параметр `?card_system=0` оставлен для быстрого визуального отката.
- Добавлены общий `CardSystemGlyph`, медленные смысловые циклы, размеры каталога `250/158px`, крупная зона деталей около `40%` и композиция персон `42/58`; карточки статей, backend/API и продуктовая логика не менялись.
- Выпуск в production прямо подтверждён владельцем после просмотра Preview; backend/API и продуктовая логика не входят в изменение.
- Создан `?card_lab=1` с пятью направлениями карточек и точными рекомендациями по размерам, вложениям, движению и назначению каждого каркаса.
- Сохранён одобренный рисунок аскезы; дыхание заменено новым спокойным полем из симметричных дуг вокруг одной золотой точки.
- Реализованы переключение направлений, раскрытие карточки, выбор роли и общий стоп/старт движения; реальные экраны и backend не менялись.
- Создан отдельный `?motion_kit=1` с 17 медленными анимированными карточками; новые SVG-прототипы изолированы внутри dev-маршрута, существующие знаки переиспользуют `SemanticGlyph`.
- Реальные экраны и production-поведение не менялись; маркеры Motion Kit отсутствуют в production-сборке.
- Локально выполнен цикл 1 `MXL-VERCEL-REVIEW-001`: приветственный лабиринт увеличен и поднят в отдельную верхнюю зону; путь и точка получили спокойное одноразовое появление без бесконечного движения. Поведение остальных шагов онбординга не менялось.
- Все полученные Vercel-комментарии сведены в `MXL-VERCEL-REVIEW-001` с точными критериями и ссылками; циклы 1–3 реализованы, отправлены в draft PR #20 и ждут проверки владельцем в новом Preview.
- На «Сегодня» удалена шумная подпись под приветствием, карточка «Тема недели» получила ту же серую рамку, что и «Нить дня».
- Запрос напоминаний «как будильник» вынесен в отдельную заблокированную задачу `MXL-REMINDERS-002`, потому что он требует согласования продуктового поведения и проверки приватного backend/API.
- Финальная иллюстрация вечернего check-in увеличена до `184 × 184` CSS px и заполняет квадрат плотнее.
- Пять упражнений нейротренажёра получили крупные прямоугольные карточки и собственные смысловые рисунки в общем `SemanticGlyph`.
- Раскрытая «Нить дня» сокращена примерно вдвое по высоте; прямая шкала заменена волнистым маршрутом с четырьмя узлами.
- В карточках Собеседника, Наставника и Следопыта увеличено поле рисунка до `42%` высоты и сокращены пустые интервалы; внешний размер карточек сохранён.
- Утверждённый Today motion-вариант перенесён из dev-сравнения в обычный экран: «Нить дня», крупная «Точка внимания» и обновлённое «Самое важное» включены по умолчанию; `?today_compare=1` сохранён для отката и сравнения.
- Общий `SemanticGlyph` остаётся единственным источником рисунков для UI Lab и реальных карточек ритуалов, аскез, практик, статей и AI-персон.
- Починен `favicon.ico`/`favicon.svg` (404 в проде) — PR #12.
- Починена кнопка завершения check-in в вебе (`WebActionBar` в `CheckIn.jsx`) — PR #13.
- `WebActionBar` вынесен в переиспользуемый компонент, применён на `Rituals.jsx`/`Ascezas.jsx`/`ThemeScreen.jsx` — PR #14.
- Починена кнопка «Назад» в вебе на 13 экранах (`BackButton.jsx`, неверная проверка платформы) — PR #15.
- `MXL-WEB-BACKBTN` и `MXL-WEB-ACTIONBAR` закрыты — ручная проверка пройдена (со слов пользователя: 13 экранов + Telegram-регрессия; ThemeScreen/Rituals/Ascezas/CheckIn на реальном backend) — PR #16.
- Добавлен `MENTALIX_SEMANTIC_MOTION.md`, сверен с `DESIGN_SYSTEM.md` (расхождений в токенах нет; уточнена область действия правил анимации §4) — PR #17.
- Аудит `MXL-PWA-ICONS`: описана минимальная спецификация недостающих иконок манифеста (файлы не генерировались).
- Найден и исправлен дубль заголовка `## Сейчас` в `TASKS.md`.
- Временный локальный прокси `/api` → prod-backend использован для визуальной проверки формы веб-логина и затем откачен; `vite.config.js` в PR не входит.
- PR #18 с `AGENTS.md`, `CHANGES.md` и `TASKS.md` смёржен squash-коммитом `90023c`.
- Codex переработал геометрию нейротренажёра, общей аскезы, Наставника и Следопыта в едином `SemanticGlyph`; backend, API, данные, тексты и навигация не менялись.
- В UI Lab добавлена контрольная карточка общей аскезы для проверки нового знака рядом с двумя предметными аскезами.
- Исправлен неполный перенос UI Lab в реальные экраны: старые мотивы заменены общим `SemanticGlyph` в пустых состояниях ритуалов/аскез, на вступительном и финальном экранах дыхания, на экране фокуса и в карусели AI-персон.
- На главном экране нейротренажёра добавлен знак нейронной связи; рисунки практик и статей увеличены внутри существующих карточек. Логика, данные, навигация, backend и API не менялись.
- По прямому запросу владельца восстановлен dev-only proxy `/api` → Railway в `vite.config.js`, чтобы веб-вход и реальные данные работали на `localhost:5173` так же, как через rewrite на Vercel.
- Онбординг переведён на спокойную motion-систему: один переход шага, точечные transitions вместо `transition-all`, ручное подтверждение возраста и стабильное последовательное проявление финальных карточек.
- Для проверки реального компонента добавлен dev-only маршрут `?onboarding_preview=1`; он не попадает в production и не сохраняет данные при preview-пользователе.

**Изменённые файлы текущего переноса Card System**

- `src/lib/cardSystem.js`
- `src/components/CardSystemGlyph.jsx`, `src/components/CardSystemGlyph.css`, `src/components/CardSystem.css`
- `src/components/PracticeCard.jsx`
- `src/screens/Practices.jsx`, `src/screens/Today.jsx`, `src/screens/Rituals.jsx`, `src/screens/Ascezas.jsx`
- `src/screens/BrainTrainer.jsx`, `src/screens/Breathing.jsx`, `src/screens/Focus.jsx`
- `src/screens/mentalix/PersonaPicker.jsx`
- `CHANGES.md`, `TASKS.md`

**Изменённые файлы текущего Motion Kit**

- `src/main.jsx`
- `src/components/ui-lab/PracticeMotionKit.jsx`
- `src/components/ui-lab/PracticeMotionKit.css`
- `CHANGES.md`, `TASKS.md`

**Изменённые файлы предыдущего UI commit** (`0d420fc`, draft PR #20)

- `src/components/MazeLogo.jsx`
- `src/components/Motif.jsx`
- `src/components/SemanticGlyph.jsx`, `src/components/SemanticGlyph.css`
- `src/components/TodayMotionExperiment.jsx`, `src/components/TodayMotionExperiment.css`
- `src/screens/Onboarding.jsx`, `src/screens/Onboarding.css`
- `src/App.jsx`, `src/screens/Today.jsx`
- `src/screens/CheckIn.jsx`, `src/screens/BrainTrainer.jsx`
- `src/screens/mentalix/PersonaPicker.jsx`
- `CHANGES.md`, `TASKS.md`

**Исторический список файлов предыдущих сессий**

- `public/favicon.ico`, `public/favicon.svg`, `index.html`
- `src/screens/CheckIn.jsx`, `src/components/WebActionBar.jsx`
- `src/screens/Rituals.jsx`, `src/screens/Ascezas.jsx`, `src/screens/ThemeScreen.jsx`
- `src/components/BackButton.jsx`
- `MENTALIX_SEMANTIC_MOTION.md`
- `CHANGES.md`, `TASKS.md` (документация по ходу сессии)

Следующий большой список — исторический снимок до merge PR #19, а не текущий `git status`; он сохранён только как контекст предыдущей передачи:

```
 M CHANGES.md
 M DESIGN_SYSTEM.md
 M MENTALIX_SEMANTIC_MOTION.md
 M TASKS.md
 M src/App.jsx
 M src/components/ArticleCover.jsx
 M src/components/Motif.jsx
 M src/components/practice-art/AskesisArt.jsx
 M src/components/practice-art/BreathingArt.jsx
 M src/components/practice-art/FocusArt.jsx
 M src/components/practice-art/MeditationArt.jsx
 M src/components/practice-art/NeuroArt.jsx
 M src/components/practice-art/RitualsArt.jsx
 M src/main.jsx
 M src/screens/Ascezas.jsx
 M src/screens/BrainTrainer.jsx
 M src/screens/Breathing.jsx
 M src/screens/Focus.jsx
 M src/screens/Onboarding.jsx
 M src/screens/Practices.jsx
 M src/screens/Rituals.jsx
 M src/screens/Today.jsx
 M src/screens/mentalix/PersonaPicker.jsx
 M src/screens/mentalix/art/PersonaArt.jsx
 M vite.config.js
?? CLAUDE_UI_HANDOFF.md
?? src/components/SemanticGlyph.css
?? src/components/SemanticGlyph.jsx
?? src/components/TodayMotionExperiment.css
?? src/components/TodayMotionExperiment.jsx
?? src/components/ui-lab/
?? src/screens/Onboarding.css
```

Прокси `/api` снова добавлен в незакоммиченный набор по прямому запросу владельца после того, как локальный веб-вход перестал связываться с backend. В PR #18 он не входил. Остальное — незакоммиченная работа Codex (SemanticGlyph, UI Lab, Today motion-эксперимент), не трогать без согласования.

**Проверено**

- Card System: на авторизованном `localhost` открыты реальные экраны «Сегодня», каталог «Практики», вступительные экраны дыхания и фокуса, карусель Наставника; данные и действия остались прежними.
- Card System: точечный ESLint — 0 ошибок; предупреждения в `BrainTrainer`/`Breathing`/`Focus`/`Practices` существовали до переноса и относятся к прежним эффектам/таймерам. `npm run build` и `git diff --check` проходят.
- Новый SVG-компонент использует циклы `8.8–13.5s`, одну золотую точку и отключение движения через `prefers-reduced-motion`; функциональная фазовая анимация дыхания не заменялась.
- Card Lab: точечный ESLint — без замечаний; полный `npm run lint` — 0 ошибок и 34 прежних предупреждения; `npm run build` и `git diff --check` проходят.
- Card Lab визуально проверен на `390 × 844`: все пять направлений переключаются, раскрытие карточки и выбор роли работают, стоп движения выставляет `data-animated=false`, горизонтального переполнения и ошибок консоли нет.
- В обычной production-сборке отсутствуют `card_lab`, `CardDirectionsLab` и `mx-card-lab`; лабораторный чанк создаётся только для Vercel Preview/development.
- Цикл 1 онбординга визуально проверен в dev-only `?onboarding_preview=1` на `390 × 844` и `320 × 568`: лабиринт имеет итоговый размер `266 × 266` на основном мобильном viewport, кнопка полностью видна, `scrollWidth` совпадает с шириной viewport, на маленьком экране контент не обрезан.
- Покадрово и по вычисленным стилям подтверждены длительности: `1600ms` для дорисовки пути и `520ms` для появления вступительного блока; ошибок и предупреждений в консоли preview нет.
- Полный `npm run lint`: 0 ошибок, 34 ранее существовавших предупреждения; новые файлы цикла 1 не добавили предупреждений (`Onboarding.jsx:132` — прежний `no-empty`).
- `npm run build` проходит из фактической папки `work/frontend-deploy`; остаётся прежнее предупреждение Vite о чанке больше 500 kB. `git diff --check` проходит.
- Точечный ESLint по затронутым JSX-файлам проходит без ошибок.
- Полный `npm run lint` завершён: 0 ошибок, 34 существующих предупреждения в старом коде; новых lint-ошибок текущая UI-доработка не добавила.
- `npm run build` проходит; остаётся прежнее предупреждение Vite о чанке больше 500 kB.
- `git diff --check` проходит.
- В dev-only UI Lab на мобильной ширине проверено раскрытие/сворачивание новой компактной «Нити дня»; путь и подписи не обрезаются.
- ESLint по затронутым UI-файлам проходит без ошибок; предупреждения в игровых эффектах `BrainTrainer`/`Breathing`/`Focus` существовали до этого визуального переноса и не относятся к новым SVG.
- `npm run build` проходит; остаётся прежнее предупреждение Vite о чанке больше 500 kB.
- В `dist` отсутствуют маркеры `UiExperiments`, `mx-lab` и `ui_lab`: лабораторный код не попал в production-сборку.
- `git diff --check` проходит.
- `npx eslint src/components/SemanticGlyph.jsx src/components/ui-lab/UiExperiments.jsx`: проходит без предупреждений.
- `npm run build`: проходит на текущем незакоммиченном UI-дереве; остаётся прежнее предупреждение Vite о чанке больше 500 kB.
- ESLint по файлам визуального выравнивания (`Rituals`, `Ascezas`, `Breathing`, `Focus`, `BrainTrainer`, `PersonaPicker`, `ArticleCover`, `Practices`, `SemanticGlyph`) проходит без ошибок.
- Повторная production-сборка после визуального выравнивания проходит из фактической папки `work/frontend-deploy`; в `dist` по-прежнему нет маркеров UI Lab.
- Локальный dev-proxy проверен через `GET http://localhost:5173/api/analytics/pulse`: `200`, `application/json`, ответ Railway получен.
- Во встроенном браузере открыты и проверены обе страницы: UI Lab по точному адресу `?ui_lab=1` и авторизованный экран «Сегодня» на `localhost:5173` через сохранённую локальную сессию.
- Полный preview онбординга пройден до финала: выбор фокуса, ручное подтверждение возраста, выбор напоминания, проявление трёх карточек и доступная кнопка «Войти».
- ESLint для `Onboarding.jsx`/`main.jsx`, `npm run build` и `git diff --check` проходят после замены анимаций.
- Статический сравнительный монтаж до/после для `neuro`, `asceza`, `mentor`, `pathfinder` проверен; обрезки и выходов геометрии за рабочую область нет.
- Визуальная проверка (`claude-in-chrome`, локальный dev-сервер, без реального backend, кроме отмеченного): CheckIn — полный морнинг-флоу до «Завершить»; Rituals — список, кнопка «Назад», создание ритуала с `WebActionBar`; Ascezas — форма создания. `MXL-WEB-BACKBTN` (13 экранов + Telegram) и `MXL-WEB-ACTIONBAR` (ThemeScreen/Rituals/Ascezas/CheckIn на реальном backend) — по заявлению пользователя, не перепроверялись мной напрямую в этой сессии.

**Не проверено / открыто**

- Перенос Card System проверен владельцем в Vercel Preview и разрешён к выпуску. Полноценную проверку внутри Telegram/iPhone после production-деплоя выполнить отдельно; при визуальной регрессии доступен `?card_system=0`.
- Исходный Card Lab не проверялся на реальном iPhone отдельно, однако владелец визуально утвердил направления и разрешил подготовить перенос в реальные экраны; окончательная мобильная проверка относится к `MXL-CARD-SYSTEM-001`.
- Циклы 1–3 `MXL-VERCEL-REVIEW-001` опубликованы в новом Vercel Preview, но ещё не подтверждены владельцем и не проверены на реальном iPhone внутри Telegram.
- Для `MXL-REMINDERS-002` не выбран режим повторения и не проверен приватный backend/API; реализацию не начинать до снятия блокера.
- Реальные авторизованные экраны нейротренажёра, вечернего check-in и AI-персон после этой доработки нужно проверить на телефоне; агент не запускал email OTP.
- Telegram/iPhone ещё не проверен. Backend/API не затрагивались.
- Живое раскрытие/сворачивание «Нити дня» на авторизованном экране «Сегодня» и визуальные состояния реальных карточек после переноса.
- Telegram/iPhone и мобильный viewport не проверялись; локальный desktop-браузер и dev-сервер работают.
- Авторизованный экран «Сегодня» открылся через существующую сохранённую сессию. Остальные реальные карточки (`Rituals`, `Ascezas`, `Practices`, роли Наставника) ещё нужно пройти вручную.
- `TodayMotionExperiment.jsx`/`.css` и остальная UI Lab не менялись в этом цикле и остаются незакоммиченными.
- `MXL-PWA-ICONS` — сами файлы иконок не созданы, только спецификация.
- Полный email OTP не запускался агентом: это отправляет код на внешний адрес. Dev-proxy уже работает; для ручного входа нужен действующий email вместо тестовой строки.

**Следующий шаг**

- После merge PR №27 владелец открывает production на телефоне и проверяет «Сегодня», «Практики», ритуалы/аскезы, нейротренажёр, дыхание, фокус и «Наставника». При расхождении сравнивает с прежним вариантом через `?card_system=0` и присылает скриншот.
- Владелец открывает новый [Vercel Preview ветки `codex/mobile-ui-density`](https://mentalix-git-codex-mobile-ui-density-smiraandre2-8311s-projects.vercel.app), один раз входит в веб-версию и проверяет онбординг, «Сегодня», нейротренажёр, финальный вечерний check-in и три карточки наставников.
- После визуальной проверки владелец либо присылает скриншоты расхождений, либо отдельно разрешает перевести draft PR #20 в готовый и выполнить merge; без такого разрешения `main` не менять.
- Напоминания `MXL-REMINDERS-002` оставить отдельной задачей до выбора режима повторения и проверки backend-контракта.
- Пользователь открывает Vercel preview текущего draft PR на телефоне и проверяет «Сегодня», «Практики» → ритуалы, аскезы, нейротренажёр, дыхание и фокус; затем «Наставник», «Библиотеку» и новый онбординг.
- После мобильной проверки пользователь либо присылает один свежий скриншот конкретного расхождения, либо отдельно подтверждает merge в `main`; без такого подтверждения draft PR не сливать.

**Нельзя менять без согласования**

- Карточки статей — эталон визуального стиля.
- Реальные экраны нельзя менять на основании одного лабораторного эксперимента.
- Backend не менять без необходимости.
- Не выполнять commit, push, merge или stash без прямого согласования.
