# Контекст проекта — вставлять в начало нового чата

Проект: Mentalix — Telegram Mini App (система ритуалов, аскез, чек-инов).

Фронт: github.com/Smira31/Mentalix (публичный) — React + Vite + Tailwind,
деплой Vercel, автосборка при пуше в main.
Бэкенд: репозиторий mentalix-bot (приватный) — FastAPI + SQLAlchemy + aiogram,
деплой Railway. Папки: backend/ (API), bot/ (Telegram-бот).

Дизайн: стиль stoic. — плоский тёмный/светлый минимализм, одна золотая
акцентная краска (#D9B45B тёмная тема / #8B6914 светлая), шрифт Onest,
символ — лабиринт, заполняющийся золотом по прогрессу.
Тон: на «ты», коротко, без «успешного успеха». Аудитория 25–35.

Что уже сделано и что в планах — см. CHANGES.md, TASKS.md, STOIC_FEATURES.md
в корне репозитория.

---

## Шаблон запроса в новом чате

Проект Mentalix, репозиторий github.com/Smira31/Mentalix — склонируй
и посмотри код сам. Стиль и планы описаны в CONTEXT.md, TASKS.md, CHANGES.md.

Задача: <что нужно сделать>

Отдай изменённые файлы по одному (не архивом), скажи, в какую папку класть.

---

## Правила экономного общения

1. Новый чат на каждую задачу. Длинный чат = я перечитываю всё каждый раз.
2. Не пересылать заново то, что есть в репозитории — я прочитаю сам.
3. Видео и большие PDF — только когда без них никак (это самое дорогое).
4. Скриншот вместо описания — хорошо. Скриншот + подпись «что не так» — лучше.
5. Просить файлы поштучно, если меняется 1–3 файла: меньше вывода, меньше
   шансов ошибиться при загрузке на GitHub.
6. Складывать несколько мелких правок в один запрос вместо пяти подряд.
7. Когда задача закрыта — закрывать чат, а не продолжать в нём следующую.

## Правило загрузки на GitHub (чтобы не терять файлы)

- Тащишь папку `src` — стой в КОРНЕ репозитория.
- Тащишь голые файлы (App.jsx, api.js) — сначала ЗАЙДИ в нужную папку.
- После коммита проверь: сколько файлов изменилось. Ноль — значит не доехало.
