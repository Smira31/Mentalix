name: Vercel Production Watchdog

# Firebase Hosting is now the target for frontend Preview and Production.
# This workflow is intentionally manual and never calls the Vercel API.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Retired after Firebase migration
    runs-on: ubuntu-latest
    steps:
      - name: Explain migration target
        run: echo 'Vercel watchdog retired; Firebase Hosting is the deployment target.'
